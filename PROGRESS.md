# PROGRESS.md — 悬浮球拖放保存（feature/drag-to-ball）

> 开工时间：2026-09-16 23:0x｜设计已冻结，禁区清单见 `REGRESSION.md` B-15 段的「改这块前必读」

## 开工回执

1. **理解的目标**：把文字/文件拖到悬浮球，文字→立即存本地笔记+闪绿+竖向小条（AI 问答／得到大脑）；文件→什么都不做+闪绿+紧凑卡片（AI 问答／存网盘／发得到大脑／更多渠道）。总开关默认关。
2. **阶段顺序**：①设置项三件套 → ②两个浮层窗口 → ③悬浮球拖放接入 → ④四个动作接线 → ⑤AIDialogHelper.Open 附件参数扩展 → ⑥回归（快层21／慢层166／快照／9 条现象级验收）。
3. **最大风险**：拖放期间 `GetForegroundWindow()` 拿源程序、微信 temp 图片随时被清、"零副作用"——拖入不落地、不点则无痕，是本功能最容易悄悄破的底线。
4. **禁区回查结论**：不做悬停触发、不拖拽中弹菜单、浮层必须独立窗口（透明区穿透）、不做浏览器虚拟图片、无暂存区、小条竖向无图标无提示语、卡片无图标整行悬停、副行"大小 · 来自来源"。

## 阶段完成记录（2026-09-16）

| 阶段 | 内容 | 验证证据 |
|---|---|---|
| 1 | 设置项三件套（AppSettings + 「显示」板块控件 + LoadSettings 回填 + Changed 事件 + REGRESSION 登记） | 编译 0/0；快照 24 出图：开关默认未勾选、滑块 90% / 3 秒 |
| 2 | 两个浮层窗口（DropActionStrip / DropActionCard） | 编译 0/0；快照 19~23 出图：小条 96 宽竖向、卡片 ≤276 贴合内容、无图标、末项灰色 |
| 3 | 悬浮球拖放接入（AllowDrop + 四事件 + 判定 + 展开 + 闪绿） | 编译 0/0；慢层「拖放保存组」46 条断言判定顺序与取值口径 |
| 4 | 四个动作接线（存笔记 / 存网盘 / AI 问答 / 得到大脑）+ Open 附件参数扩展 | 编译 0/0；动作链路全部复用现有服务，未新增外发机制 |
| 5 | 回归 | 快层 **21/21**、慢层 **212/212（0 失败，基线 166 未退）**、快照 24 张、编译 0 警告 0 错误 |
| 6 | **首测修复**（用户实机发现，commit `cc1db09`） | 点任一动作时白弹「界面错误」框 → `DropActionCard.Dismiss()` 二次 Close。已修（幂等 + `OnClosing` 上锁 + 摘监听），`DropActionStrip` 同类加固。编译 0/0；快层 21/21、慢层 212/212 复跑无退 |

## 首测修复详情（2026-09-16 23:4x，commit `cc1db09`）

**症状**：拖文件到球 → 点「AI 问答」或「存到网盘」→ 先弹「FocusCapture 遇到一个界面错误：在窗口关闭期间…」，
点掉之后功能照旧可用。crash.log 于 23:33~23:38 连报 7 次。

**根因**：`DropActionCard` 关闭入口五个（三按钮 / 点外面 / Esc / 兜底超时）互相踩 ——
按钮路径「先 Dismiss() 再抛事件开 AI 窗」，AI 窗一激活本窗就 `Deactivated`，`_armed` 早已 true
→ 二次 `Close()` → WPF `VerifyNotClosing()` 抛异常。栈顶指名到行：
`DropActionCard.Dismiss()` line 89 → `Window_Deactivated()` line 112。**功能没事，纯白弹框。**

**修法**：① `Dismiss()` 幂等；② `OnClosing` 上锁 + 停表（覆盖 MainWindow 直接 `Close()` 的路径）；
③ 关窗前摘 `Deactivated` 监听；④ 顺手修掉自己引入的 CS8622（`Window_Deactivated` 签名改 `object?`）。

**遗留**：`crash.log` 里 2026-08-27/28 另有 8 条同类报错，是待办汇总窗被二次 `Show()`，**v3.8 已修**，与本条无关。

### 尚未完成（需要你动手，无法由自动化代劳）

9 条**现象级验收**里有 8 条需要真人拖放（OLE 拖放没有可编程入口）。
机器能证明的部分（默认关、判定规则、命名规则、零副作用的判定段）已进检查点；剩下的是终态观察：
拖文件看卡片、拖文字看笔记、拖微信图片看命名、点四个动作看落点、拖滑块看浮层透明度。

### 实施中发现的方案问题（未自行改设计，等你定）

1. **板块落位自相矛盾**：设计稿原写放「外观」板块（`PanelAppearance`），但**实际三个透明度滑块都在「显示」（`PanelDisplay`）**，
   不在 `PanelAppearance`。已按"可验证锚点"（现有三个透明度滑块所在板块）落在**「显示」**。
   要挪到「外观」= 把 `SettingsWindow.xaml` 里那四块整段剪切过去，一处改动。
2. **卡片关闭时机方案没写**：已实现为「点卡片外面 / Esc / 兜底 60 秒」+ 400ms 防抖（防激活没抢到造成一闪就没）。
3. **拖放浮层多屏**：定位用 `SystemParameters.WorkArea`（主屏），与悬浮球既有口径一致；跨屏时浮层可能弹在主屏。

