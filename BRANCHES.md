# BRANCHES.md - FocusCapture 分支索引与管理规范

> 创建：2026-09-01 ｜ 2026-09-09 瘦身定稿：只登记活跃分支 + 放弃/未并入归档；已完全并入的分支不再登记（`git log` 即归档）
> 用途：全项目分支的唯一登记表。每次建分支、合并、删除后必须同步更新本表。
> 定位：「施工日志」——但只记 git 现查不到的事。

---

## 一、分支命名规范（铁律）

| 前缀 | 用途 | 示例 |
|------|------|------|
| `feature/xxx` | 新功能开发 | feature/import-range-search |
| `fix/xxx` | bug 修复 | fix/recycle-baml-error |
| `docs/xxx` | 文档、落地页 | docs/website-v2 |
| `release/vX.Y.Z` | 发版分支 | release/v0.3.0 |
| `experiment/xxx` | 实验探索，做坏即弃 | experiment/xxx |

**禁止**：
- `codex/` 前缀（2026-09-01 已全部清理，属历史遗留，不再使用）
- 分支名带 phase 编号（阶段拆在提交信息里，不拆分支）
- 无类型前缀的裸分支名

## 二、分支生命周期铁律

1. **一个功能 = 一个分支**：从 main 拉 `feature/xxx`，开发完合并回 main
2. **合并即删**：功能合并进 main 后，立即删除本地 + 远程分支，**不登记归档**（git log 就是归档）
3. **main 只做合并接收方**：绝不在 main 上直接改代码
4. **开工前查重**：`git log --all --grep=功能关键词` 先查 main 是否已有类似功能，防重复开发
5. **阶段拆在提交信息**：多阶段功能只用一个分支，commit message 标注阶段（如 `feat(todo): phase2 面板徽标`）

## 三、当前分支索引

| 分支 | 功能板块 | 状态 | 最后提交 |
|------|---------|------|---------|
| main | 主线（含 2026-09-11 自动化检查点体系、2026-09-12 行身份改造 + 全局热键、2026-09-12 灵感速览时间筛选重构、2026-09-13 灵感速览可组装标题栏 + 窗口壳升级 + 界面快照工具、2026-09-14 AI 问答附件（图片/文档混排 + 附件服务 + 自绘预览浮层与双击大图）+ 输入区 8 项交互修复、2026-09-16 百度网盘本地缓存（含删除接口定型与到期清理重构，REGRESSION B-14）、2026-09-16 悬浮球拖放保存（文本/文件拖入 → 小条或卡片 → 存笔记／存网盘／AI 问答／得到大脑，REGRESSION B-15）、2026-09-17 Agent 工具 15→26 + 文档解析基座 + 模型时间上下文（REGRESSION B-16）） | 活跃 | 不硬编码，见 §五 注 |
| fix/badge-and-icon-sync | ① 悬浮球角标「圆被窗口切一块 + 数字偏上」修复（`Windows/FloatBall.xaml` 纯样式，不碰逻辑）② 自定义图标**两处同源**：新增 `Services/AppIconService`（窗口任务栏图标 + 托盘图标 + 沉浸记录托盘图标共用一张图），ClassHandler 一处注册覆盖全部窗口，另修掉托盘 HICON 的 use-after-free 隐患 | 未合并 | 见下登记段 |

> **只登记「未合并的活跃分支」，合并即删即除名。**
>
> **2026-09-18：`fix/quickview-clipboard-dblclick` 已 ff 并入 main 并除名**（`8eaf659..af8d33b`，12 文件 / +382 −30；
> 三个提交：`67d6e84` 修复双击进不了编辑 → `2cca572` 取消单击复制 → `af8d33b` 新增 `tools/clipdiag` 占用探针）。根因链：`NoteItem_Click` 双击的第一下先走「单击复制」→
> `Clipboard.SetText` 在剪贴板被其他进程占用时抛 `CLIPBRD_E_CANT_OPEN` → 该处无兜底，冒泡到 `App.DispatcherUnhandledException` 弹模态框 →
> 第二下点击落在弹框上，`ClickCount==2` 永远到不了（右键「编辑」走 `CtxEdit_Click`，不经剪贴板，故一直正常）。
> **crash.log 显示该故障 2026-08-09 即已存在**（当时行号 261），不是新回归。
> 修法：新增 `Services/SafeClipboard`（指数退避重试 25/50ms、仅对暂时性异常重试、**永不抛异常**、空内容不写；
> 刻意零 WPF 依赖 —— 底层写入委托由调用方注入，以便快层工程直接链接做检查点），替换全部 5 处 `Clipboard.SetText`。
> 快层 21 → **31** 条（新增 [5] 剪贴板写入容错组 10 条）；慢层 **257 条全过**（15.6s）；REGRESSION B-12 补 2 条验收项。
> ⚠ **人工验收未做**：真实环境「双击笔记进编辑态」「剪贴板内容不被双击改动」两条待用户实测。
>
> **同日第二轮（用户拍板，方案 A）**：**单击复制机制整个取消** —— 用户反馈「双击要等好久才进编辑」，
> 根因正是双击的第一下必然先同步写一次剪贴板（OLE 写入阻塞 UI 线程，占用时还要退避重试）。现在：
> **单击无动作、双击直接进编辑**，复制只留给右键菜单「复制」（仍走 `SafeClipboard` 容错）；
> 同时把 `BeginEditNote` 聚焦的 Dispatcher 优先级从 `Background` 提到 `Loaded`
> （仍晚于模板切换所需的 DataBind/Render，但不再排在列表刷新之后）。
> REGRESSION B-12 相应改为「双击：立即进编辑，且**剪贴板内容不得被改动**」「单击：什么都不发生」两条。
> 第一轮那份「剪贴板占用时单击只出红字提示」的验收项随机制取消而作废（右键复制仍适用同一容错）。
>
> **复核（同日第三轮，用新探针实测）**：采样 650 次、失败 20 次（3%），**20 次 100% 由
> `GameViewer(pid 10464) class=UURemoteClipboard` 持有** —— 网易UU远程
> （`C:\Program Files\Netease\GameViewer\bin\GameViewer.exe` v4.40.1.2090）的剪贴板同步：
> 每次剪贴板内容变化后立刻抓取并持有剪贴板 **2.4~4 秒**（err=5 拒绝访问）；占用窗口之外 **630/630 全部成功**。
> → 结论：**非本应用 bug**，是外部程序周期性抢占系统剪贴板。用户拍板「UU 不常用」，
> 故代码侧的异步补写（B）与编辑框 Copy 拦截（C）两个候选方案**均未实现**。
> ⚠ **对照实验未做**（退出 UU 远程后复测失败率是否归零）—— UU 仍在运行；现有证据是
> 「占用者分布唯一 + 占用窗口外 100% 成功」两条。
>
> **合并**：`git merge --ff-only` 成功（`8eaf659..af8d33b`）；第 5 次触发 git 2.55 索引异常
> （`git checkout main` 后 **74 个文件误标 `D`**），按既定 restore 恢复 —— 恢复后跟踪文件 **174** 齐备，
> 合并后 **178**（+4 新文件：`SafeClipboard.cs` + clipdiag 三件套）。
> ⚠ **本次未推送双远程**（用户要求暂不推送）：GitHub/Gitee 仍停在 `8eaf659`，本地 main 已到 `af8d33b`。
>
> 2026-09-17（第二批）**`feature/agent-tools-batch1` 已 ff 并入 main 并除名**（`20ce6a1..d10b8e5`，20 文件 / +2971 −154）。
> 三个提交：`fc286aa` Agent 工具 15→26 + 文档解析基座 + 模型时间上下文 → `b800b33` export_notes 精选导出 → `d10b8e5` 慢层逐组计时 + 断网组优化。
> 分支已删（无远程副本），双远程已推送（含本条登记提交，最终哈希以 `git ls-remote` 实测为准）。
> 合并第四次触发 git 2.55 索引异常（`git checkout main` 后 **68 个文件被误标 `D`**），按既定流程 restore 恢复；
> 恢复后跟踪文件 **177** 齐备（切换前 `git status` 干净、无 `??` 未跟踪文件，故无需备份）、工作区干净、编译 0 警告 0 错误。
> 决策纪律（原文档已解耦，可删）：开工前先做影响面分析、逐项与用户确认后才动代码；候选工具 21 条。
> 关键决策四条：① 改内容 = **原地替换**（不是追加编辑行），且**笔记与待办的旧内容都先写回收站**兜底
> ② PDF 引 PdfPig（Apache-2.0 纯托管，**扫描件读不了会明说，不做 OCR**）③ 输入框一并支持 xlsx
> ④ 导出落 `AppSettings.ExportFolderPath`、**不上传网盘**。
> 明确放弃 `read_cloud_image`：tool 消息在协议层带不了图片部件（证据见 REGRESSION B-16）。
> 慢层检查点 212 → **257**（新增「Agent 工具组」42 条）；快层 21 条不受影响。
> ⚠ **人工验收未做**：B-16 的 ⚠ 条目（模型会不会挑对工具 / 会不会谎报 / 拖 PDF 能读 / 拖扫描件会明说读不了 / xlsx 日期列不显示成 44927 / "今天几号"隔天跟着变）只能靠真实对话验收，机器测不了。
>
> 2026-09-17：`feature/drag-to-ball` 已 ff 并入 main（`db6559d..4998550`，20 文件 / +2834 −3），分支删除除名，
> **双远程（Gitee origin + GitHub）均已推到 `4998550`**。三个提交：`adbc19d` 功能 → `cc1db09` 首测修复 → `4998550` PROGRESS 记录。
> 慢层检查点 166 → **212**（新增「拖放保存组」46 条）；验收清单 REGRESSION B-15（**禁区见该段的「改这块前必读」**）。
> 合并第三次触发 git 2.55 索引异常（**111 个文件被误标删除**），按既定流程 `git restore --source=HEAD --staged --worktree .` 恢复，恢复后 161 个跟踪文件齐备、工作区干净。
> ⚠ **该恢复命令的边界（2026-09-17 实测，差点再吃一次教训）**：它是**全树通配路径**——
> 会删掉「**已暂存但 HEAD 里没有**」的文件（`A ` 状态，索引+磁盘一起没），**但不动未跟踪文件**（`??`）。
> 实测对照：`A staged.md` → 消失；`?? untracked.md` → 存活。
> 推论：**索引损坏时 `git status` 本身不可信**（异常当场把 111 个跟踪文件标成删除），
> 所以**跑这条命令之前，先把 `??` 未跟踪文件清单抄下来、并把它们备份出仓库**，别假设"是 `??` 就一定安全"。
> （本轮确实丢了一个未跟踪文件 `docs/悬浮球拖放-开发提示词.md`；机制无法唯一确定 —— 可能是索引异常卷走，
> 也可能是索引当时已把它纳入条目后被 restore 清掉。**功能不受影响，该文件使命已完成**，如实留痕。）
> **首测教训（用户当晚 23:33 实机发现，已修）**：点卡片/小条任一动作时白弹「FocusCapture 遇到一个界面错误」。
> 根因＝卡片关闭入口有五个（三按钮／点外面／Esc／兜底超时）互相踩 → 二次 `Close()` → WPF `VerifyNotClosing()` 抛异常。
> **功能其实一直是好的** —— 异常发生在关闭流程的再入里，动作早已派发出去（用户点掉框后问答/上传都正常）。
> 修法：`Dismiss()` 幂等 + **`OnClosing` 里也上锁**（覆盖 MainWindow 直接 `Close()` 的路径，别指望调用方守规矩）+ 关窗前摘 `Deactivated` 监听；
> `DropActionStrip` 同类加固。**通则：凡有多个关闭入口、或带 Deactivated/Closing 回调的窗口都得这么写。**
> ⚠ **当时人工验收未做**：9 条现象级验收里 8 条需要真人拖放（OLE 拖放没有编程入口），B-15 逐条标 ⚠、一条都没敢标绿。
> ✅ **2026-09-17 结案**：B-15 已由用户人工验收完成。
>
> 2026-09-16（晚）：`feature/baidu-netdisk-cache` 已 ff 并入 main（`00e4969..2ada001`，39 文件 / +8493 −28），分支删除除名。
> 合并再次触发 git 2.55 索引异常（58 个文件被误标删除），按既定流程 `git restore --source=HEAD --staged --worktree .` 恢复；
> 恢复后工作区与 HEAD 完全一致、161 个跟踪文件齐备、编译 0 警告 0 错误。
> 2026-09-16：新建 `feature/baidu-netdisk-cache`（当时未合并、未推送；**当晚已并入 main 并除名，见上**）。慢层检查点 86→107→121→128→134→139→143→**166**（第九轮 +23：到期清理语义 / 待清理标注 / 退避 / 彻底删除 / 按月路径 / MetadataEquals 含 CloudState）。
> 三条红线写在 REGRESSION B-14，动这块之前必读。功能与验收清单见 B-14（含四次事故的终态验收）。
> 同日**四次**上传事故，形状完全相同 —— 不是「逻辑写错」，是**把外部约定当成了事实**：
> ① 沙箱守卫粒度错位（「列 /apps」被自己的守卫拒掉）
> ② 响应 `block_list` 被当 md5 字符串读（它其实是数字序号）
> ③ **秒传判据用错字段**（拿 `return_type` 当秒传信号，真实判据是「待上传分片列表是否为空」）
> 　＋ create / 建目录漏传 `rtype` ＋ 工具输出缺上传状态，导致 AI 谎报"已上传成功"
> ④ **分片上传域名写死 `pan.baidu.com`** → 403；官方要求"传数据前先调 locateupload 取域名"
> 四次的根因与教训都写在 B-14 的事故记录里。**碰这块之前：先拿一次原始响应，别再推断。**
> **收尾实测（2026-09-16）**：用本机令牌把整条链路直接跑通（precreate → locateupload → superfile2 → create → list），
> 网盘上确实出现了文件。四个接口的真实响应样本已写进 REGRESSION B-14 —— 以后再碰这块先看样本，别猜。
> 同一批还发现 `filemanager&opera=delete` 在沙箱里一律 errno=2（删除功能可能不被平台允许，待再验）。
>
> **第九轮（2026-09-16 下午，用户三问触发的核实 + 修复）**：
> ① **删除接口已定性** —— `filemanager&opera=delete` 穷举 12 种参数形态全废（errno=2），
>   **换旧式 `method=delete&path=<完整路径>` 一次即成功**；成功响应**没有 errno**、只接单个 path 不认 filelist。
>   顺带用它的"能删目录"把当初建目录 bug 造的三个垃圾空目录清掉了。
> ② **到期清理重构** —— 旧顺序「云端删成功 → 才打墓碑 + 贴淘汰标记」让删除接口一坏整条链卡死
>   （云端没删、本地没清、还每 30 分钟整批重试）。改为**本地先推进、云端尽力而为**：
>   到期即贴淘汰标记 + 标注「云端待清理」（不打墓碑），删除成功才升级为「已到期清理」；
>   失败指数退避 + 上限 3 次，用户点「重试云端清理」可立即再试。
> ③ **附件云端按月分目录**（`attachments/2026-09/`）—— 人工兜底从"逐文件勾选"降到"一个月删一次文件夹"。
> ④ **彻底删除接用户入口**（云文件卡片第四个按钮，快照 18 守护）；AI 依然没有删除工具（红线）。
> ⑤ 新增 `tools/baidu-diag`（引用主项目做**同栈**真机验证：list / probe-delete / e2e），
>   端到端 13 项断言全过 —— "不要人肉测试"的落点。
> 教训与 08/09 轮同源：**别让本地状态推进依赖外部接口的成败**。
>
> 2026-09-14：`feature/rich-input-attachments` 已 ff 并入 main 并删除（4 个提交：`63dc93f` 附件功能 → `5506ac9` 分支登记 → `d70719e` 输入区 8 项交互修复 → `aa2ecdf` 登记更新），慢层检查点 75→86（新增 H 组 11 条）。**合并时再次触发本机 git 2.55 索引异常**（77 个文件被误判为删除且真的从磁盘消失），按既定流程 `git restore --source=HEAD --staged --worktree .` 恢复，恢复后 `HEAD^{tree}` 与分支一致、140 个跟踪文件齐备。

## 四、放弃 / 未完全并入归档（本表核心价值）

> 这些分支的末位提交**没有**完全进 main，功能 main 里没有完整版，丢了就真丢了，故永久登记。
> 找回：`git branch <name> <hash>` —— ⚠ **只靠 `git reflog` 兜底，能撑多久不确定**（受 `gc.reflogExpire` / `gc.pruneExpire` 策略影响，可能短到 2 周）。
> 想让它**永久**可恢复，唯一可靠办法是 `git tag <tag> <hash>`（tag 会让对象永不被 gc 回收）。
> 末位提交是否已并入 main 以 `git merge-base --is-ancestor <hash> main` 实测为准（2026-09-09 全部实测过，此前文档 5 处「功能已在 main」的记载是错的）。

| 原分支 | 功能板块 | 备注 | 最后提交 |
|--------|---------|------|---------|
| feature/reminder-sound | 提醒音效 MVP（4 款内置 + wav/mp3 自定义 + 音量/试听） | 确认放弃（2026-09-05） | d620f79 |
| feature/annotation-quote | 批注功能（选中文字一键批注，UIA 三层取文） | **永久搁置**，未来扫描自动跳过 | f33d0d0 |
| feature/build-trim-iconfix | 构建瘦身（锁 RID win-x64）+ 图标嵌入修复 | 末位提交未并入 main | 27b1c02 |
| codex/quest-v3-sync | 云同步 v3 | 主体已合入，独有 docs 提交 | 00306cb |
| feature/license-gate | v0.2.0 发版：LicenseGate + 设置面板大改版 + 输入框 v3.6 | 功能已 cherry-pick 进 main，独有 docs 提交 | 2f752f0 |
| feature/website-landing | 官网落地页 v1（被 v2 替代） | 有独有提交未并入 | 349f767 |
| feature/inspiration-sync-buttons | 灵感速览云同步入口 / 回收站多选（早期并行版） | 有独有提交未并入 | e2df13b |
| feature/sync-line-identity | 行身份改造：行 ID 去路径 / 原地改行补墓碑 / 未到期待办全库扫行 / 空上传时刻补齐 | **代码已并入 main（2026-09-12）**；独有内容是 `MIGRATION.md`（含本机绝对路径的一次性迁移指引，已加入 .gitignore，**不推送远端**，只留本机工作区） | 9ada255 |
| feature/quark-cloud-drive | 夸克网盘适配器（借道官方 CLI）：`Services/Destinations/Quark/` 四文件（CliRunner / Destination / NodeLocator / RuntimeManager，约 1200 行）+ 设置页安装与授权 + AI 问答工具化，合计 **13 文件 / +1931 −2** | **2026-09-17 按用户指令彻底删除本地分支**（从未推送 → 无远程副本，也无 tag，**已无任何 ref 指向**）。搁置原因：官方 CLI 校验宿主白名单、第三方无门（假绿实验见 §一 第 15 条）。**恢复只能靠 reflog，过期即永久丢失**。⚠ 补登缘由：9-09「瘦身定稿」重写本表时把它漏掉了 | aa96192 |

> **`feature/quark-cloud-drive` 删除留档（2026-09-17）** —— 丢了什么，记清楚：
> 5 个提交（`2a5ff50` 接入夸克网盘 → `cc1793f` 支持上传对话附件 → `e5f854f` 登记 → `8cdc1a7` 补记「可行性结论：本方案不成立」→ `aa96192` 标注搁置），
> 内容含 **`docs/夸克网盘接入方案.md`（285 行，随分支删除已不在仓库）** —— 那份文档的结论「本方案不成立」本身有价值（负面结论也是结论）。
> **结论层已另有留存**（项目 MEMORY.md §三 第 15 条「夸克适配器三特性」+ skill `platform-integration-recon`），
> 但**文档正文随分支一起没了**。若要救回：`git branch tmp-quark aa96192`（趁 reflog 还在），或先打 tag 保永久。

## 五、远程分支状态

> **本表不硬编码哈希 —— 只记「结构」**（有几个分支、有没有 tag、有没有残留），具体哈希一律以 `git ls-remote --heads <remote>` 实测为准。
> 理由（2026-09-17 补注，就是一处漂移）：本表原写「实测 main = `4998550`」，其后又落了 docs 提交（`20ce6a1`），登记值当场过期。
> **硬编码哈希必然漂移 —— 记住「怎么查」比记住「查到什么」有用。**
> 本地 `refs/remotes/` 缓存可能滞后（本机 git 索引异常史见工作区记忆）。

| 远程 | 分支 | tag | 备注 |
|------|------|-----|------|
| Gitee（origin） | 仅 main | v0.1.0、v0.2.0 | 旧分支已删（2026-09-01）；除 main 外无分支。**2026-09-17 最后一次实测：与本地 HEAD 一致** |
| GitHub | 仅 main | v0.1.0、v0.2.0 | 2026-09-02 删 4 个残留；`feature/license-gate` 已于 2026-09-12 补删。**2026-09-17 最后一次实测：与本地 HEAD 一致**（17:3x 失败、17:5x 重试成功，详见下方证据块） |

> **⚠ GitHub 在 WorkBuddy 沙箱里会间歇性连不上（2026-09-17 实测，修正「网络窗口」这个模糊说法）**
>
> 同日 17:3x 连续 3 次 `git push github main` 全失败（`RPC failed; curl 7 CONNECT tunnel failed, response 502`），遂做对照实验：
>
> | 路径 | 结果 |
> |---|---|
> | 经沙箱代理（`HTTP_PROXY=http://127.0.0.1:46638`）访问 `github.com` | **`CONNECT tunnel failed, response 502`**（10 秒） |
> | 经同一代理访问 `gitee.com` | **200，0.41 秒** |
> | 绕过代理直连 `github.com:443` | **连不上，21 秒超时**（沙箱无直连出口） |
>
> - **事实**：沙箱唯一出口是本地代理，它对 gitee 通畅、对 github 时通时不通；绕过代理则完全无出口。
> - **判断**：GitHub 推不上去是**沙箱代理到 GitHub 的上游链路问题**，既不是 git 参数、也不是用户本机网络。
> - **已验证**（同日晚补测）：17:5x 再试一次即成功，`20ce6a1..1f9a1cb` 落后 4 个提交一次补齐。
>   → **"直接重试"这条经验成立**；失败时**累计的提交别怕，一次能补完**。
> - **未验证**：代理为何间歇 502（上游被墙 / 代理策略 / 容量），拿不到代理日志，**不下定论**。
> - **行动**：① 失败就隔一会儿重试（当日已两度验证有效）② 或换本机终端推（本机可直连/有 VPN）。
>   **别改 git 参数** —— `http.version`、`http.proxy` 都已验证无效（绕代理直接 21 秒超时）。

> **两侧 tag 已对齐（2026-09-13）**：`v0.2.0`（`2f752f0`）此前只在 GitHub，当日补推至 Gitee。该 tag 指向已放弃分支 `feature/license-gate` 的末位提交（功能已 cherry-pick 进 main，但 **tag 未打在主线提交上**）——历史遗留，不影响代码完整性，详见第四节留档。
>
> **GitHub 推送经验（2026-09-17，归因已被上文修正）**：9-16 连续失败（HTTP2 framing 层错误，`-c http.version=HTTP/1.1` 重试仍 SSL eof）与 9-17 00:0x 一次成功，
> 差异不在参数而在**外部出口是否通畅**——所以要补推时**直接重试，别纠缠参数**。落后越多越容易在中途断，及时推比攒着推稳。
> ⚠ 但 9-17 17:3x 的对照实验把"网络窗口"这个模糊说法换成了具体一层：**WorkBuddy 沙箱经本地代理连 github 一律 502、直连无出口**（证据见上）。凡是**在 WorkBuddy 里**推 GitHub 失败，先按沙箱出口问题看，不要怀疑参数。
