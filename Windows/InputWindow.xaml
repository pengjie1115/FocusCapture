<Window x:Class="FocusCapture.Windows.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Width="600" Height="80"
        Background="#1E1E1E"
        Opacity="0.80"
        WindowStartupLocation="Manual"
        Loaded="Window_Loaded"
        Deactivated="Window_Deactivated">

    <Window.Resources>
        <DropShadowEffect x:Key="BoxShadow" BlurRadius="20" ShadowDepth="4" Opacity="0.4" Color="#000000"/>

        <!-- v3.5：类型小按钮（选中态由代码切换 BorderBrush/Foreground） -->
        <Style x:Key="TypeButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="BorderBrush" Value="#3A3A3A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="10,2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#2D2D2D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="10" Background="#1E1E1E"
            Effect="{StaticResource BoxShadow}"
            BorderBrush="#3A3A3A" BorderThickness="1">
        <Grid Margin="12,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="26"/>
            </Grid.RowDefinitions>

            <TextBox x:Name="InputBox" Grid.Row="0"
                     Background="Transparent"
                     BorderThickness="0"
                     Foreground="#E0E0E0"
                     CaretBrush="#4CAF50"
                     FontSize="15"
                     FontFamily="Microsoft YaHei UI"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     MaxHeight="150"
                     PreviewKeyDown="InputBox_PreviewKeyDown"
                     TextChanged="InputBox_TextChanged"
                     SpellCheck.IsEnabled="False"
                     Padding="4,2">
                <TextBox.Resources>
                    <Style TargetType="{x:Type ScrollViewer}">
                        <Setter Property="Background" Value="Transparent"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>

            <TextBlock x:Name="Placeholder" Grid.Row="0"
                       Text="记录你的灵感…（回车保存，Esc 取消）"
                       Foreground="#666666"
                       FontSize="12"
                       FontFamily="Microsoft YaHei UI"
                       IsHitTestVisible="False"
                       VerticalAlignment="Bottom"
                       HorizontalAlignment="Left"
                       Margin="4,0,0,6"/>

            <!-- v3.5：类型选择（左下角独立一行，不与占位文字重叠） -->
            <StackPanel Grid.Row="1" Orientation="Horizontal"
                        HorizontalAlignment="Left" VerticalAlignment="Center">
                <Button x:Name="BtnNoteType" Content="笔记" Tag="Note"
                        Click="TypeButton_Click" Style="{StaticResource TypeButtonStyle}"/>
                <Button x:Name="BtnTodoType" Content="待办" Tag="Todo"
                        Click="TypeButton_Click" Margin="6,0,0,0" Style="{StaticResource TypeButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
