<Window x:Class="FocusCapture.Windows.DropActionStrip"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Background="Transparent"
        Width="96"
        SizeToContent="Height"
        WindowStartupLocation="Manual"
        KeyDown="Window_KeyDown">

    <!--
      文字拖入后浮出的竖向小条。
      设计意图（用户原话）："让人不仔细看都不注意到它的存在都行" —— 所以整体压到低对比，
      只有鼠标悬停才亮起来。标题、图标、"已存为笔记"提示语**都是被否掉的旧版**，别加回来。
      必须是独立窗口：悬浮球窗口的透明区穿透，画在球里收不到任何鼠标事件（实测结论）。
    -->
    <Window.Resources>
        <!-- 竖向小条按钮：无图标、无边框、居中文字，悬停整块高亮 -->
        <Style x:Key="StripButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#999999"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3A3A3A"/>
                                <Setter Property="Foreground" Value="#E0E0E0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#666666"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#2A2A2A"
            BorderBrush="#3A3A3A"
            BorderThickness="1"
            CornerRadius="6"
            Padding="4">
        <StackPanel>
            <Button x:Name="BtnAiAsk" Content="AI 问答" Style="{StaticResource StripButton}"
                    Click="BtnAiAsk_Click"/>
            <Button x:Name="BtnGetNote" Content="得到大脑" Style="{StaticResource StripButton}"
                    Click="BtnGetNote_Click"/>
        </StackPanel>
    </Border>
</Window>
