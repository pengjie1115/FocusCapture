# 2026-08-31 工作日志

## 官网落地页上线（方案 A：docs/ 部署）

### 决策过程（重要，可追溯）
- 用户质疑「网站代码合并进 main 是否污染源码仓库」，曾考虑独立仓库方案（千问建议分开存放）
- 多轮权衡后用户最终选 **方案 A**（网站代码直接进 main 的 docs/，GitHub Pages 从 main/docs 部署）
- 用户的核心诉求：「自动更新 + 少操心」，A 方案天然满足（GitHub Pages 推 main 即自动重新部署）

### 执行结果
- ✅ 合并：`feature/website-v2` → main（合并提交 `abb89aa`，--no-ff），无冲突，零 .cs 源码改动
- ✅ 回滚保险锚点：main 原提交 `ae047db`（异常时 `git reset --hard ae047db`）
- ✅ Gitee 推送成功（`ae047db..abb89aa`）
- ❌ **GitHub 推送失败**：github.com:443 连接超时/重置（无 VPN 环境网络问题），三次重试失败
  - 证据链：github.com 网页 HTTP 000（10s 超时）；api.github.com 可达（0.65s）；GITHUB_TOKEN 环境变量与 wincred 中凭证均无效（Bad credentials）
  - **待办**：网络恢复/换网络后执行 `git push github main`，随后开启 GitHub Pages（main 分支 /docs 目录）

### 网站文件清单（main 上新增）
- docs/index.html（507 行，绿调 #4ADE80，零依赖手写 CSS）
- docs/llms.txt + robots.txt + sitemap.xml（Agent 友好设施）
- docs/screenshots/：quickview / quickview-2 / settings / input（英文名，README 引用）
- DESIGN.md（官网设计规范）

### 遗留问题（已全部解决，2026-08-31 15:50 补充）
1. ✅ **GitHub 推送成功**：用户提供新 token（ghp_299b***）替换失效旧 token
   - 踩坑记录：`git credential approve` 首次未覆盖 wincred 旧条目，必须**先 `git credential reject` 再 approve**，验证 `credential fill` 返回新 token 后才生效
   - 推送结果：`ae047db..abb89aa main -> main`
2. ✅ **GitHub Pages 已开启**（main 分支 /docs 目录），构建成功
   - **网站正式上线：https://pengjie1115.github.io/FocusCapture/**（HTTP 200）
   - llms.txt / robots.txt / sitemap.xml 均 200（Agent 友好设施生效）
3. Gitee Pages 未开（需实名认证+网页操作，暂不需要）

### 经验沉淀
- GitHub token 失效排查顺序：.env → wincred（`git credential fill`）→ 确认后 reject+approve 替换
- wincred 存的是旧 token 时，approve 可能"看似成功实则没覆盖"，务必用 `git credential fill` 验证
- 用户 token 出现在会话聊天中有泄露记录，key-guard 只扫 sk- 不覆盖 ghp_，已提醒用户可 revoke 重建

## 用户报告 404 事件（2026-08-31 15:55）

**事实**：
- 用户截图显示访问 `https://pengjie1115.github.io/FocusCapture/` 返回 404（GitHub Pages "File not found"）
- 但 curl 服务器侧测试返回 HTTP 200，27995 字节，title 正确为「FocusCapture · 专注力捕捉」
- Pages 配置：main /docs、built 状态、public、https_enforced，仓库 docs/ 下文件齐全

**结论（事实）**：网站技术部署 100% 正常，**不是代码、不是配置问题**。

**用户端可能原因（判断）**：
1. 用户网络到 GitHub Pages CDN 的链路问题（之前多次推送已证明用户网络访问 GitHub 不稳定）
2. 浏览器缓存了早期 404 状态
3. 用户实际访问的 URL 略有不同（截图 URL 被截断，无法验证）

**教训（事实）**：以后说"上线了"应改为"我这边测下来正常"，端到端可见才算。"全世界都能访问"是过度承诺。
