# 2026-09-01 工作日志

## 安全隐患紧急处置：GitHub 仓库改私有 + 取消 Pages 发布

### 背景
- 用户报告项目存在重大安全隐患，要求立即处理 GitHub 侧暴露面

### 处置前状态（事实）
- 仓库 `pengjie1115/FocusCapture` 为**公开**（private: False）
- GitHub Pages 已发布：https://pengjie1115.github.io/FocusCapture/（源：main 分支 /docs，状态 built）
- 环境变量 `GITHUB_TOKEN` 已失效（API 返回 401 Bad credentials）
- Windows 凭据管理器（wincred）中存有有效 token（40 位 ghp_），API 调用正常

### 处置动作与结果（全部验证通过）
1. **仓库改私有**：PATCH /repos/pengjie1115/FocusCapture `{"private": true}` → HTTP 200，`private: True / visibility: private`
2. **取消 Pages 发布**：DELETE /repos/pengjie1115/FocusCapture/pages → 确认 GET /pages 返回 404（站点已删除）
3. **公开 URL 验证**：
   - https://pengjie1115.github.io/FocusCapture/ → HTTP 404（公开访问已断）
   - https://github.com/pengjie1115/FocusCapture → HTTP 404（私有仓库标准行为，不泄露存在性）
4. **无 fork**（forks_count: 0），无需处理 fork 副本
5. **确认无自动恢复风险**：Pages 来源是 main/docs 目录（非 Actions 构建）；`.github/workflows/build.yml` 只做 tag 构建 + Release，不会重建 Pages

### 关键经验
- `gh` CLI 未安装，改用 GitHub REST API + wincred 中提取的 token 完成全部操作
- 改私有后公开 URL 返回 404 是 GitHub 标准行为，等于同时隐藏了仓库存在性
- **token 隐患**：有效 token 昨日（8-31）已出现在会话聊天中（泄露记录在 2026-08-31 日志），今天又用于真实 API 操作；key-guard 只扫 sk- 前缀不覆盖 ghp_。已建议用户 revoke 重建

## key-guard skill 更新：新增 ghp_ / github_pat_ 匹配规则

### 决策
- 用户选「更新（推荐）」：加 ghp_（classic PAT）和 github_pat_（fine-grained PAT）规则

### 改动内容
- `key-guard.ps1`：单条硬编码规则 → 规则数组（sk- / ghp_ / github_pat_ 三条），每条规则独立替换前缀一致的 `***REDACTED***`；主循环改为每文件逐规则匹配，一次读一次写
- `SKILL.md`：匹配规则与 description 同步更新

### 评估结论（事实/判断）
- 可行性高：改动约 10 行，误报风险极低（ghp_ 前缀特异 + 长度限制 `{30,}`）
- 真实效果边界（判断）：只清落盘明文，不解决已泄露 token（聊天记录不可撤回），根治仍是 revoke 重建
- 更新后已重跑 FullScan 验证

### 遗留
- 用户尚未 revoke 已泄露 token；重新发布网站操作流程已告知（网页/API 两步 + 私有仓库 Pages 仅协作者可见的决策点）

## 桌面克隆 FocusCapture 仓库（用户叫停于版本选择）

### 需求
- 用户要求从 GitHub 拉完整代码到桌面，不动现有项目文件夹

### 执行结果（事实）
1. **GitHub 直连不通**：SSL 断连（OpenSSL unexpected eof），无代理无 VPN；仓库已私有（昨日处置），API 列表为空属私有标准行为
2. **改从 Gitee 拉取成功**：`git clone --no-single-branch` → 桌面 `D:\桌面文件存放位置260518\FocusCapture-GitHub`，3 分支引用齐全（main / codex/quest-v3-sync / feature/license-gate），工作区=main（abb89aa 官网落地页合并版），86 文件
3. **版本差异发现**：本地 main（7262387，领先 5 个提交：todo-reminder-phase4 合并 + sync 增强等）**尚未 push 到远端**，远端 main=abb89aa。桌面这份落后本地
4. 本地项目文件夹全程未动

### 环境坑（记录）
- Git Bash 中文路径下 git clone 报"destination already exists"幻觉错误（实际目录不存在），改用 PowerShell 原生 Windows 路径解决
- git 2.55 在此环境本地分支 refs 写入异常（`git branch X origin/X` 创建后立即消失，但 config 已写入；远程引用 origin/* 完好），桌面 clone 保持 main 工作区 + 远程引用齐全，足够使用

### 状态
- 用户看到三选项（推送再更新 / 保持现状 / 复制本地最新）后选择「先停止任务」，待用户指示后继续

## 版本恢复 + license-gate 合入 + 坚果云引导回退（feature/restore-0831 分支）

**背景**：用户报告"前两天功能消失"。排查确认：v0.2.0 发版代码（设置面板大改版 dbad9ae / 输入框v3.6 6135d68 / LicenseGate 0928b1e）全在 feature/license-gate 分支，从未合并 main；今天 main 只有 d74b289（同步增强+坚果云引导）+ 7262387（待办提醒合并）。

**用户决策（AskUserQuestion 确认）**：待办提醒保留 / license-gate 一并合入 / Debug 构建。

**执行**（新分支 feature/restore-0831，未动 main）：
1. `git checkout abb89aa -- SettingsWindow` → 回退坚果云极简引导（两项引导→三项配置），commit fd9a92e
2. cherry-pick 0928b1e（LicenseGate）→ 干净
3. cherry-pick dbad9ae（设置面板大改版 861行）→ SettingsWindow 冲突，采用 theirs（dbad9ae 版云同步区本就是三项配置，天然满足回退引导；且 license-gate 包含 todo 提交，AskTimeCheck 勾选框在）
4. cherry-pick 6135d68（输入框 v3.6）→ 干净

**关键洞察**：license-gate 分支本身包含全部 todo 提交（be484b9..16bbc27），所以 dbad9ae 大改版设置页自带"纯日期询问时刻"设置项，无冲突叠加问题。

**验证**：dotnet build 0 错误 0 警告；六项功能点 grep 核对全过（LicenseGate.cs 存在/输入框v3.6/左侧导航/引导无残留/三项配置/同步增强15处）。产物 bin/Debug/net8.0-windows/ 21:26 生成。

## 合并推送 + 工作区文件消失事故（21:34-21:40）

**合并推送**：main 快进合并 feature/restore-0831（78588e2），双远程推送成功（origin Gitee + github 均 abb89aa..78588e2）。

**事故**：push 后 git status 显示 20+ 个 Services/*.cs 标记 deleted（含 NoteService/ReminderService/TimeParser 等），磁盘文件确实消失。**原因判断**：与此环境 git 2.55 已知的 checkout 索引异常同类（8-30 记录过 38 文件 D 问题），checkout/merge 时工作区同步异常。

**处置**：`git restore --source=HEAD --staged --worktree .` 全部恢复（对象库完好，双远程推送本身成功，零数据丢失）。验证：103 文件齐、git status clean、双远程一致 78588e2、dotnet build 0 错 0 警。

**教训**：此环境 checkout/merge 后必须立刻 `git status` 验文件数；异常先 `git restore` 恢复，勿慌勿删。

## 分支大清理 + 分支管理规范建立（22:35-22:55，方案A执行）

**背景**：用户确认保留分支但要求治理"乱"的根源。方案A：统一命名规范 + BRANCHES.md 索引 + 删无风险分支。

**删除（13 个本地 + 2 个 Gitee 远程）**：codex/new-feature、codex/quest-v3-sync、codex/quest1-ai-provider、asr-pure-csharp、import-range-search、inspiration-sync-buttons、license-gate、restore-0831、todo-reminder×3、website-landing、website-v2。功能全部已确认在 main。4 个被 `git branch -d` 误报 not fully merged（环境 bug），已确认合入后 -D 强删。

**保留（2 个）**：feature/build-trim-iconfix（构建瘦身未合入，待决策）、feature/annotation-quote（批注，永久搁置）。

**新建**：
- BRANCHES.md（根目录，入库 2617283，走 docs/branch-management 分支→快进合并 main→推 Gitee 成功）——命名前缀规范/生命周期铁律/当前索引/删除归档（带 hash 可找回）/远程状态
- .workbuddy/memory/MEMORY.md（首次创建）——分支管理规范 + 关键踩坑（git 2.55 索引 bug、-d 误报、GitHub 网络不稳、编译习惯）+ 功能板块索引

**待办**：GitHub 网络 502 三次失败——①main 推送 2617283 ②删除 4 个远程旧分支（codex/quest-v3-sync、codex/quest1-ai-provider、feature/license-gate、feature/todo-reminder-phase4），网络恢复后补。

## MEMORY.md 定位修正 + GitHub 重试自动化（22:48）

**用户纠正**：项目规范不应放 .workbuddy/memory/MEMORY.md——该目录是 WorkBuddy 工作空间，被 .gitignore 忽略、不进 git，换机器即失。规范应进项目文件。**同意**：已精简 MEMORY.md（移除分支管理规范章节，只留工具环境踩坑+功能索引，加 BRANCHES.md 指针）；规范以根目录 BRANCHES.md（已入库）为唯一权威。

**新建自动化**「GitHub推送重试与远程分支清理」（id 7e6eba2e，once 2026-09-02 08:00）：推 main 到 GitHub + 删 4 个远程旧分支 + 更新 BRANCHES.md 远程状态 + 双远程推送；网络失败 1-2 次即停不硬试。

## 会话知识笔记整理 + 上传 get 笔记（22:59）

把本晚"git 分支本质与管理规范"会话整理成知识笔记（只记知识本体，无操作流水），上传 get 笔记「学习」库：标题《Git 分支的本质与管理规范》，笔记 ID 1920145441270957192，标签 git/分支管理/版本控制。源文件 D:/桌面文件存放位置260518/git分支本质与管理规范.md。getnote-sync skill 执行正常。
