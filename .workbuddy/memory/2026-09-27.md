# 2026-09-27 事件账（交付 / 决策 / 踩坑）

## 〔WorkBuddy〕凌晨 · AI 整理 + snap 按需出图 整体合并进 main 并推送

**交付**
- `feature/ai-tidy` + `feature/snap-scoped-output` 整体 **ff-only** 并入 main（`9c29c01` → `c7c68fc`，
  6 个提交 / 25 文件 / +1576 行）；两条分支**已删除**（合并即删，git log 即归档）。`c7c68fc`
- 推送：**GitHub 成功**（`e120d9b..c7c68fc`）；**Gitee（origin）失败** —— `Oauth: Access token is expired`（403）。
  本地与 GitHub 均已到位，Gitee 落后 6 个提交，待用户更新令牌后重推。

**决策**
- 合并走 `dev.ps1 merge`（ff-only + 合并后自动校验索引异常）而没手敲 git —— 本机 `checkout` 有丢文件的
  历史，脚本里带着自愈逻辑。
- 删分支只在合并成功后做，且只删这两条；`feature/chat-restore-cross-device`（未并入）保留。
- 分支是线性链（snap 从 ai-tidy 开出），所以 ff-only 合 `snap-scoped-output` 一条 = 两条线的提交一起进 main，
  不需要 cherry-pick。

**踩坑**
- **Gitee 凭据过期 ≠ 沙箱限制**：判据还是「有没有输出」—— 有输出（403 + `Access token is expired`）就是
  真实凭据问题，零输出才是沙箱出不了网（`dev.ps1 push` 的失败分支就是按这条分的）。
  凭据由 **git-credential-manager** 管（`credential.helper` 指向 PortableGit 自带 GCM）。**token 不进聊天**，
  必须用户自己在终端更新。
