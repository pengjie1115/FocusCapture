
## 2026-09-25 [ZCode] 全局搜索居中覆盖层 + 词级高亮
- 分支 feature/chat-search-overlay：ChatSearchWindow（独立窗体）→ ChatSearchPanel（AIDialogWindow 居中覆盖层 + 主内容 BlurEffect 毛玻璃）；三段结构对标千问；最近会话列表复用 ListSessions 取前 100；跳转高亮从「整气泡变色」升级为词级（TextBox Select + SelectionBrush，因正文需保留拖选回填不能换 TextBlock）
- 用户拍板（AskUserQuestion）：列表=最近会话（非关键词历史）；高亮升级词级；点面板外可关闭
- 踩坑：XAML 元素带参构造函数会报误导性 MC3054（须留默认构造）；快层断言子串撞车（ClearSearchHighlightAsync 含 "SearchHighlight"、注释字样）
- ready 全过，已提交（约 440+/194-），按用户指令未合并未推送

## 2026-09-25 [WorkBuddy] chat-search-overlay 合并推送 + 清理两个已合并分支
- 合并：`dev.ps1 merge`（ff-only）把 `feature/chat-search-overlay`（d0c9182）并入 main；合并后 `git diff main..feature/...` 为空 = 树与分支完全一致，故分支上 ready 全过的证据可直接平移，未重复跑 ready
- 推送：bash 直跑 `git push origin main` / `git push github main`，双远程均一次成功（Gitee 51fa710..d0c9182、GitHub 6bc191e..d0c9182）；`ls-remote` 核实两端 refs/heads/main = d0c9182，与本地一致
- 删分支（用户指令）：`git branch -d` 删 `feature/chat-search-overlay`（was d0c9182）、`fix/ai-chat-ui-bugs`（was a0672b7），均属已并入 main 故 -d 直接放行；现存仅 `feature/chat-restore-cross-device`
- 执行路径变更：**没走 `dev.ps1 push`**（NOW 旧记称其因 GCM 读不到凭据失败），改 bash 直推一次成功 → 佐证真正原因是「PowerShell 会话里的 git 出不了网（零输出判据）」而非凭据，与 `PITFALLS.md` 已有条目一致

## 2026-09-25 [WorkBuddy] 人工验收销账 + REGRESSION 文档修正
- 用户确认「旧账都已验收通过」→ REGRESSION 加 7 处 ✅ 结案注记：B-9(1 条) / B-10(8 条) / B-12(5 条) / B-16(6 条，原「⚠ 本节尚未经真人验收的部分」小节标题一并改为 ✅) / B-21(19 条) / B-22(整组) / B-6(热键 3 条)
- **修文档 bug：REGRESSION 里 17 处「2026-09-26」是笔误** —— 经 git 提交时间戳核对，那批改动实为 1eb9bec / 0b41e47 / a0672b7（09-24 21:00~23:51 提交，1255f6c 为 09-25 14:01），已全部改回 `2026-09-24`
- **B-22 段描述过期同步**（本次改动导致的行为变更，按维护约定强制更）：段头与覆盖说明仍写「ChatSearchWindow 独立搜索窗」，而 1255f6c 已删该窗体改 `ChatSearchPanel` 居中覆盖层 → 段头 + 覆盖说明 + 条目「都弹出独立搜索窗」→「居中搜索覆盖层」、「整气泡背景高亮」→「词级高亮」；触发表 `Windows/ChatSearchWindow*` → `Windows/ChatSearchPanel*`
- 保留 ⚠ 唯一一处：B-6「任务栏/开始菜单/桌面/exe 图标不跟随」= 平台能力边界（读 exe 内嵌资源）
- 销账口径：**段头加结案注记、表格内 ⚠ 字形保留**（注记里写明「自此视同已人工验收，保留字形仅表示无机器检查点覆盖」）—— 避免 66 处逐条替换的编辑风险，成本与收益匹配
- `docs/2026-09-24-AI问答重构交接文档.md` 里的旧窗体名**刻意不改**（历史交接文档记的是当时状态）

## 下午：AI 问答搜索一轮调整（[ZCode]，分支 feature/chat-search-refinements，未合并未推送）

- 用户四问核实后拍板开发：① 3号搜索（收起态标题栏 🔍）按状态分流：对话打开=向右展开「会话内查找条」（只搜本会话，n/m 计数+‹ ›循环切换+黄选区高亮，Enter/Shift+Enter/Esc/×），起手页/分组视图=全局覆盖层（4号不变）；1号（展开态）恒开覆盖层，独立入口 `BtnSidebarHeaderSearch_Click`；② 收起态标题栏加 ＋新建会话（回答转后台可切回）；③ 搜索面板默认列表由「最近会话」改**真·搜索词历史胶囊**（WrapPanel 错落排布，逐条×，头部×旁加「清空」；存 `chat_search_history.json`，去重挪队首、上限 20；`ChatSearchHistoryStore` 新文件）；④ 修 bug：分组视图里点侧边栏/搜索面板的会话切不过去 —— 根因 `LoadHistorySession`/`Activate` 不退分组视图，`GroupViewPanel` 挡着；修法 = `LoadHistorySession` 里补 `CloseGroupView()`（快照/分组内行点击路径均 no-op）
- 用户提供的 6 张图曾有 4 张是同一文件（image.png 被覆盖），第一轮据此停下让用户重传 —— 3号=收起态对话中、4号=收起态起手页，**同一颗按钮按状态分流**是用户确认的关键口径
- 新检查点：慢层「AI 分组与批量 UI」41 条（+1）+ 搜索历史/InSessionFindMatcher 纯逻辑 + 面板结构与 `LoadHistorySession` 含 `CloseGroupView` 断言；REGRESSION B-22 段已同步（新增 6 行 ⚠ 待人工验收）
- InSessionFindMatcher（Services/AI/）纯逻辑：跨气泡有序、OrdinalIgnoreCase、空词无命中；UI 侧命中高亮走 BubbleText.Select，查找期间不自动消失（区别于全局搜索跳转的 2.5s 高亮），关闭/切会话才清
