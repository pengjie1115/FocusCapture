# NOW — 当前状态（唯一活文件）

> **谁干活谁覆盖更新；并行开发按分支分节；旧状态被覆盖即自动作废。** 硬上限 40 行。
> 维护规则见根目录 `AGENTS.md`「三、开发记忆」。更新：2026-09-24

## 分支 fix/ai-chat-ui-bugs（第三批：标题栏重排 + 模型机制改版；**开发完，待提交**）

- 前两批已提交：`1eb9bec`（输入区沉底 / 发送按钮两态 / 相对时间 / 自定义欢迎语 / 头像圆形）、`6eb43e6`（头像圆形出图守护）、`ca3fabe`（补记遗留记忆流水）
- 本批做完：① **标题栏两态重排**——列结构上提到顶层、`DrawerSplitter` 加 `RowSpan=2` 贯通到窗口顶部、去掉 2px 绿 accent 线、收起态 `≡+🔍` 靠最左 / 展开态 `🔍+‹` 紧贴竖线（两态顺序相反）、标题栏与窗口 `Title` 都跟随「AI 助手名称」② **模型机制改版**——新增 `DefaultModelKey`（设置页唯一露出的模型配置，点一下即选）、`ActiveModelKey` 语义改为「**上次使用**」（持久化、不进设置页）、新会话走 `ResolveForNewSession`（上次使用→默认模型→第一个可用）、会话内切模型回写「上次使用」、删下拉「跟随全局默认」、设置页旧两行文案 → 可点击「默认模型」行 + 首次配置弹窗询问 ③ 模型显示名去供应商前缀（跨供应商重名才补「（供应商）」）
- 关键定位（下次别重查）：**「分组里选的模型不生效」的根因** = `SendCurrentInput` 在分组态先调 `StartNewSession` 建新会话，而 `StartNewSession` 恒取 `AppSettings.ActiveModelKey`、会话内切换又从不回写它 —— 用户刚选的值当场被覆盖。修法是 `ApplySessionModel` 回写 + `StartNewSession` 改走优先级链
- 检查点：快层 **175 过** / 慢层全过 / `ready` 全量绿（文档引用 20 处）/ 快照 **42 张**（新增 `10i-AI 对话-标题栏长助手名`）
- 快照 10i 当场抓到真 bug：Auto 列被长助手名撑宽（侧边栏 220→400）→ 修法 `MaxWidth` 绑 `Sidebar.Width` + 边距下沉到子元素；已进 PITFALLS
- 决策已记 DECISIONS **D10**（默认 vs 上次使用）/ **D11**（标题栏重排）；验收条目已更新 REGRESSION **B-20 / B-22**
- 待人工手点（REGRESSION B-22）：点「默认模型」弹列表、首次配置弹窗、两处图标点击、切模型后新建会话是否沿用
- 下一步：提交 → 用户验收 → 合并 main → 推双远程
- 阻塞：无

## 上一个分支（已合并，勿再当成进行中）

- `feature/ai-chat-ui-revamp` **已合并进 main**（`687cf05`）并推 Gitee；GitHub 当时 SSL 失败按指令未重试
- AI 模型多供应商改造已于 9-23 并入 main

## 人工验收欠账

- B-16（Agent 工具）、B-12 —— 需真实环境手跑（B-15、B-6 已结案勿再提）
- 本分支新增人工条目（B-9/B-10/B-21/B-22）：输入区沉底 / 发送停止两态 / 欢迎语改版 / 欢迎语图标删除 / 头像圆形 / 侧边栏相对时间 / 标题栏两态与竖线贯通 / 默认模型行与首次弹窗
