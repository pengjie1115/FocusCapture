# NOW — 当前状态（唯一活文件）

> **谁干活谁覆盖更新；并行开发按分支分节；旧状态被覆盖即自动作废。** 硬上限 40 行。
> 维护规则见根目录 `AGENTS.md`「三、开发记忆」。更新：2026-09-26

## 分支 fix/ai-chat-ui-bugs（第四批：分组三点/批量管理千问对标；**开发完，待 ready 后提交**）

- 前三批已提交：`1eb9bec`（输入区沉底等）、`6eb43e6`（头像守护）、`0b41e47`（标题栏两态+模型机制）
- 本批（用户 2026-09-26 拍板，5 个问题已当场确认）：
  ① **进分组不再收起侧边栏**（`HandleGroupSelected` 与 `SendCurrentInput` 两处 `OpenDrawer(false)` 删除，推翻 09-24 旧拍板；发消息只退出分组视图、侧边栏保持展开）
  ② **分组内会话补三点菜单**（重命名/置顶此对话/批量管理/移动到分组▸/导出对话▸/删除对话）；「移动到分组」子菜单 = 现有分组+收藏（点即移入）/ 移出本组（收藏里叫移出收藏，回未分组）/ ＋新增分组（同名只提示不静默塞入）；单条与批量共用 `BuildMoveToGroupMenu`
  ③ **分组批量管理 1:1 复刻千问**：行首复选框 + 底部「☑全选 已选n | 取消/移动到分组/删除(红底)」；不设上限（用户拍板「已选 n」非 n/100）；与侧边栏批量互斥；`DeleteSessions` 改返回 bool（确认才删/才退批量）
  ④ **侧边栏批量条改版**：删「点会话选中…」提示语，改 全选/取消/移入分组/删除 4 按钮同款；行首复选框；菜单文案「批量操作」→「批量管理」；选中走 IsSelected INPC（不再整表重建）
  ⑤ **设置页默认模型**：文字+模型框整体 258（=两提供方按钮总长，DockPanel），去「更换」，点框弹下拉
  ⑥ **标题栏底色 #252525→#1E1E1E**（与正文统一）
- 关键防线：点复选框不得二次翻转 —— 行点击里 `ChatSidebar.FromBatchCheck(e.OriginalSource)` 放行 + CheckBox Click 只同步集合（顺序：MouseLeftButtonUp 冒泡 → OnClick toggle → Click）
- 检查点：慢层新增组「AI 分组与批量 UI」23 条（源码结构断言）；快照新增 **10j** 分组批量管理（10h 已被「回答中按钮」占用，撞号已修）；REGRESSION B-21/B-22 手动清单与 §一 已同步；慢层 583 条全过、快层 175 全过
- 未做/待用户拍板：千问批量态会把标题换成「批量管理」大标题（用户只发图未提要求，暂未复刻）；进入批量是否默认全选（千问图是全选态，现默认不选）
- 下一步：`dev.ps1 ready` → 提交（不合并不推送）→ 用户验收
- 阻塞：无

## 上一个分支（已合并，勿再当成进行中）

- `feature/ai-chat-ui-revamp` **已合并进 main**（`687cf05`）并推 Gitee；GitHub 当时 SSL 失败按指令未重试
- AI 模型多供应商改造已于 9-23 并入 main

## 人工验收欠账

- B-16（Agent 工具）、B-12 —— 需真实环境手跑（B-15、B-6 已结案勿再提）
- 本分支新增人工条目（B-9/B-10/B-21/B-22）：输入区沉底 / 发送停止两态 / 欢迎语改版 / 头像圆形 / 侧边栏相对时间 / 标题栏两态 / 默认模型框 / 进分组不收起 / 分组三点与移动到分组 / 两处批量管理全流程
