# NOW — 当前状态（唯一活文件）

> **谁干活谁覆盖更新；并行开发按分支分节；旧状态被覆盖即自动作废。** 硬上限 40 行。
> 维护规则见根目录 `AGENTS.md`「三、开发记忆」。更新：2026-09-26

## 分支 fix/ai-chat-ui-bugs（AI 问答界面 6 项修复；6 条全做完，待提交）

- 已完成：① 发首条消息后输入区**自动下沉** ② 发送按钮箭头**居中** + 回答中改「蓝圆 + 中心白方块」铜钱态 ③ 侧边栏时间改**相对时间**（刚刚 / N分钟前 / N小时前 / N天前）+ 每分钟刷新（**分组视图保持 MM-dd HH:mm**）④ 设置新增**自定义欢迎语**，默认「{昵称}，我帮你」，昵称留空取 Windows 登录名，支持 `{昵称}` 占位符 ⑤ **删除欢迎语图标**（起手页 + 设置入口 + `ChatAssetsService` 4 个 API；已落盘文件按红线 7 保留不删）⑥ 侧边栏头像**圆形裁剪**
- 关键定位（下次别重查）：输入区下沉的根因是 `SendCurrentInput` 先 `ResetInput()`（那一刻 `Bubbles.Count==0` → 被判起手态居中），气泡加入后**无人重算布局**；修在 `SendAsync` 的 `AddBubble` 之后
- 检查点：快层 **175 过** / 慢层 **543 过**（会话分组组 63→74，新增 11 条相对时间边界用例）/ 快照 **41 张**（新增 10g 对话态·沉底、10h 回答中按钮、03d 设置-AI 问答界面）
- 另一会话在此分支工作区留了未提交的记忆流水 `.workbuddy/memory/2026-09-24.md`（16:45 得到大脑那几行）—— **刻意不动、不提交**（其记录写明"按用户指令不做提交"）
- 下一步：提交 → `ready` 全量 → 用户验收 → 合并 main → 推双远程
- 阻塞：无

## 上一个分支（已合并，勿再当成进行中）

- `feature/ai-chat-ui-revamp` **已合并进 main**（合并提交 `687cf05`）并推双远程；旧记录里那句「未合并未推送」已作废
- AI 模型多供应商改造已于 9-23 并入 main

## 人工验收欠账

- B-16（Agent 工具）、B-12 —— 需真实环境手跑（B-15、B-6 已结案勿再提）
- 本分支新增人工条目（B-9/B-10/B-21）：欢迎语改版 / 欢迎语图标删除 / 头像圆形 / 侧边栏相对时间与分组视图时间口径差异
