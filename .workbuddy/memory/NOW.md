# NOW — 当前状态（唯一活文件）

> **谁干活谁覆盖更新；并行开发按分支分节；旧状态被覆盖即自动作废。** 硬上限 40 行。
> 维护规则见根目录 `AGENTS.md`「三、开发记忆」。更新：2026-09-26

## 当前：main `9c29c01` 干净；分支 `feature/ai-tidy` `117a248` —— **已提交，未合并、未推送**

- 2026-09-26 [dsh] AI 整理（需求：灵感速览的笔记 / 待办，一大段没逻辑的文字要交给 LLM 理顺）
  - 三入口：灵感速览**行尾**（AI 按钮排在 × 删除左边）、待办汇总行按钮区（只读态 + 编辑态各一个）、
    全屏编辑窗底部
  - 点按钮 → LLM 整理（保原意 / 分点 / 小标题 / 去重 / 理顺序 / 改错别字，不翻译、不新增事实）
    → **预览对照窗**（左原文只读、右结果可编辑）→ 三出口：替换原文（旧行先进回收站）/ 另存新笔记 / 复制
  - 模型：设置 → AI 模型 新增「AI 整理模型」框（第一项「跟随当前模型」，点中即生效无确认框）；
    存 `AppSettings.AiTidyModelKey`，空 = 跟随活跃模型，键失效 / 那家没 Key 一律回落
  - 上限 8000 字符，超出直接拒绝（不截断、不分段）；落库口径收在 `Windows/AiTidyFlow.cs`
    （三入口各只调一次，防三处行为漂移）
  - 顺带修：待办汇总编辑态**点编辑框以外自动保存退出**（灵感速览早有，本次对齐；空内容 = 取消退出）
  - 检查点：快层 [17] 13 条 + 慢层「AI 整理」组 22 条 → 快层 96→**109**、慢层 686→**709**
  - **实测**：`ready` 退出码 0 —— 编译 0 错 0 警 / 快层 109 / 慢层 709 / 文档引用 20 处 / 条数核对一致；
    慢层墙钟 **35.7 秒**（文档锚点写「约 170 秒」，差异未查证，本次未改锚点、只改条数）
- 关键取舍：预览窗**不做**「追加为 AI 子条目」（要动 NoteService 标记行解析 + 慢层那 20 条断言，
  用户拍板本次不做）；整理后**不跑**时间识别（整理只改文字，不顺手改提醒）

## 分支

- `feature/ai-tidy` —— 本次（`117a248`），**按用户指令：只提交、不合并不推送**
- `feature/chat-restore-cross-device` —— 未并入，保留

## 遗留

- DSH 的 `pwsh` 在本项目中文路径上沙箱初始化失败（`SetNamedSecurityInfoW` Win32 5）→
  每条命令都要带 `sandbox_permissions=danger-full-access` 才跑得起来（已进 PITFALLS）
- 慢层实测 35.7 秒 vs 锚点「约 170 秒」的差异未查证（本次只改条数，未动锚点）
- 推送走 bash（`dev.ps1 push` 在 PowerShell 侧出不了网）；GitHub 偶发 502 / SSL，重试即可
- 已实测到的 2 个 flaky（`[8]` 子进程流式读、Agent 工具组的时间前提）仍未隔离
