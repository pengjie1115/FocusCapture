# 2026-09-06

## 调研：灵感速览笔记同步到得到大脑（只读调研，未动代码）

**背景**：用户提出「灵感速览」笔记一键上传到得到大脑（原 Get笔记）。本次仅调研验证可行性，未改任何代码。

**关键结论（事实）**：
- 得到大脑开放平台：biji.com/openapi，Base URL `https://openapi.biji.com/open/api/v1`，鉴权双 header（Authorization: gk_live_xxx API Key + X-Client-ID: cli_xxx）
- 创建笔记接口 `POST /resource/note/save`，note_type=plain_text 同步返回 {note_id,...}；无幂等键（无 client_note_id）→ 去重需本地记录指纹映射
- 官方文档 doc.biji.com/docs/WOxgwObNNiyMHWk1dl0cJqSxnEd；错误码 10201 非会员 / 10202 QPS 限流（retry_after）；skill 文档建议创建笔记间隔 1 分钟以上（是否为硬限制未验证）
- 用户本机已有跑通资产：`~/.workbuddy/skills/getnote-sync/scripts/save_note.py`（凭证读 `~/.openclaw/openclaw.json` 的 GETNOTE_API_KEY/GETNOTE_CLIENT_ID，实测可建笔记）；weread-weekly-digest 同接口在传笔记
- FocusCapture：灵感速览=QuickViewWindow，数据模型 NoteEntry（Timestamp/Content/SourceWindow/Tag/Type(Note|Todo)/DueTime/TodoStatus），本质是单行 markdown 条目（\u23CE 转义换行）；现有 ↑↓ 按钮是自家 WebDAV 同步与得到大脑无关；已有 NoteExportService 可复用做 markdown 聚合

**可行性判断**：高。推荐方案 A=应用内直连 OpenAPI（新增 GetNoteSyncService + 灵感速览窗口加按钮 + 设置面板存凭证[Dpapi] + 指纹去重）。方案 B=调 python save_note.py（开发量小但集成脆弱）。灵感笔记纯文本无图片字段 → 无需 OSS 图片上传，难度大幅降低。

**待用户决策**：推送粒度（整批一篇 vs 逐条）、目标知识库、触发入口、凭证来源（设置面板 vs 读 openclaw.json）

## 追加：用户提出「通用同步接口」设想（多平台，非只得到大脑）

**用户设想**：写通用接口，让用户自选笔记同步目标（得到大脑/飞书/金山文档等），不绑死单一平台。

**平台事实核查（2026-09-06 WebSearch）**：
- 得到大脑：笔记条目语义（标题+markdown+标签+知识库+语义搜索），鉴权=双 header（API Key+Client ID），最贴近灵感速览
- 飞书：docx/v1/documents 文档块结构（block 树，markdown 要转 blocks），自建应用 app_id/secret + 权限审批 + tenant token；语义是「文档」非「笔记」
- 金山文档：developer.kdocs.cn 个人文件 API（POST /api/v1/openapi/personal/files，测试 500 次/天、正式 10 万/天），OAuth+scope（user:file:write 等），语义是「文件对象」（.ksheet/.docx），最不贴近
- 三平台语义/认证/幂等全不同 → 「统一写入接口」只能取最小公共语义（标题+正文+可选标签/文件夹），各平台适配器翻译

**我的架构判断**（未实施，等用户拍板）：INoteTarget 适配器抽象层 + 首发仅实现得到大脑 + 预留 Feishu/Kdocs/WebhookTarget 插槽；另建议低成本高杠杆的「Webhook 通用口」（POST JSON 到自定义 URL，接 n8n/Worker/Agent 生态），把多平台翻译外包给已有生态。避免一次性铺多适配器（过度设计红线）。关键岔路：「用户」指自用还是未来分发（决定要不要多账号/资质向导）。

## 第三轮：AI 动手能力（对话 Agent）→ 已拍板写实施文档

用户提出把 FocusCapture AI 从问答升级为可操作 Agent（对话里查笔记/改待办/推得到大脑）。代码调研确认：LLM 客户端 OpenAICompatibleProvider 支持流式但**无 function calling**；AIDialogWindow + ChatSessionService（20 条裁剪）已就绪；NoteService CRUD 齐全。用户最终拍板按 P0+P1 走（用户放弃 QUEST 模板，要求正常写文档）。

**产出**：根目录 `AGENT_IMPLEMENTATION.md`（v1 方案稿）——得到大脑 C# 直连客户端（GetNoteClient）+ 去重指纹（GetNoteSyncState，MD5 指纹落盘 getnote_sync_state.json）+ Agent 工具层（AgentTool/Registry/AgentRunService，4 工具：search_notes/list_todos/save_quick_note/push_to_getnote）+ 写操作安全确认闸 + 设置开关 AgentEnabled 默认关 + tools 不支持自动降级普通问答。技术要点：note/save 无幂等键→指纹去重；int64 note_id C# 无精度问题；QuickViewWindow 加按钮与对话共用同一工具实现。判级 L2 边界改动（改 ChatSessionService 消息模型 + AIDialogWindow + AppSettings + 新增目录）。**待用户拍板后开工**，需跑 feature 分支。

## 追加：云端读取纳入范围（AGENT_IMPLEMENTATION.md v1.1）

用户提出「能否对话查询得到大脑已有笔记」→ 确认可行（官方有 recall 语义搜索 / note/detail 接口，非关键词硬匹配），文档已扩展：GetNoteClient 动作 2→4（+recall/+detail），工具清单 4→6（+search_getnote_notes 只读 / +read_getnote_note 只读），写死「云端检索仅显式意图触发」（用户提得到大脑/云端才搜，避免隐私无感外发与延迟），测试连接顺带探测读权限。注意：Key 权限由开放平台建应用时勾选决定，只勾写入查不了。

## 定案：共享卡车 + 双包装台模型（AGENT_IMPLEMENTATION.md v1.2）

用户用「苹果(标品笔记)/卡车(传输)/老王(LLM 包装)/目的地A(得到大脑)」类比纠正了我「外壳全锁死」的过度设计。**定案模型**：苹果=NoteEntry 源头统一格式（代码）；卡车=GetNoteClient 唯一传输通道（鉴权/去重/知识库代码锁死，两入口共用）；包装台①按钮=代码制式排版（GetNotePayloadBuilder，稳定可预期）；包装台②对话=老王自由组织标题/正文/结构（与按钮形态不同被允许）。老王红线两条（代码强制防幻觉）：①不能编造内容/note_id ②合并跳过必须明示。**稳定预期来自卡车同路+苹果标品，不来自包装统一**。文档已改 v1.2：新增 5.1 分层模型、5.2 包装台①排版规则表（标题/正文/标签/知识库/长度/预览），原节顺延 5.3-5.8，决策表加「对话路径呈现自由度」，验收加 4b。工具清单 6 个不变。

## 开工：AI 动手能力（解耦架构 v2.0，feature/agent-tool-framework 已提交）

**需求纠偏（用户明确指出）**：v1.2 把功能焊死在得到大脑上不是他要的。总纲领=「AI 能调工具」通用框架；得到大脑只是工具之一，未来要插飞书/金山等，框架不能写死。用户追问"工具是否必须预制 8 个工具类"→ 引出方案 B：**能力目录数据驱动**（function calling 的 tools 数组是运行时数据；每适配器一份能力清单 + 一个通用执行器；API 授权几个能力不用预先猜，全量声明+权限不足运行时报错）。

**实施（commit 5f46f30，15 文件 +885/-27，编译 0 警告 0 错误）**：
- 文档：AGENT_IMPLEMENTATION.md 重写为 v2.0（在桌面级目录 `D:\桌面文件存放位置260518\`，不在项目内）
- 地基：AppSettings +AgentEnabled/GetNoteApiKey/GetNoteClientId；ChatMessage 加可选 ToolCallId/ToolCallsJson（两参构造全兼容）；OpenAICompatibleProvider +ChatWithToolsAsync（LlmRequestException 带.StatusCode）；ChatSessionService 支持 tool 消息、裁剪整组同进退
- 框架（Services/Agent/，零目的地知识）：AgentTool 基类 / AgentToolRegistry / AgentRunService（5 轮上限+确认闸+4xx 降级）/ LocalTools（search_notes/list_todos/save_quick_note）
- 目的地（Services/Destinations/）：IOutboundDestination 接口 + OutboundTool 包装 + GetNoteDestination（4 能力：save_note/search/read_note/list_notebooks；凭证空时统一报未配置）
- UI：AIDialogWindow Ask 模式+开关开启走 Agent 循环（非流式，工具状态实时显示）；SettingsWindow AI 板块加开关
- 清单：REGRESSION.md +B-7（7 条现象级验收）；BRANCHES.md 登记 feature/agent-tool-framework

**关键实现事实**：recall/note/detail 响应结构未经实测（save_note.py 只验证过 save/knowledge_list），适配器做成 data 原样透传+截断，实测有出入只改 GetNoteDestination 一处；note_id/topic_id 均按字符串处理；确认闸用 MessageBox（v1.2 的 60 秒自动取消简化掉，已记入文档决策表）；去重指纹/按钮直传/凭证 UI 全部移下期。

**下期待办**：灵感速览按钮直传 + 包装台①制式排版 + 指纹去重 + 凭证设置界面/测试连接；recall/detail 实测后校准适配器。

**踩坑记录**：C# 插值字符串内嵌三元表达式需加括号（GetNoteDestination 知识库列表一处，已改为先算变量）；System.Threading/Json.Nodes 不在 GlobalUsings，新文件需显式 using。

## Agent v2 修复轮（92c4d46，21:30-22:40）

用户实测暴露两个问题+一个诉求：
1. **缺工具**：AI 无法创建待办/标记已办/改回待办/删除。补 4 个工具（create_todo/complete_todo/reopen_todo/delete_note），条目定位协议 = ref_time([yyyy-MM-dd HH:mm])+content_hint 消歧，0/多命中都报错让模型澄清
2. **模型幻觉实锤**：翻 chat_history 会话 JSON 确认模型两次无工具调用就宣称"已创建待办/已标记已办"（21:52/21:36 会话）。根因=缺工具+无防线。修法=补工具+系统提示词注入防幻觉红线（工具结果为唯一事实来源/写操作先对话征询/没工具直说做不到）
3. **运行日志**：新增 Services/AppLog.cs（AppData\Roaming\FocusCapture\logs\app_日期.log，按天滚动、保留30天、单日10MB熔断、线程安全），埋点：Agent 主循环全链路/Provider 请求失败/NoteService 写删改失败/全局崩溃

确认策略改版（用户拍板）：写操作**默认不弹窗**，靠系统提示词对话内确认（软）+回收站兜底+日志留痕；设置面板「写操作弹窗确认」开关（AgentWriteConfirmPopup，默认关）可开回弹窗。用户明确否决了「删除强制弹窗」加码。
「AI 读日志自查」判定为过度设计推迟（框架数据驱动，将来半天可加）。
发现：AGENT_IMPLEMENTATION.md 桌面文件仍是 v1.2（之前 v2.0 重写未落盘），本轮更新到 v2.1 并在头部标注架构已解耦落地。

## 日志设置项（2554c7e，22:50）

设置-通用板块新增「运行日志」区：打开日志文件夹按钮（AppLog.EnsureLogDir + explorer）+ 保留天数输入（AppSettings.LogRetentionDays，1-365 钳制，默认30，TextChanged 保存+AppLog.ApplyRetention 立即生效重清理）。
坑：AppLog（FocusCapture.Services）里引用 AppSettings 需全限定 FocusCapture.Models.AppSettings（AppLog 无 using，全局 using 不含 Models）。

## 合并推送（23:10）

feature/agent-tool-framework 快进合入 main（e1461fb→2ca5754，4 个 commit 含 BRANCHES.md 状态更新），双远程推送成功（Gitee e1461fb..2ca5754、GitHub e1461fb..2ca5754）。
git 索引 bug 第三次复发：checkout main 后 25 个 Services 文件误标 deleted（与 8-30/9-1 同款）。按规程 git restore --source=HEAD --staged --worktree . 恢复，验证 status 0 + 文件在盘 + main 编译 0 错误后才推送。
AGENT_IMPLEMENTATION.md 留在桌面目录不进仓库（用户拍板）。feature 分支未删（用户未提）。

## git 索引 bug 专项诊断（23:16-23:30，只读）

用户问根治。诊断发现：
- 环境里有两套 git：WorkBuddy 内置 PortableGit（PATH 优先）+ 系统自带 C:\Program Files\Git，**版本相同** 2.55.0.windows.3 → 版本差异排除
- 仓库用 reftable 存储；无 assume-unchanged/skip-worktree 标记（113 条全 H）；config 干净
- 复现实验：3 轮 checkout 往返全干净（但两分支同树，重写面小，说服力弱）
- **自伤教训：并行发两条 git 命令直接触发 index.lock 争抢（"Another git process"）→ 本环境禁止并行 git 调用**
- 空闲 8 秒观察：无后台进程碰 .git（当时）
- 杀软状态未验证（PowerShell 工具三次空输出，通道故障）

机制判断（未实锤）：误标 deleted = 索引刷新时 lstat 工作区文件瞬间失败，嫌疑排序：①批量重写文件时被杀软实时扫描锁 ②WorkBuddy 沙箱文件层 ③并发 git（已实证会引发锁争抢）。
方案已报用户：A 自愈 hook（post-checkout/post-merge 检测"标记删除但文件在盘"自动 restore）/ B 用户自己终端对照实验定位真凶 / C 杀软排除仓库目录 / D 迁移 ASCII 短路径（不推荐先动）。
