
## 2026-09-09 凌晨（约 00:57-01:10）

1. 只读排查未合入分支：本地 feature/chat-session-sync-and-history（领先 main 5 commits）+ 远程 github/feature/license-gate
2. 按最新 main 编译 Debug 版本：成功，0 警告 0 错误，产物 bin\Debug\net8.0-windows\FocusCapture.exe（01:00）
3. 用户拍板彻底删除 feature/chat-session-sync-and-history（含 5 个未合并 commit：会话元数据/批量操作/导出/回收站 + ChatSyncEngine 会话同步引擎），只删该分支不动 main
4. 执行：清 0 字节 stale index.lock → checkout main → restore 修索引 bug → git branch -D 删除分支 → 验证 main 完好（a10cba8、118 文件、工作区干净、无 ChatSyncEngine/HistoryDrawer 残留）
5. **索引 bug 三连发**：本次会话 checkout 每次必触发（2-30、9-1 之后第三次复发，且一次 checkout 中途被打断留下 0 字节 index.lock）。处置已完全流程化：restore --source=HEAD --staged --worktree . + 核对文件数
6. 注意：main 仍领先 origin/main 5 commits 未推送；远程 github/feature/license-gate 未合并仍残留
7. 推送：main（a10cba8）成功推至双远程——Gitee origin（2ca5754..a10cba8）+ GitHub（2ca5754..a10cba8，今晚网络通畅一次过）。git status 确认 main 与 origin/main 已同步，无 ahead 标记

## 2026-09-09 下午（约 14:27-14:41）：云同步卡死排查

8. **用户反馈**：设置面板"保存并连接"+ 灵感速览"↑上传"按钮一直停在"正在连接并同步…"不报错；AI 问答同步疑似触发卡死
9. **关键突破口（用户自查）**：勾选"同步 AI 问答记录"= 卡死；取消勾选 = 上传"成功"但云端文件时间未更新
10. **静态分析路径**：读了 WebDAVProvider.cs / SyncEngine.cs / ChatSyncEngine.cs / SettingsWindow.xaml.cs / QuickViewWindow.xaml.cs / MainWindow.xaml.cs 的同步装配 / SyncSettings.cs / AppLog.cs / fc-sync-test-archive/Program.cs
11. **关键代码事实**：
    - WebDAVProvider.cs:37 HttpClient.Timeout=30s；PROPFIND/PUT/GET/DELETE/MKCOL 全部走 30s 超时
    - SyncEngine.SyncNowAsync 在笔记成功后触发 CycleCompleted（MainWindow.xaml.cs:281），handler 是 `_chatSyncEngine.RunOnceAsync().ContinueWith(...)`——这个 Task 等 RunOnceAsync 跑完才完成
    - ChatSyncEngine.RunOnceAsync 内部 try/catch 兜底，**唯一不写 SetChatStatus 的情况是 await 永远不返回**——理论上 30s HttpClient 超时兜底
    - **Services/Sync 下 WebDAVProvider / SyncEngine 完全没有 AppLog 调用**——只有 ChatSyncEngine / RecycleBinService 用 Debug.WriteLine（不进文件）
12. **日志现场**：%AppData%\FocusCapture\logs\app_2026-09-09.log 351KB，**完全无任何同步相关记录**——印证"代码没写日志"。日志中只有 AIDialogWindow 反复 XamlParseException 刷屏 + 11:34 启动记录
13. **settings.json 现场**：
    - ChatSyncEnabled = true（用户当前勾选）
    - LastSyncResult = "成功"，LastSyncAt = "2026-09-09 11:34"（与日志启动时间吻合，启动后跑了次笔记同步）
    - **ChatSyncResult = ""，ChatSyncAt = ""**（AI 会话同步状态字段**始终是初始空值**）——说明 ChatSyncEngine.SetChatStatus **从未被调用过**
    - AutoSyncEnabled = false（手动同步模式）
14. **症状推断**：
    - 症状 1（勾选卡死）：ChatSyncEngine.RunOnceAsync 内部某个 await 没在 30s 内返回；UI 状态被 `await handler()` 间接挂住，永远不更新；理论上 30s 后应超时抛错——用户体感 30s 内放弃
    - 症状 2（取消勾选 → UI 成功但云端没更新）：两种可能——A. **用户没改笔记 = 走幂等跳过 = 设计行为**（PushAsync 见到 existing==pair.Value 就 continue，Bug 在用户预期，不在代码）；B. **真没传上去 = 同步失败被吞**
15. **待用户澄清**：取消勾选后"做了一次笔记上传"前**改过笔记吗**？——决定症状 2 是设计行为还是真 bug
16. **方案对比待用户拍板**：
    - A. 加日志（WebDAVProvider/SyncEngine/ChatSyncEngine 关键 await 点加 AppLog.Info/Warn/Error，下次卡立刻定位）
    - B. 加 UI 实时进度反馈（"正在 PROPFIND 列目录/正在推 3/N 个会话"），让用户能看到卡哪一步
    - C. 把 CycleCompleted 改成真 fire-and-forget（不 await handler，让笔记同步先返回，ChatSyncEngine 后台跑）——牺牲"会话同步的完成时间可见"换"笔记同步 UI 不被拖慢"
17. **架构红线**（MEMORY.md 既有）：WebDAVProvider / CryptoService / Dpapi 是强制 L2 单点故障——动 WebDAVProvider 加日志必须同步更新 REGRESSION.md A 级/B-3 板块

## 2026-09-09 云同步双 Bug 排查（只排查未改码）

- **Bug1 实锤（同步闸自死锁）**：SyncNowAsync 搭车 hook（SyncEngine.cs:234）先 `await _gate.WaitAsync()` 再调 CycleCompleted → ChatSyncEngine.RunOnceAsync 内部再次 `await _gate.WaitAsync()`（ChatSyncEngine.cs:128）。SemaphoreSlim(1,1) 不可重入 → 死锁。现象完全对上：勾选 AI 问答同步后「正在连接并同步…」卡死；取消勾选 RunOnceAsync 首行 return 不碰闸 → 正常。副作用：死锁后闸永不释放，笔记同步也全堵死（重启才恢复）。旁证：settings.json 里 ChatSyncResult 为空串、chat_sync_state.json 不存在 = 会话同步从未跑完一轮。
- **Bug2 判断（增量游标 + 原始时间戳，先前就有）**：PushFlowAsync 推送时 UpdatedAt=笔记行原始时间戳（非上传时刻，SyncEngine.cs:449）；PullAsync 增量过滤 UpdatedAt>=since（WebDAVProvider.cs:51），且游标推进取全桶最大 UpdatedAt（:53，含未投递的）。→ B 设备自己推过一轮后游标=云端最新时间戳，A 事后上传的时间戳更旧的笔记永远被过滤，B 一条拉不到/部分拉不到（5→4）。与 AI 问答同步无关，属存量设计缺陷。
- **附带发现**：今日日志 3 次 AIDialogWindow XamlParseException（xaml 121 行 52 列，DeferredBinaryDeserializerExtension），AI 对话窗口今天打不开过；疑与 tmp_baml_test/BAML 缓存相关，未深入。

## 2026-09-09 双 Bug 修复（两个分支，均未合并待验收）

- **Bug1 死锁修复 → feature/ai-chat-sync（55763be）**：SyncNowAsync 搭车 hook 去掉预拿闸，串行由 RunOnceAsync 内部拿闸保证。改动 1 文件 -9/+7 行。编译通过（0 警告 0 错误）
- **Bug2 游标修复 → feature/sync-cursor-fix（036ce99，从 ai-chat-sync 切出，含死锁修复）**：双时间戳方案——SyncNote +UploadedAt（上传时刻）作增量游标键，存量行空值始终投递（升级首轮全量补齐历史漏项）；云端已有行保留原 UploadedAt 防桶漂移；三处墓碑生成补 UploadedAt；软删重放走 RecycleBinService.AddIfAbsent 幂等落站。改动 6 文件。编译通过
- 决策记录：曾考虑「pull 直接全量对账」最简方案，因 RecycleBinService.Add 不去重（墓碑重放会重复塞回收站）而否决；选定双时间戳
- REGRESSION.md B-3 +4 条验收；BRANCHES.md 两分支状态已登记
- 未做：真机双设备验证（B 设备行为是推断）、未合并未推送（等用户验收）

## 2026-09-09 B 端拉取失败二次排查（只排查未改码）

- **根因1（实锤，最严重）：A/B 两端 E2EE 密钥不一致 + 云端盐为空**。用 A 的 DEK 解密云端 49 行笔记：34 行（A 推）成功、15 行（B=a50cf30d 推）全部失败。云端 sync_meta.json saltBase64=""（空）→ 无对齐锚点。SetTokenKeyAsync 判定缺陷：「meta 存在但盐空」被当「真首配」各自生盐；且 _saltNeedsUpload 是实例字段，引擎重建/重启即丢 → 生了盐也可能永远没上传。B 端拉 A 的行全部静默解密失败（CryptographicException → skip → 状态仍「成功（仅拉取）」）→ 一条拉不到。A 同理解不开 B 的 15 行。历史上 5→4 时代两端盐应一致，某次变更后分叉。
- **根因2（实锤）：ChatSyncEngine 分组/删除清单 Base64 引号 bug**。上传时 JsonSerializer.Serialize(cipher) 给密文包了 JSON 引号（云端 chat_deletions.json 实测内容 '"AGz9..."'），下载后直接 CryptoService.Decrypt(原始文本) → Convert.FromBase64String 抛 ArgumentException（截图报错原文）。A 第一轮上传成功（云端无文件不解密）是假象，A 第二轮起也会炸同样错。该异常让 RunOnceAsync 整轮 catch 中断 → SyncSessionsAsync 不执行 → B 一条会话都拉不到（云端已有 85 个 chat 文件）。会话文件 chat-*.json 本身没问题（Data 在包络对象内）。
- **设计缺陷3：解密失败静默跳过 + 状态显示成功**，误导用户（PullFlowAsync 只 StatusChanged 一闪而过）。
- 云端现状：笔记 49 行（34A+15B）、chat 会话 85 个、chat_deletions.json 带引号、无 chat_groups.json、cursor=11:52Z、uploadedAt 全空（A 修复版推送时桶内容无变化未重写）。
- 修复方向（待确认）：①盐——用 A 端「重置同步状态」统一密钥（云端全量重传+A 盐上云，B 拉到盐变更自动刷新重派生，B 本地独有数据下次 push 自动重传不丢）；②引号——下载侧先 Deserialize<string> 再解密（兼容存量）；③解密失败计数并显示在状态里。

## 2026-09-09 第三轮：盐自愈 + 引号修复（696270a，feature/sync-cursor-fix，未合并）

- 实测 A 端重置后云端：30 行笔记全部 A 密钥（解密 30/30 成功）、uploadedAt 全有值（游标修复生效）；但云端盐仍空（旧版 ResetSyncAsync 不写盐）、chat_deletions.json 带引号仍在
- 修复三件套：①SaltNeedsUpload 持久化（SyncSettings 新字段）+ PullFlowAsync 盐空自愈补传 + ResetSyncAsync 重置写盐；②ChatSyncEngine 上传改裸 Base64 + UnwrapCipherJson 下载兼容两种格式；③解密失败计数进 LastSyncResult + 失败轮不推进游标（防游标永久过滤变体）
- 编译通过；REGRESSION.md B-3 累计 +7 条
- 待办：A 端部署新版跑一轮同步（盐上云）→ B 端部署新版同步（自动对齐密钥拉全量）→ 双向验证；分支未合并未推送

## 2026-09-09 第四轮：B 端 503 限流排查（只排查未改码）

- **好消息（实测）**：B 端「上次同步成功」= 盐对齐成功，笔记待办已拉通，密钥分叉问题已解决
- **503 根因（实锤）**：ChatSyncEngine.SyncSessionsAsync 对云端 85+ 个会话文件「全量逐个 GET、紧密无间隔、全部 GET 完才落地」。B 首拉一轮 85+ 请求 + 用户反复点保存并连接，击穿坚果云红线（代码注释自认：30min ≤600 请求/单次 >200 文件 503）
- **恶性循环（实锤）**：22:40 左右用 A 端机器发单个只读 PROPFIND 也 503（3 次重试跨 40 秒全失败）= 账号处于限流惩罚期。A/B 两端若都开着自动同步，每 30 分钟各来一轮 85+ GET 全量对账，会不断重新触发限流、惩罚期被续期
- **「正在连接并同步」卡住解释（实锤）**：设置页固定文案，等 SyncNowAsync 返回才刷新；SyncNowAsync 会 await 会话同步全程（85 GET 约 1-3 分钟），且设置页不订阅 ChatSyncEngine.StatusChanged → 看起来像死机，其实在慢慢 GET。关闭重开读 settings.json 留痕才看到真实结果
- **设计缺陷**：对账全有全无——503 抛在 GET 循环中段，前面已下载的全部作废，一条会话都没落地。云端 85 个文件不清掉，B 永远拉不全
- **止损（已建议用户）**：A/B 两端先关自动同步或退出应用 30 分钟，让坚果云惩罚期自然解除，否则互喂限流
- **修复方向（待拍板）**：①ETag/LastModified 增量对账（稳态请求 85→近零）；②分批+请求间隔+503 退避；③已下载部分落地（不再全有全无）；④设置页订阅 ChatSyncEngine 状态

## 2026-09-09 第五轮：B 端两个新现象排查（只排查未改码）

- **退出弹窗（机制正常，暴露新问题）**：MainWindow.FlushBeforeExitAsync 4 秒硬超时（MainWindow.xaml.cs:439）→ SyncNowAsync 现 await 会话同步全程（85 GET 1-3 分钟）→ 只要会话同步开着，退出 flush 必然超时弹窗。选「否」数据不丢（本地事实源，下次启动补传）。新交互退化：503 期间点「是」重试必失败，毫无意义
- **灵感速览无新笔记（大概率正常现象组合，已给验证法）**：①灵感速览默认 _selectedDate=今天（QuickViewWindow.xaml.cs:155），LoadNotes 按行内日期过滤；A 端 30 条行内日期分布=09-07×10、09-09×8、09-01×8、09-08×3、09-03×1 → 「今天」视图最多见 8 条，22 条旧笔记要翻日期；②QuickViewWindow 刷新时机=override Show() + Window_Activated，窗口一直开着不重刷；AppendLine（拉取落地）不发 NotesChanged（也不该发，防反向触发上传）。验证法：B 端翻到 09-07 或点别的窗口再点回灵感速览
- 方案追加：⑤退出 flush 预算与交互修正（笔记部分才值得弹窗，会话失败静默补传）；⑥拉取落地后发专用 UI 刷新通知（独立事件，不触发上传）

## 2026-09-09 第六轮：B 端 0 拉取定案（B 端日志 + 云端只读实测）

- **推翻「日期过滤」解释**：B 端实测翻日期也没有、MD 文件没生成 = 真的一条没落地。灵感速览日期过滤只是叠加的显示现象，不是根因
- **B 端日志**：同步引擎不写日志文件（设计），日志仅启动记录+1 个无关剪贴板崩溃（QuickViewWindow.NoteItem_Click OpenClipboard 失败，小 bug 待修），无同步线索
- **云端实测（限流已解除）**：云盐已上云且=A 本地盐（44/yvhBt，A 端「盐空自愈」生效实锤）；笔记 34 行=A 30 条+B 4 条测试行；uploadedAt 全有值，A 30 条≈12:54Z（20:54 本地）重传，B 4 条最晚 14:22:13Z（22:22 本地，B 22:22 轮推的）；chat 85 个
- **定案（推断，证据链闭环）**：B 端在装最新版**之前**的实例（20:45 启动的旧版）跑过同步——旧版「解密失败静默跳过+游标照样推进」把 B 的 LastCursor 推到云端最大 uploadedAt（≈13:xxZ）；B 22:22 装新版后 pull 时 A 的 30 条 uploadedAt 全 < 游标 → 空投递 → decryptFailed=0 → 显示纯「成功」→ 游标又推到 14:22Z。游标只进不退 → B 永远拉不到。这正是「解密失败不推进游标」修复要防的场景，但 B 的游标在修复生效前已被推坏（迁移期残留伤害）
- **修复方案追加⑦**：盐对齐切换成功时清空 LastCursor（旧密钥时代的游标作废）+ 设置页加「从云端重新拉取」按钮（清游标跑仅拉取，幂等去重）——后者是 B 现状的一键自救入口（B 已对齐过盐，不会再触发 saltChanged 分支，纯清游标逻辑救不了，必须给显式入口）
- B 端本地笔记现仅 4 条：A 重置前 B 推过云端的 15 条，若 B 本地也没有则已真丢（提醒用户核对 B 本地笔记数）

## 2026-09-09 第七轮：七项修复全部完成（7094863，feature/sync-cursor-fix，未合并）

- 用户拍板：B 端丢失的 15 条笔记放弃，重点保证以后不出问题
- 七项落地：①会话增量对账（IFileStorageProvider.ListFilesAsync 升级 CloudFileInfo+LastModified、ChatSyncState.KnownStamps、未变文件跳过 GET，稳态请求 85+→近零）②503/429 退避重试（SendAsync 请求工厂化 5s/15s ×2）+150ms 节流 ③部分落地（SyncSessionsAsync 返回 partialError，状态「部分成功」）④退出 flush includeChat=false（治 4 秒必弹窗）⑤CloudDataLanded 独立事件刷灵感速览+角标（绝不接 NotifyLocalChange）⑥设置页「从云端重新拉取」按钮+StatusChanged 实时订阅 ⑦密钥对齐自动清游标
- 编译 0 警告 0 错误；REGRESSION.md B-3 累计 +7 条（今日三批共 +11）；BRANCHES.md 已更新
- B 端验收路径：装新版 → 设置 → 云同步 → 点「从云端重新拉取」→ 应显示「重新拉取完成」且 A 的 30 条笔记落地（部分在旧日期，需翻日历看）
- 未做：真机双设备验证；合并/推送等验收后
- 附带发现：QuickViewWindow.NoteItem_Click OpenClipboard 崩溃（B 端日志），低优先级待修

## 2026-09-09 收尾：合并 + 推送完成

- feature/sync-cursor-fix（0240c3b，含 BRANCHES.md 状态登记）→ main fast-forward 合入（带着 ai-chat-sync 全部功能 + 死锁修复，云端同步功能正式进主线）
- 合并后 main 编译 0 警告 0 错误；推送 origin(Gitee) 与 github 均成功（a10cba8..0240c3b）
- GitHub 推送 SSL/502 失败 2 次，第三次（等 120s）成功——符合「失败别反复硬试」经验
- **索引异常 bug 第三次复发**：checkout main 后 68 个文件被误标 D 且真的从磁盘消失；`git restore --source=HEAD --staged --worktree .` 一次恢复，零损失。git 环境问题确认未修，今后每次 checkout/merge 后必验 `git status`
- 待办遗留：QuickViewWindow 剪贴板 OpenClipboard 崩溃（低优先级）；两台设备部署 main 最新版

## 2026-09-09 深夜（23:35-23:55）文档清理

1. 全量核实两文档 vs git 实况：BRANCHES.md 抓出 7 处失实（3 条已并入分支仍标「开发中」、build-trim-iconfix 重复登记自相矛盾、website-refresh 漏登记、GitHub「仅剩 main」失实）；归档表 5 个「功能已在 main」的 hash 实测全部有独有提交未并入，旧登记不准
2. 执行方案 B：BRANCHES.md 重写（269 行级压缩：归档表拆 4.1 有独有提交/4.2 已并入一句话化，第三节只留 main）；REGRESSION.md 修 B-7 写死日期（09-07→次日 12:00）、B-8 写死尺寸、清 B-3 行内日期标注 4 处
3. 删除 6 个已合并本地 feature 分支（-D）；GitHub license-gate 残留补删两次 SSL 断连失败，已如实记入文档待网络窗口补删
4. 提交 c21bd86（main，docs）未推送；诚实结论：REGRESSION.md 收益>成本值得养，BRANCHES.md 建议降级为「只登记活跃分支+放弃未并入归档」，已并入行可整个砍
5. 用户拍板「按你说的做」：BRANCHES.md 二次瘦身定稿（662f255）——删 4.2 已并入 21 行，铁律第 2 条改「合并即删且不登记」；文档定位改「只记 git 现查不到的事」。两个 docs commit（c21bd86/662f255）均在 main 未推送
