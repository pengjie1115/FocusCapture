# 2026-09-05

## 双 Agent 并行开发前耦合摸底（只读分析，未改任何代码）

- 目标：DeepSeek harness 改「AI 模型板块」与 WorkBuddy 改「待办提醒板块」并行开发，先评估耦合
- 结论：可并行，但有 3 个共享冲突点——
  1. `Windows/SettingsWindow.xaml.cs`(854行)+`.xaml`(679行)：PanelAi 与「待办与提醒」面板同文件混居，最大冲突点
  2. `Models/AppSettings.cs`(129行)：AI 字段(57-59行)与提醒字段(SnoozeMinutes/DailySummary*)同文件，引用 20 处
  3. `Services/TodoEditService.cs`(104行)：逻辑级耦合——待办时间解析用 IChatProvider 做 LLM 兜底（ResolveDueAsync→LlmFallbackAsync），AI 接口改动会波及待办
- ReminderService.cs(210行) 本身引用面很小（仅 MainWindow），板块内部安全
- **git 异常**：`.git/HEAD` 指向 `refs/heads/.invalid`（该分支文件不存在）——与已知 git 2.55 环境异常吻合。建 worktree/分支前必须先 `git status`/`git branch` 核实仓库状态
- 建议顺序：先单 Agent 拆 SettingsWindow（AI 面板/提醒面板分离）→ 写 AGENTS.md 公约（划地盘+AppSettings 分区规则）→ worktree 并行

## 云同步授权码提示文案修复（feature/sync-hint-text-fix 分支）

- 背景：核实坚果云网页端「安全-第三方应用管理」已不给生成入口，应用密码只能客户端/手机 APP 申请（用户截图实锤，官方旧帮助文档信息过时）
- 改动：SettingsWindow.xaml 第 552 行提示文案「网页端『安全-第三方应用管理』」→「客户端（或手机 APP）『设置 → 第三方应用管理』」，仅 1 行
- 分支 feature/sync-hint-text-fix，commit 6c35f5a，编译通过（0 警告 0 错误），待用户验收后合 main
- **遗留待用户决策**：同样过时的「网页端」文案还有 4 处——SettingsWindow.xaml.cs:768（状态提示）、WebDAVProvider.cs:232 + SyncEngine.cs:243/287（401 报错提示），用户要求本次只改 1 处，未动
- **踩坑（重要）**：WorkBuddy 的 bash shell 环境变量被裁剪——APPDATA/PROGRAMFILES/ProgramData 均为空，导致 dotnet restore 报「Value cannot be null. (Parameter 'path1')」（NuGet.targets GetRestoreSettingsTask）。编译前需手动 export APPDATA、PROGRAMFILES 等变量。非项目问题，用户本机终端编译不受影响。已同步到用户级 MEMORY.md

## 云同步自动建目录核实（19:11，只核实未改码）
- 用户用新账号实测：填账号+授权码后云端目录未自动创建，与此前说法矛盾。核实结论：
- 事实：EnsureDirectoryAsync（PROPFIND 404→MKCOL）只挂在 PushAsync 和 SaveSaltAsync 两条路径；首次同步顺序 PushSalt→Pull→Push，**拉取路径（FullAsync/ListFilesAsync PROPFIND）无任何目录保护**
- 判断（高置信）：换账号场景本地 E2eeSalt 不清空 → _saltNeedsUpload=false → 跳过盐上传 → 直接拉取 → PROPFIND 404 → 「列目录失败 (HTTP 404)」，MKCOL 执行不到；用户手工建目录后 PROPFIND 通，现象完全吻合
- 判断：真·全新用户理论走盐上传路径可自动建目录，未实测
- 修复方向（待用户确认后动）：拉取路径补 EnsureDirectoryAsync（WebDAVProvider 单文件，最小改动）

## 4 处过时文案修复（19:19，feature/sync-hint-text-fix 分支）
- 分支同 6c35f5a，commit c204f3b：SettingsWindow.xaml.cs:768、WebDAVProvider.cs:232、SyncEngine.cs:243/287，「网页端『安全-第三方应用管理』」→「客户端（或手机 APP）『设置 → 第三方应用管理』」
- 影响面 L0：纯字符串字面量，已扫描确认无代码依赖文案内容；dotnet build 0 警告 0 错误
- Debug 版已编译：bin/Debug/net8.0-windows/FocusCapture.exe（19:19）
- 待办：云同步自动建目录拉取路径无保护问题，用户换电脑测试后再定；分支验收后合 main + 推双远程

## 用户第二台电脑实测反馈（19:29）
- 实测报「连接失败：列目录失败 (HTTP 404)」——与 19:11 判断完全吻合（拉取路径无目录保护，PROPFIND 404 直接抛错）
- 用户质疑代码硬编码其 QQ 邮箱：全项目 grep「3097199704」「@qq.com」零匹配；WebDavUser 默认值就是空字符串（SyncSettings.cs:20）。设置页账号来自 %AppData%\FocusCapture\settings.json 回填（SettingsWindow.xaml.cs:329），点保存并连接时先存盘再连接（774-778 行），失败也会持久化。判断：用户看到的是自己输入后被记住的，非代码写死
- 修复方案已给用户：FullAsync 开头补 EnsureDirectoryAsync（约 2 行），等用户确认后动

## 拉取路径自动建目录修复（19:35）
- 分支 feature/sync-auto-mkdir（叠在文案分支上），commit 11960fd：FullAsync 开头补 EnsureDirectoryAsync，1 文件 +2 行
- 编译 0 警告 0 错误，Debug exe 已更新（19:35）
- 已知边界：双全新设备同秒首同步 MKCOL 撞 405 为既有行为，未顺手改
- 待用户现象级验收：删云端目录 → 填账号授权码 → 连接应成功且云端自动出现 FocusCapture 文件夹

## 目录探测 400 白名单补充（20:03）
- 用户新设备实测：报错从「列目录失败 404」变「WebDAV 目录探测失败 (HTTP 400)」——证明拉取路径保护已生效，卡在 EnsureDirectoryAsync 白名单（只认 404/405/409，坚果云实测对不存在目录返回 400）
- commit ea5be50：白名单加 400 → MKCOL。400 语义未验证（无官方文档），风险低：目录实际存在时 MKCOL 返回 405 显性报错
- 若 400 仍在，Plan B：将 4xx（除 401/403）全部视为不存在走 MKCOL
- 截图确认授权码新文案已上线，验证链闭合

## 分支合并收尾（21:33）
- 用户确认新设备实测：自动创建文件夹成功（400 白名单修复生效，全链路打通）
- 合并：feature/sync-auto-mkdir（含全部 4 commit）ff 进 main（8e156b3→ea5be50），合并前后文件数 106=106 无索引异常
- 删 3 个本地分支：sync-auto-mkdir、sync-hint-text-fix、build-trim-iconfix（远程已删的残留）；BRANCHES.md 已登记，commit c40192c
- 推送：Gitee 成功（至 c40192c）；GitHub 两次失败（HTTP2 framing / 502 代理），按「不反复硬试」规矩暂缓，待网络恢复后 `git -c http.version=HTTP/1.1 push github main`

## 网站调整（21:45 前后，feature/website-refresh 分支）
- 修复 docs/index.html 第四张截图（input.png，750x100 细长条）预览显示问题：根因是 .shot img 的 16:10+object-fit:cover 把黑区域放大填满卡片；新增 .shot img.fit{object-fit:contain} 专用类
- 新增 Gitee 渠道（gitee.com/pj1115/FocusCapture）：导航按钮、Hero 主下载按钮改为 Gitee（国内可达优先）、下载区双按钮、页脚链接、llms.txt 同步
- commit 5de4551，待用户验收后合并
- 遗留讨论：GitHub Pages 国内访问风险（建议先上 GitHub Pages，差评再上 EdgeOne 镜像）；留言板块推荐轻量方案（Gitee Issue + 邮箱入口），未实施

## 上手板块重写（22:00，commit b1704b9）
- 用户指出上手板块重点是「零习惯改变」的转化叙事而非快捷键手册；重写为：标题「装完即用，不用学」+ 三步（下载解压双击运行/该干嘛干嘛/想起来去看看）+ 热键压缩为「只需记住 Alt+Space」一句话
- 发现异常：两次提交之间 docs/index.html 被外部工具加上了 data-page-node-id 属性（全文件），去属性后内容 diff 干净，无损。原因未查（判断：用户侧某设计/编辑工具所为）
- 留言板块用户决定暂不做

## 隐私表述对齐（22:10，commit e1461fb）
- 用户指出「数据全本地」卡片与实际不符（实际支持绑定坚果云 WebDAV 多端同步）；按「默认全本地 + 可选端到端加密同步」统一措辞改 7 处：功能卡片（标题改「本地优先，同步可选」）、meta/og/twitter 描述、JSON-LD 两处、隐私板块 01 条目、页脚、llms.txt；keywords 补 WebDAV/坚果云/多端同步
- 校验：全站无「所有数据只存在/不涉及任何云端/数据 100% 本地」残留

## 合并+部署上线（22:15 前后）
- feature/website-refresh（3 commit）ff-only 合入 main，git status 验证正常；推送 Gitee + GitHub 双远程成功
- 仓库 pengjie1115/FocusCapture 已改为 public（gh CLI 未安装，改用 git credential fill 取本地凭据调 REST API 完成）
- GitHub Pages 已开启：main 分支 /docs 目录，https://pengjie1115.github.io/FocusCapture/ 已上线（HTTP 200，构建状态 built）
- 线上内容验证通过：Gitee 链接×5、「本地优先，同步可选」「装完即用」均已生效
