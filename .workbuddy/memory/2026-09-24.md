# 2026-09-24

## 07:00~07:30 用户睡前授权"接着干"：完成步 2b + 批 3a

用户 9-23 睡前授权"只要检查点跑完就继续下一批次，直到完成；决策点自行处理、明早汇报；小步提交，暂不合并暂不推送"。
今早（9-24）用户说"接着干"，继续推进。

**分支 `feature/ai-chat-ui-revamp` 现 6 提交**（未合并、未推送）：
`3eb5b32` 设计稿 → `a4258ce` 批 0 → `6e1f19f` 批 1 → `f001e84` 步 2a → `17c3320` 步 2b → `9c6d148` 批 3a

### 步 2b（`17c3320`）：侧边栏接入 + 分组视图

- `AIDialogWindow` 用 `ChatSidebar` 取代 `HistoryDrawer`：标题栏「历史」按钮、宽度拖拽、收起展开动画**全部复用原有机制**，只换控件
- 分组视图：点侧边栏分组 → 主区切成该分组的会话列表；工具行 = 分组名 + 添加指令 / 编辑分组名 / 删除分组 / 返回对话；**输入区不动**（在分组里输入 = 该分组下新建会话）
- 收藏是视图不是真分组 → 三个编辑按钮隐藏，不给"点了没反应"的假入口
- 分组三点菜单（重命名 / 置顶此分组 / 删除此分组）、会话三点菜单（重命名 / 置顶 / 收藏 / 分组到… / 导出 / 删除）
- `ChatItemAction` 新增 `Favorite`（收藏与分组共用 `GroupId`，归属互斥）
- `PromptDialog` 加 `multiline` 参数（分组指令要写一整段话）
- 清理：批量操作入口随旧抽屉退场，`HandleBatchAction` 删除（功能没丢）

**「置顶此分组」的实现判断（待用户确认）**：我实现为"把该分组内所有会话置顶"，因为
"分组本身排序靠前"要给 `ChatGroup` 加字段 + 再配一套跨端合并裁决。若用户要的是后者，换实现即可、不动数据模型。

### 批 3a（`9c6d148`）：起手页与输入区

- 输入框**居中 / 沉底**：起手态垂直居中（两侧留白 28），有消息则沉底留 16px；切换用 140ms 淡入
  · 位置变化（Grid.Row / VerticalAlignment）没法补间，硬做位移动画要引入 Canvas 定位、把布局搞脆 —— 不做
  · 触发点挂在 `UpdatePlaceholder` 上（发消息/切会话/切分组都会经过），避免在十几个调用点各插一次
- 欢迎语：「{昵称}，今天干点啥？」+ 可选图标（走 `ChatAssetsService`）
- 加号合并成一个（弹出「添加附件」/「引用文件」两项）
- 发送按钮文字 → 箭头图标（生成中换方块），行为未改
- 侧边栏默认展开读 `ChatSidebarDefaultExpanded`（放在 Loaded，避免窗口未渲染时动画停在 Width=0）

### 快照当场抓到的两个真 bug（值得记）

给侧边栏加了两张快照（10b 侧边栏 / 10c 分组视图），第一版图出来发现：
1. **数据重复两套** —— `SeedSidebarForSnapshot` 每个场景都造一遍，而快照工具的沙箱是同一个
2. 连带 **分组视图压根没打开** —— 「得到大脑」撞名建不出来 → `_snapshotGroupId` 为空 → 右侧一片空白，
   而**从图上完全看不出这是个错误**（只表现为"空对话区"）
修法：Seeder 只在沙箱里清掉上一场景的数据。两张图重出后确认无误。

**教训**：快照 Seeder 必须幂等 —— 每个场景 new 一个窗口，但沙箱是共享的。

### 剩余未做（明确清单，不含糊）

| 项 | 说明 |
|---|---|
| **设置项界面入口** | 昵称 / 欢迎语图标 / 用户头像 / 侧边栏默认展开 —— 代码全通，但**设置界面还没有这些控件**，目前只能改 settings.json。这是批 3 剩下的最要紧一块（否则用户没法自己设昵称、传图标） |
| 侧边栏全文搜索 UI | `ChatSearchService` + `ChatSearchMatcher` 已就绪且被检查点守着（快层 13 条 + 慢层 8 条），但**界面没接** |
| 分组视图内的搜索 | 同上（用户要求：点搜索时覆盖工具行） |
| 输入框右下角会话级模型下拉 | `SessionFile.ModelKey` 数据通路已通（批 1），**切换 UI 未做** |
| 分组视图高亮当前分组 | 小优化：在分组视图里时，侧边栏对应那一行没有高亮 |
| 收尾 | `GroupsChanged` 接线（需先给 `ChatSyncEngine` 加 `Dispose`）、失效检查点同步（`REGRESSION.md` B-13/B-9 相关条目、`tests/sync/Program.cs:679` 标题栏断言）、`ready` 全量 |

### 环境提醒

`dev.ps1 snap` **不会先编译** —— 改完代码直接 snap 出的还是旧图（今早第一次就踩了，以为场景没生效）。正确顺序：先 `build` 再 `snap`。

## 09:30~11:15 用户实测反馈 13 项，批 A~D 全部修完 + 交接文档

用户实测后一轮报了 13 个问题（悬停全员高亮 / 置顶分组没反应 / 分组内会话不归组 / 「分组到…」把分组清空 / 重命名清空标题 / 子菜单白底白字 / 分组删除复活 / 批量操作丢失 / 组内搜索缺失 / 输入框窄 / 箭头方向 / 加号括号文案 / 选择分组没做），先逐条查实根因、4 个决策点弹窗问过用户（删除即终态+墓碑永久保留；退出分组靠侧边栏+点分组自动收侧边栏；批量=三点菜单入口+底部操作条；「分组到…」现象=跳到最近最上），然后分 4 批修完：

- **批 A `d74a531`**：悬停改 `Trigger SourceName`（根因：DataTemplate 里 `AncestorType=Border` 绑到外层共享 Border）；`ChatGroup.Pinned/PinnedUpdatedAt` + 排序靠前 + 置顶徽标；合并裁决补置顶（bool LWW，取消也要刷时间戳）；App.xaml 补 MenuItem 深色模板（子菜单白底根因=系统模板弹出层背景非 TemplateBinding）；菜单父项不绑 Click（`Group+null` 被宿主解释成"移出分组"→ GroupId 清空 + SavedAt 刷新 = 用户看到的"跳到最近最上"）；PromptDialog 的 Focus/SelectAll 挪到 Loaded 后（原在 ShowDialog 前调用被忽略）
- **批 B `fc59446`**：分组删除墓碑。根因=并集合并表达不了删除。`DeletedAt` + `Load()`（UI 口径过滤墓碑）/`LoadAll()`（同步口径）分离 + `Mutate` 落盘时自动并回墓碑；删除即终态（任一端带墓碑 → 结果是墓碑，删除胜过一切修改）；同名合并败者也标墓碑
- **批 C `30fe56c`**：`StartNewSession` 加 `groupId`（此前根本不传——"分组里发消息不归组"的真根因）；发送即归组新建+退出分组视图+收侧边栏；工具行图标化、删「返回对话」、点分组收侧边栏、组内搜索 UI（防抖 280ms+后台线程+范围本分组全文）；进分组视图后欢迎语残留+输入区未沉底（快照抓到，`RefreshComposerLayout` 沉底条件补分组视图）
- **批 D `0f36d23`**：批量操作找回（三点菜单入口 → ChatSidebar 多选 → 底部操作条；选中高亮复用 `HistoryItemViewModel.IsSelected` 的 INPC——旧抽屉遗留基础设施直接用上了）；输入区按 WorkBuddy 重排（加号/发送进框、圆形向上箭头、去括号文案、「选择分组」入口：起手态选组第一句话归组/对话态点击移归属）
- **交接 `45b97a7`**：`docs/2026-09-24-AI问答重构交接文档.md`（剩余 7 任务：REGRESSION 条数同步 / 失效检查点 / 设置项 UI / 全局搜索 UI / 模型下拉 / GroupsChanged 接线（需先 Dispose）/ 杂项）

`ready` 全绿：编译 0 警 0 错 / 快层 175 / 慢层 ALL PASSED（会话分组 63 条）/ 文档 19/19。分支 9 提交，未合并未推送。

### 本轮新踩的坑（已写进交接文档 §六）

- XAML 模板根只能一个子级（MC3089）；`Button.Template` 与直接内容并存非法（MC3000）；模板内 x:Name 不进窗口 NameScope
- DataTemplate 条目状态禁用 `RelativeSource AncestorType`
- `_lastSessions` 字段类型写错（HistoryItemViewModel vs SessionSummary）编译才暴露——批量重刷需要的是 Summary 集合
- HandleChips 在重写输入区时被复制了一份 → CS0102 重复定义
- `Dispatcher.BeginInvoke` 在 async 方法里返回可等待对象 → CS4014，要 `_ =` 丢弃

## 15:10 记忆方案设计定稿（第一性原理版 + 用户三轮质疑修正）

理想方案（不参考业界推导）：`.wb-memory/`（实际沿用 `.workbuddy/memory/`）四件套——NOW.md（唯一活文件 ≤40 行，覆盖式自过期）/ DECISIONS.md（决策点卡片）/ PITFALLS.md（确定性环境坑）/ daily/（append-only 流水）。常驻面只有 AGENTS.md 三行路由。三条本项目特有设计：能进脚本的不进记忆、状态覆盖式自过期、冷启动测试验收（新会话三问答不上=记忆烂了）。

用户质疑后修订三条：
1. DECISIONS.md 由"只追加"改为**决策点卡片**：更新时只判"有无新论据"——有则旧答案压条目内一行史，无则直接覆盖。膨胀与决策点数挂钩、与翻转次数脱钩。同点 30 天翻转≥2 次=没想清楚的信号，停下找用户拍板，不继续记账。用户提的"重要性判断"判据被反驳：不可机械执行，换成"有无新论据"。
2. 蒸馏不承诺准时：WorkBuddy 自动化（每周）有保证，其他 Agent 只能挂软钩子；核心改为**迟到无害化**——daily 不参与常驻加载、三件套自身有界，蒸馏降级为锦上添花。
3. Token 事实确认：读文件=整篇进上下文（粒度=文件级）；修改=先全文读入+diff，不是只传改动；纯追加可走命令行直写尾部不付全文代价（注意 PowerShell `>>` 产 UTF-16，追加必须显式 UTF-8）。

待落地：AGENTS.md 记忆路由段、CLAUDE.md 桥接一行（@AGENTS.md，Windows 用 import 不用 symlink）、四件套初始化、周蒸馏自动化任务、冷启动测试红线进 AGENTS.md。用户说落地即开工。

## 15:25~15:45 记忆系统 v1 落地（提交 d019842 + 55c69b2，在 feature/ai-chat-ui-revamp 分支上）

- 新建：`NOW.md`（20 行当前状态）/ `DECISIONS.md`（D01~D09 决策点卡片）/ `PITFALLS.md`（五组确定性坑）/ 根目录 `CLAUDE.md`（一行 @AGENTS.md 桥接）
- 修改：`AGENTS.md`（头注改三件事 + 路由表补一行 + 末尾新增「三、开发记忆」：四文件维护规则/写入纪律/不入记忆判据/冷启动测试验收）；`.gitignore`（`.workbuddy/*` 精确放行 `memory/*.md`，备份与 automations/ 仍忽略）；`MEMORY.md` 头注（进 git + 指向共享层）
- 提交：`d019842`（32 文件，只含本任务文件，另一工作流的未提交改动零触碰）+ `55c69b2`（修正流水路径：日志固定写 memory/ 根部，`daily/` 子目录作废）
- 自动化：周维护任务已建（id 58667c38，周日 21:30，冷启动三问自测 + 超 30 天日志蒸馏）；两处对设计的偏离：①蒸馏改「删除」为「移入 archive/」（红线 7 破坏性动作留给用户）②冷启动测试并进同一周任务而非单独建
- 踩坑：`dev.ps1 check-docs` 在路由表里存在但脚本未实现（EXIT=2 未知命令）→ 已报用户，未擅自改共享脚本
- 待用户拍板：①这两笔提交随 feature/ai-chat-ui-revamp 合并进 main，若想单独走可 cherry-pick ②check-docs 要不要补实现 ③是否 `dev.ps1 push`（当前推的是 main，此分支未合并，推不了）

## 16:00~16:30 用户拍板后执行：cherry-pick 进 docs 分支 + 合并 main + 补实现 check-docs

用户指令：补 check-docs；两笔记忆提交 cherry-pick 到 docs/ 分支合并进 main；**feature/ai-chat-ui-revamp 不做任何提交或合并**。

- 主工作区有另一会话未提交改动 → 全程用 **git worktree**（`../fc-docs-wt`，从 main 开 `docs/dev-memory-system`），主工作区零接触
- cherry-pick `d019842`→`953dcc9`、`55c69b2`→`6e5c441`，基准核验干净（main 与 45b97a7 的 AGENTS.md/.gitignore/CLAUDE.md 无差异）
- **check-docs 补实现**（worktree 内改 main 版 dev.ps1，与 feature 版无差异）：独立入口复用 ready 内嵌的 Test-DocRefs（逻辑一份不复制）；顺带修两处误报源——①裸文件名引用增加 `.workbuddy/memory/` 候选目录 ②白名单补 `YYYY-MM-DD.md`（日期模式占位名，同 SKILL.md 先例）
- 自测：9 处误报 → 修后剩 1 处（AGENTS.md→MIGRATION.md，干净检出必红：该文件刻意不进 git 仅本机有，**既有行为非本次引入**，主仓库跑则绿）
- 提交 `9985e7d`，main ff-only 合并 `6bc191e→9985e7d`，worktree 已清（worktree remove 报 Invalid argument 但实测删净，worktree list 仅剩主仓库）
- **终态**：main=9985e7d（含记忆系统+check-docs）；feature HEAD=5069627（另一会话自行推进，未被我触碰）；feature 上原两笔提交（d019842/55c69b2）保留未动，将来合并 git 自动去重
- **未推送**：用户只说合并到 main，push 等确认
- 主工作区留有一处未提交改动：本日志的 15:25 与 16:00 两段（在 feature 分支上，按用户指令不做提交，随下次合并带走）

## 14:00~14:45 跨 Agent 项目开发记忆需求评估 + 业界方案调研

用户需求：一套所有 Agent（WorkBuddy/Claude Code/Codex/dsh）都读写维护的项目开发记忆。已拍板：沿用 `.workbuddy/memory/`（AGENTS.md 路由指过去）、进 git、先软后硬。关键风险：①多真源（记忆与 AGENTS.md 抢地盘——记忆只存动态内容，长期规则升级进 AGENTS.md）②其他 Agent 只许追加日志、MEMORY.md 编辑权收归约定 ③WorkBuddy 工作记忆是会话开始注入、中途更新要下次会话才生效。

调研结论（详见对话）：AGENTS.md 已是 Linux Foundation 事实标准（~6 万项目），业界主流=静态规则文件（AGENTS.md/CLAUDE.md，<200 行）+ 动态记忆（Cline Memory Bank 六文件模式，2026-07 实测演化退役 progress.md——git 是 changelog，最值钱的是"评估过并否决"的内容）+ 分层路由（宪法 200 行/活记忆 50 行/项目脑/按需知识库）。文件方案已胜出，不需要 Mem0 类记忆服务（跨应用才需要）。ETH Zurich 2026：LLM 生成的上下文文件反而降成功率+20% 成本，必须人写精简。待落地：AGENTS.md 加记忆路由段 + CLAUDE.md 桥接一行（Windows 用 @import 不用 symlink）。

## 16:35~16:50 合并 feature/ai-chat-ui-revamp 进 main + 推送 [WorkBuddy]

- 用户指令：合并进 main + 推远端，GitHub 只试一次
- main（9985e7d）与 feature（e725b8b）已分叉（merge-base=6bc191e），`dev.ps1 merge` 的 ff-only 走不通 → **手动 `git merge --no-ff`**（合并提交 `687cf05`）
- 冲突 2 处，均为 add/add 或文案级：①`2026-09-24.md`（feature 版是 main 版严格超集，取 feature）②`AGENTS.md`（仅路径写法差异 `NOW.md` vs 全路径，取 feature 侧全路径写法）
- 合并后跑 `dev.ps1 ready` 全绿：编译 + 快层 175 + 慢层「会话分组」63 等全部 + 文档引用 20 处有效，EXIT=0
- 推送：Gitee（origin）成功 `6bc191e..687cf05`；GitHub 一次失败（SSL unexpected eof，国内网络），按用户指令不重试，下次再推
- 新环境坑：Git Bash 会把 `git show 分支:路径` 里的冒号参数做 MSYS 路径转换（`/`→`\`、`:`→`;`）导致 fatal，解法 `MSYS_NO_PATHCONV=1`
- 终态：main=687cf05，工作区干净；feature/ai-chat-ui-revamp 与 main 内容一致（合并完成）
