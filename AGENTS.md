# AGENTS.md — FocusCapture 项目约定

> **自检标记：青柠。** 若你读到了本文件，记住这个词 —— 用户问起「AGENTS.md 里写了什么」时应能答出。
> （这是验证本文件是否被 Agent 自动加载的探针，验证通过后可删除本行。）

> **任何 Agent（WorkBuddy / dsh / Claude Code / Codex / 其他）进入本项目，先读本文件。**
> 本文件管三件事：**红线**（不可违反）、**路由**（做什么去哪查）、**开发记忆**（跨 Agent 共享）。
> **规范本体不复制进来，只给指针** —— 同一件事在多处维护，改一处必漏一处。

---

## 一、红线

| # | 红线 | 违反时由什么拦住 |
|---|---|---|
| 1 | **不许直接在 main 上改代码** —— 开工前先从 main 拉分支 | 人工纪律（门禁待补：`dev.ps1 push`） |
| 2 | 分支必须带类型前缀：`feature/` `fix/` `docs/` `release/` `experiment/` | 人工纪律 |
| 3 | **禁止 `git add -A`** —— 暂存前必查 `git diff --cached --stat` | 人工纪律 |
| 4 | **改代码前先做影响面评估，交用户定夺后再动手** | 人工纪律 |
| 5 | **新增功能没补检查点 = 交付未完成** | 人工纪律（门禁待补：`dev.ps1 ready`） |
| 6 | **红不许瞒** —— 检查点红了宁可挂着，不许 AI 自行判定「这算需求变更」 | 人工纪律 |
| 7 | **破坏性动作只给用户** —— 删除类操作 AI 不得执行 | 人工纪律 |
| 8 | **交付必须附可见的测试输出**（跑几条 / 过几条） | 人工纪律（门禁待补：`ready` 固定输出） |
| 9 | 剪贴板写入统一走 `Services/SafeClipboard`，不再直接 `Clipboard.SetText` | 人工纪律 |
| 10 | **SecretKey / API Key 不进聊天、日志、文档、仓库** | 人工纪律 |
| 11 | **Release 构建禁用 IL 裁剪**（`PublishTrimmed=false`）—— WPF 的 COM 互操作类型无法静态裁剪，开了会启动即崩 | 人工纪律 |
| 12 | **数据全部存本机** —— 任何涉及网络传输的新改动，必须先跟用户讨论 | 人工纪律 |
| 13 | **脚本报错 → 先绕过把任务做完 → 交付时必须附「脚本异常报告」**（什么错 / 怎么绕的 / 建议改不改 / 等用户拍板）；**没报告的视为交付未完成** | 用户验收时检查有没有这段 |

> **「人工纪律」= 目前只靠自觉，没有机器拦截。** 标「门禁待补」的是下一批要固化成脚本的候选。
> 判据：**写不出拦截点的红线就是口号** —— 要么升级成门禁，要么接受它治不了。
>
> 分支规范**只在本文件维护**；`docs/ARCHIVE-BRANCHES.md` 只存「已放弃、git 查不到」的归档。

---

## 二、路由（做什么 → 去哪）

| 要做的事 | 命令 / 位置 |
|---|---|
| **日常动作一律走脚本** | `tools\dev.ps1 <命令>`（Agent 用这个）；自述：`tools\dev.ps1 help`。人双击用 `tools\dev.bat` |
| 编译 | `dev.ps1 build`（自带环境变量补丁,不用再手动补那 4 个变量） |
| 跑检查点 · 快层 | `dev.ps1 test` |
| 跑检查点 · 慢层 | `dev.ps1 test -Slow` |
| **交付前自检(必做)** | `dev.ps1 ready` —— 编译 + 全量检查点 + 文档引用检查 + **文档条数核对**(REGRESSION 声明的条数 vs 实际跑出的条数,防「人肉数字漂移」),打印固定格式结果;**交付时把这段完整贴给用户** |
| **检查节奏(何时跑哪层)** | 中途迭代默认只跑快层(**114 条 / 实测 0.5 秒** —— 2026-09-25 物理分层后;超 3 秒由检查点自带的墙钟闸门判红,不用人盯);慢层(**709 条 / 约 170 秒**——2026-09-25 物理分层后含 6 个「真做事」组,墙钟由 108 秒升至 170 秒)仅当改动命中 `REGRESSION.md` §二触发表(或正在迭代慢层覆盖板块)才中途加跑;**全量(编译+快+慢+文档)只留给 ready,一次交付只跑一遍**。机制不选层、不跳过——省时间靠少跑重复遍数,不靠砍闸门(2026-09-21 拍板) |
| 看当前现状 | `dev.ps1 status`(分支 / 改动 / 与 main 差距 / 待推送 / 文件数) |
| 开分支 | `dev.ps1 start <分支名>`(自动补类型前缀 + 开工查重) |
| 合并到 main | `dev.ps1 merge`(ff-only + 合并后自动校验索引异常) |
| 推双远程 | `dev.ps1 push` |
| 界面问题(看不清 / 被挤出 / 豆腐块) | `dev.ps1 snap`(全量 45 张);**只想看某几张**(如改了一个设置板块)→ 先 `dev.ps1 snap -List` 拿清单,再 `dev.ps1 snap -Only '03b,10e'`(**用编号;值必须加引号** —— 不加引号时 `03d` 会被 PowerShell 当 decimal 后缀吃成 `3`,静默匹配一堆无关场景;没匹配到会报错退出)。出图到 `%TEMP%\fc-ui-snapshot\<时间戳>` + 打印尺寸表;保留最近 5 次 |
| git 索引异常 | `dev.ps1 recover`(默认只诊断 + 备份;加 `-Apply` 才恢复) |
| 提交 | `dev.ps1 commit`(从 `.git\FC_COMMIT_MSG` 读提交信息) |
| 判断改动影响面、该跑哪层 | `REGRESSION.md` §一 枢纽清单 + §二 触发表 |
| 检查点纪律(八条硬规则) | `REGRESSION.md` §二 |
| 数据迁移 | `MIGRATION.md`(**顺序不可颠倒**;本机文件,不进远端) |
| 查有哪些分支、有没有未合并 | **当场用 `git` 查**(不再维护分支索引) |
| 找已放弃的分支(git 已查不到的) | `docs/ARCHIVE-BRANCHES.md` |
| 版本变更历史 | `git log`;`CHANGELOG.md` 仅在发版时整理 |
| 想知道脚本为什么这么设计 | `docs/dev-script-plan.md` |
| **开发记忆（当前状态/决策/坑）** | `.workbuddy/memory/`：**开工先读 `.workbuddy/memory/NOW.md`**；决策与坑查 `.workbuddy/memory/DECISIONS.md` / `.workbuddy/memory/PITFALLS.md`；维护规则见下「三、开发记忆」 |

退出码 `0` = 检查点全过，`1` = 有失败。检查点跑在临时沙箱，**不触碰真实数据**。

### 改这几块之前必读的禁区

| 板块 | 去哪读 |
|---|---|
| 网盘 / 文件仓库 | `REGRESSION.md` B-14 段「改这块之前必读」三条红线 |
| 悬浮球拖放保存 | `REGRESSION.md` B-15 段「改这块前必读」六条被推翻的结论 |
| Agent 工具 | `REGRESSION.md` B-16 段「改这块之前必读」三条红线 |

**永久搁置**：批注 / quote 功能。未来扫描自动跳过，除非用户主动提起。

**必须真实环境手动验证**：COM 互操作 / 全局热键 / 剪贴板相关改动 —— 自动化证明不了。

### 提交信息

遵循 Conventional Commits：`feat:` `fix:` `docs:` `refactor:` `chore:`（中文描述）。
多阶段功能用一个分支，阶段标在提交信息里（如 `feat(todo): phase2 面板徽标`）。

---

## 三、开发记忆（跨 Agent 共享，进 git）

> 位置 `.workbuddy/memory/`。**开工先读 `.workbuddy/memory/NOW.md`**。记忆要能答三问：进行到哪 / 为什么这么定 / 有什么坑。
> Claude Code 经根目录 `CLAUDE.md`（内容 `@AGENTS.md`）桥接到本文件。

| 文件 | 用途 | 维护规则 |
|---|---|---|
| `.workbuddy/memory/NOW.md` | 唯一「当前状态」（≤40 行） | **谁干活谁覆盖更新**；并行开发按分支分节；旧状态被覆盖即自动作废 |
| `.workbuddy/memory/DECISIONS.md` | 决策点卡片：当前答案 + 否决理由 | 更新只判「**有无新论据**」：有 → 旧答案压一行史；无（纯横跳）→ 直接覆盖。同点 30 天翻转 ≥2 次 → 停下找用户拍板，不记账 |
| `.workbuddy/memory/PITFALLS.md` | 确定性环境坑（只收推导不出来的） | 新坑追加；失效即删 |
| `YYYY-MM-DD`（`.workbuddy/memory/` 根部，按日期命名，形如 `.workbuddy/memory/2026-09-24.md`） | **事件账**（不是流水账；WorkBuddy 固定写这里，路径改不了） | **每条 1-3 行 + commit hash**，只记三类事件：**交付 / 决策 / 踩坑**。跨会话要留的「为什么这么定」进 DECISIONS.md、「环境坑」进 PITFALLS.md —— **日常日志不承担这两件事**；append-only，带 `[Agent名]` 标识，不改他人条目；超 30 天由每周日自动化蒸馏归档 |

**写入纪律**：daily 事件账可用命令行直写文件尾（PowerShell 追加必须显式 UTF-8，`>>` 产 UTF-16）；正式文件先读后改；红线 10（禁 Key / 敏感信息）同样适用于记忆文件。
**不入记忆判据**：能从代码 / 脚本 / git 推导的信息不入（**daily 事件账就是照这条设计的** —— 过程流水全都能从 `git log` 推出来）；「评估过并否决」的理由**只在** DECISIONS.md。
**冷启动测试（记忆的验收）**：新开一个无历史会话，只凭记忆文件应能答三问；答不上 = 记忆失效，当场修。
