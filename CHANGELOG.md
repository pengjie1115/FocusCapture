# Changelog

本项目所有重要变更都记录在此文件。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added
- 灵感速览标题栏可组装：八个功能按钮（时间筛选/上传/拉取/查找/刷新/AI 问答/导出/存）可由用户在设置里添加、移除、排序，最小化/最大化/关闭为固定铬区不占槽位；配置按像素预算准入（随面板宽度增减），手改配置文件自动清洗回退
- 新增设置板块「灵感速览」（v3.9）：面板宽度（480–1280，默认 620，边缘拖拽自动记回）、窗口置顶开关（默认开）、标题栏自定义编辑器（双列表 + 上移/下移/添加/移除/恢复默认 + 像素预算条）；唤出行为自「通用」板块迁入
- 灵感速览窗口壳升级：最小化（任务栏常驻图标，点击恢复）、最大化/还原（最大化不盖任务栏、圆角自动取消）、边缘自由缩放（宽度记忆落盘）；面板默认宽度 560 → 620（铬区需要空间）
- 灵感速览时间筛选弹层（替代旧热力图日历入口）：左列 7 项常驻——今天 / 昨天 / 近 7 天 / 近 30 天 / 本月 / 上月 / 自定义，点预设即选即关；自定义展开双月日历，两击选区间（同日两击 = 单日），悬停实时预览区间
- 日历日期圆点标注：绿色 = 当天笔记数（深浅三档），橙色 = 有未到期的待办（两者并存并排）；悬停 Tooltip 显示笔记篇数
- 新增设置项「灵感速览」（设置 → 通用）：唤出面板时显示当天的笔记（默认）或恢复上次的筛选（模式与自定义区间跨重启记忆）
- 开源准备：README、LICENSE (MIT)、CONTRIBUTING、Issue 模板、GitHub Actions 自动构建
- 自动化检查点体系：`tests/`（快层 21 条：加密 / 时间解析 / 灵感速览标题栏目录）+ `tests/sync/`（慢层 75 条：双向同步 / 桶拆分 / 删除传播 / 断网降级 / 游标保护 / 换码重传 / 行身份与未到期待办）。双击对应 `run-*.bat` 运行，退出码 0 = 全部通过；全程跑在临时沙箱，不触碰真实数据
- 笔记/待办重复行清理工具：`tools/todo-cleanup.ps1`（默认只报告，`-Apply` 才执行且先整份备份）+ 双击入口 `tools/run-todo-cleanup.bat`
- 全局热键新增两个：`Ctrl+Alt+A` 唤起 AI 问答、`Ctrl+Alt+T` 打开/收起待办汇总面板（均可在「设置 → 热键」里改）

### Changed
- 灵感速览标题栏按设置生成：八个功能按钮的容器与配置可由用户组装（功能目录 `QuickViewToolbarCatalog` + `RebuildToolbar`），按钮点击逻辑、时间筛选弹层、同步状态机制原样复用；老用户 settings.json 无新字段时默认布局与旧版一致，无感迁移
- 灵感速览唤出行为设置项从「设置 → 通用」迁入新板块「设置 → 灵感速览」（行为与存储字段不变）
- 灵感速览时间按钮直接显示当前筛选状态（预设名或日期区间，如「📅 近 7 天」「📅 09-06 ~ 09-12」），替代旧「单日： …」指示文案（查找 / 未到期筛选的指示保留）
- 导入预览对话框的「目标日期」选择改用单选即关的 MiniCalendarPicker（原复用 CalendarWindow）

### Removed
- 旧热力图日历弹窗 `CalendarWindow` 整体退役（灵感速览与导入预览两条链路均已迁走；日历热力底色由圆点标注替代）

### Fixed
- 灵感速览：「▼ 导出」按钮固定 68px 宽，勾选条目后文案变「▼ 导出已选 N 条」被截断 —— 现文字按钮宽度随内容自适应（时间筛选/AI 名称同理）
- 云同步：更换授权码后未用新密钥重写云端数据，会导致换码后（持新钥匙）反而读不到自己的数据 —— 现改为检测到换码即自动全量重传
- 云同步：解密失败时推送环节仍会推进游标，使未拉取的数据被增量过滤永久跳过（即便密钥随后对齐也捞不回）—— 现改为解密失败时回退游标
- **待办：点「已完成」后过一会又变回未办、每轮多留一份已办副本** —— 根因是「待办原地改行 → 行文本变化 → 行 ID 变化 → 云端旧版本行无人删除 → 每次拉取被当新行投递回来」。修复：原地改行后为被替换的旧行生成删除墓碑，随下次同步传播
- **待办：本机创建的「未到期待办」删不掉（提示「未在笔记文件中找到该条目」）** —— 根因是带提醒的待办按**提醒日**归类写入，而删除/编辑查找按**创建日**猜文件。修复：定位改为全库扫行（整行精确匹配优先，前缀匹配兜底）
- **云同步：一条记录在两台设备上被当成两条**（跨端删除不生效、副本反复落地）—— 根因是行 ID 含相对路径，而相对路径在系统内有三套口径（写入按提醒日 / 落地按创建日 / 查找按创建日）→ 同一行身份分裂。修复：**行 ID 改为只由行文本决定**，物理位置不再参与身份；配套加"内容级兜底"防升级期的重复落地
- 云同步：`UploadedAt`（上传时刻）为空的存量记录每次拉取都被无条件投递、永不衰减 —— 现于推送时一次性补齐上传时刻，保留"升级后补齐历史漏"的一次投递语义
- 灵感速览：按日期查看时，行的物理文件位置不再影响能否被看到（改为扫描全部 md 后按归类时间过滤，与日历计数、面板排序口径统一）
- 全局热键：键位被其他程序占用时注册失败完全静默，用户只表现为「按了没反应」—— 现写运行日志并在「设置 → 热键」板块显式提示换一组
- 全局热键：在设置里录制新热键期间旧热键仍然生效，按下组合键会当场触发对应功能（如录 Ctrl+Alt+V 就弹灵感速览）—— 现录制期间临时注销全部热键，录完 / 按 Esc 取消 / 中途关窗三个出口均恢复注册

## [0.1.0] - 2026-08-03

首个公开版本。以下变更按 commit 时间线整理。

### Added
- 灵感捕获器核心：桌面悬浮球 + 全局热键 + 托盘图标
- 剪贴板自动捕获：复制即存，400ms 防抖过滤瞬态写入，自动去重
- 沉浸式语音输入：纯 C# 本地语音识别（sherpa-onnx + FireRedASR2 CTC INT8），离线可用
- 灵感速览：最近 3 天笔记切换查看，支持长笔记折叠与回收站
- 一键导出：Markdown / JSON / TXT / Word（.docx 手写 OOXML，零额外依赖）
- 笔记来源窗口记录与标签

### Changed
- 语音识别从 Python 子进程（asr_server.py）迁移为纯 C# SDK，消除 Python 环境依赖
- 剪贴板捕获增加防抖与多段落完整捕捉
- 关闭 IL 裁剪，修复单文件 exe 启动时 TypeLoadException (RegisterDragDrop)

### Fixed
- 语音识别乱码问题
- 剪贴板瞬态写入（"选中即复制"类工具）导致的误捕获

[Unreleased]: https://github.com/pengjie1115/FocusCapture/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/pengjie1115/FocusCapture/releases/tag/v0.1.0
