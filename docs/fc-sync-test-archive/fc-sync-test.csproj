<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <!-- 测试程序不放主项目子目录（WPF **/*.cs glob 会吞 Program.cs → wpftmp CS0579），放仓库外 + ProjectReference（PROGRESS 踩坑速查 1） -->
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="D:\桌面文件存放位置260518\项目代码\专注力工具 - 上传git - 260726\FocusCapture\FocusCapture.csproj" />
  </ItemGroup>

</Project>
