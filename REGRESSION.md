# REGRESSION.md - FocusCapture 回归验收清单

> **用途**：改动后逐条验证，防止「改一处坏一片」「修了 A 的 bug 冒出 B 的 bug」。
> **配套**：项目根 `AGENTS.md`（红线 + 路由）——本文件是它的**项目级回归细则**。
> **用法**：先按规程判级 → **先跑自动化检查点（第二节）** → L2 过本文 A 级全量 / L1 过对应 B 级板块 / L0 只保构建通过。
> **构建命令**：`dotnet build`（Debug）
> **自动化检查点**：双击 `tests/run-tests.bat`（快层）/ `tests/sync/run-sync-tests.bat`（慢层），细则见第二节
> **枢纽数据日期**：2026-09-03（引用数会随代码变化，建议每季度重扫一次）

---

## 一、枢纽文件清单（判级依据）

统计口径：该类出现在多少个 `.cs` / `.xaml` 文件中（字符串匹配，含注释提及，属粗略指标）。

| 文件 | 被引用文件数 | 级别 |
|---|---|---|
| NoteService | 25 | **L2** |
| AppSettings | 20 | **L2** |
| NoteEntry | 17 | **L2** |
| Win32 | 9 | **L2** |
| SyncNote | 8 | **L2** |
| SyncSettings / SyncEngine / IChatProvider | 7 | **L2** |
| ImmersiveSessionService | 6 | L1 |
| TimeParser / LicenseGate | 5 | L1 |
| DeletedNote / TodoEditService / ThemeService / RecycleBinService / OpenAICompatibleProvider / NoteImportService / CryptoService / AppJsonContext | 4 | L1 |
| ChatModels / ChatAttachment / ChatAttachmentService | 3-5 | **L1** |
| AttachmentPreview（悬停预览浮层 + 大图查看窗，输入区卡片与气泡卡片共用） | 2 | L1 |
| WebDAVProvider / PromptBuilder / NoteExportService | 3 | L0 |
| 其余（VoiceService、QuickViewToolbarCatalog 等） | 1-2 | L0 |

### 人工修正（重要）

**引用数 ≠ 重要性。** 以下文件引用数虽低，但属单点故障，改坏即整块功能报废，**一律按 L2 处理**：

| 文件 | 为什么升 L2 |
|---|---|
| WebDAVProvider | 云同步的唯一实现，无替代路径 |
| CryptoService | 端到端加密，改错会导致数据不可读 |
| Dpapi | 本地密钥保护，改错会丢失已有凭据 |

**另有两条"低引用数但要按 L1 走人工清单"的理由**（2026-09-14 补）：

| 文件 | 为什么不能按 L0 放行 |
|---|---|
| ChatModels / ChatAttachment | 会话数据的持久化契约。字段改错的两个后果都**不容易当场发现**：①老会话 JSON 读不出来（用户几天后才点开历史会话）②附件引用写坏 → 附件变孤儿被清理掉 |
| ChatAttachmentService | 图片/文档上行的唯一实现。压缩档位跑偏 = 悄悄多花钱；落盘路径写错 = 附件全丢 |
| `Services/Files/` + `Services/Baidu/`（2026-09-16 新增） | 文件仓库与网盘客户端。看着像"新增功能、坏了大不了不用"，实际有两处**坏了不容易察觉**：① 淘汰器写错 = 悄悄删掉用户唯一的本地副本（此时云端还没传到）② 合并规则写错 = 他端记录被本机覆盖掉。**一律按 L1 走人工清单 + 慢层检查点** |

---

## 二、自动化检查点（机器验证，优先于人工清单）

> **铁律**：能自动验证的先跑，跑完再走人工清单。**交付前必跑，结果必须贴出来。**
> **位置**：`tests/`（快层）+ `tests/sync/`（慢层）—— 均随项目进 git，双击对应 `run-*.bat` 即跑。
> **跑法（2026-09-20 起，首选）**：一律走脚本 —— `tools\dev.ps1 test`（快层）/ `tools\dev.ps1 test -Slow`（慢层）/
> `tools\dev.ps1 ready`（**交付前总检**：编译 + 快层 + 慢层 + 文档引用检查，一次跑完并按固定格式打印结果）。
> 脚本内部就是调上面那两个 `run-*.bat`，所以直接双击它们依然可用。
> **退出码**：`0` = 全部通过；`1` = 有失败（机器可直接判定，无需人读输出）。
> 脚本自身另有一套退出码：`0` 成功 / `1` 任务失败（该改代码）/ `2` 脚本故障（该改脚本）—— 见项目根 `AGENTS.md`。

### 分层与触发条件

| 层 | 位置 | 条数 | 特征 | 什么时候必须跑 |
|---|---|---|---|---|
| **快层** | `tests/` | **98** | 纯逻辑，秒级 | **每次代码改动后** |
| **慢层** | `tests/sync/` | **416** | 需引用主项目（编译较慢）；**每组耗时直接输出** | **改动涉及 `Services/Sync/`、`Services/AI/`、`Services/NoteService.cs`、`Models/SyncNote.cs`、`Models/NoteEntry.cs`、`Services/TodoEditService.cs`、`Windows/AIDialogWindow*`、`Services/Skills/`、`Services/UiThread.cs`、`Windows/SkillAuthWindow*`、`Windows/SettingsWindow*`、`Models/AppSettings.cs`、`Services/Files/`、`Services/Baidu/`、`Services/DragDropSaveService.cs`、`Services/AppIconService.cs`、`Windows/FloatBall*`、`Windows/DropAction*`、`builtin-skills/`、`FocusCapture.csproj`、`App.xaml`、`App.xaml.cs`、`Services/DarkTitleBar.cs` 时**；交付前 |

当前覆盖：加密解密、时间解析、灵感速览标题栏目录（配置清洗/回退/像素预算，快层）、**剪贴板写入容错（占用重试 / 指数退避 / 绝不抛异常 / 空内容不写，快层 [5]，10 条）**；双向同步收敛、桶拆分、删除传播（含"他端可恢复"）、断网降级、游标保护、换授权码重传、密钥不一致提示、行身份与未到期待办（G 组）、**AI 附件（H 组：格式判定 / 文档抽文本 / 非 UTF-8 拒绝 / 压缩档位 / 会话只存引用不嵌 base64 / 会话往返保留混排偏移 / 旧会话兼容 / 孤儿清理）**、**网盘文件仓库（文件仓库组：句柄不可伪造 / 多设备合并规则 / 未上传不淘汰 / 数据目录校验，57 条）**、**Agent 工具（Agent 工具组：原地改行 / 回收站兜底 / 表格解析 / PDF 边界 / 时间上下文，42 条）**、**悬浮球拖放保存（拖放保存组：判定顺序 FileDrop 优先 / 只认 UnicodeText / 哈希名换微信图片名 / 标题截断 / 文本类判定 / 严格 UTF-8 / 卡片头部 / 判定零副作用，46 条）**、**应用图标（应用图标组：图标源容错 / 两处同源 / 恢复默认回落 / 角标完整落在窗口内 / 数字居中 / 角标圆心在球外且悬停放大后仍在球外 / 重叠深度≤5px / 投影余地够淡完，21 条）**、**UI 线程封送（自验：发起端确实不在 UI 线程 / 回调被搬到 UI 线程 / 取消如实传回 / 无调度器与已停用一律按未确认，6 条）**、
**Skill 外部依赖与授权（自带优先定位 / 静态扫描识别与不误判 / 状态解析七种输入 / 真跑状态查询 / 设备码流拿到 device_code 与验证链接 / 二维码真出 PNG 且落在指定目录 / 失败必须返回 null /
**依赖闸：未授权不得执行脚本、完成后同一次调用继续执行、不传依赖表则不预检 / 授权类失败的措辞与应用内入口 / 面向用户文案不得含开发者路径 / 候选目录顺序（自带 → 数据目录）与子进程 PATH 全前置 / NotConfigured 单列一态 / 验证链接提取 / 前置配置空转 / 无协议依赖的在位判定 / 注册式依赖表 / 兜底入口字段，54 条；⚠ 依赖闸三条自 2026-09-20 起用**桩依赖**（固定返回未授权），不再依赖真实 lark-cli 登录态 —— 当晚用户真机扫码授权后这三条曾全红，登录态是机器环境不是代码行为）**、
**标记行挂靠与编辑=替换（跨文件 ref 挂靠：标记行与原笔记被拆进不同文件也能合并 / 同文件顺序颠倒且多条编辑行时按时间戳取最新 / 孤儿标记行带 RawLine 且删除必成功进回收站 / 编辑=替换不追加【编辑】行且旧行进回收站发墓碑 / 替换后旧【编辑】痕迹清理且【AI 释义】保留，20 条）**、
**Skill 授权窗口（先准备再扫码 / 准备失败不出码 / 第一次用停在选择面板 / 失败写日志 R9 / 失败文案同步 R10 / 布局高度自校准两条，12 条，STA 线程）**、
**深色滚动条与标题栏（App.xaml 隐式 ScrollBar 样式在位 / A+B 配色 #3E3E3A+#5FA46A / PART_Track / 翻页按钮隐藏且禁用 / AI 问答窗口申请深色标题栏 / DWM 属性 19+20 / 标题栏 2px 品牌绿线 / 死样式 ScrollbarStyle 不许复活，8 条，2026-09-21）**、
**内置技能（随包分发的桥接 Skill：源里有 / 能落地到数据目录 / 落地产物能被扫描器认出且带脚本 / 能从脚本认出「要用 lark-cli」这条依赖 / 脚本不许硬编码别的应用安装目录 / csproj 复制规则存在且不带 Condition 且为 PreserveNewest / **落地动作必须排在 `--snapshot`·`--dragprobe` 之后**（源码顺序断言，理由见下），10 条）**、
**运行时下载（配方数据 + 打包契约：两条配方齐 / 部件名与依赖层常量一致 / 下载源全是 https 且不指向本机或别的应用目录 / **与 `tools\fetch-*.ps1` 的主机名不漂移**（漂移哨兵）/ Python 版本与脚本一致 / Python 配方必须「整包解压 + 删 `*._pth`」/ lark-cli 配方必须「从包里挑 `lark-cli.exe`」/ csproj 必须有 publish 侧清理目标，8 条）**（慢层）。
另有**快层 [10] 运行时按需下载 RuntimeDownloader**（20 条，用本地假下载源 + 自己这个 exe 当被测文件）：
多源兜底（首源失败自动试下一个）/ 全失败时失败信息带上是哪些源 / 进度真的被调且字节单调不减 /
体积下限（下到错误页不能当安装包）/ **自检没过绝不提交**（目标目录不许留半成品）/ 断流不抛 /
整包解压保留目录结构 + 解压后删 `*._pth` / 包里找不到目标文件要说清「包结构可能变了」/
**已装好的不重复下载**（一个请求都不再发）/ 压缩包里的 `../` 越界项必须拒绝 / 自检真的能起进程读输出。
另有**快层 [9] 内置 Skill 落地与恢复 BuiltinSkills**（16 条）：源缺失不抛 / 该落地的都落地（含 scripts 子目录）/
已有 SKILL.md 时一个字节不动 / 判据是"有没有 SKILL.md"而非"目录在不在" / 落地产物能被扫描器认出 /
恢复必须先备份且原子（备份不落在 Skills 目录内，清单里不许出现两行同名技能）/ 未落地时直接落地不产生空备份 /
技能名越界与"源里没有"必须拒绝 / 列内置技能只列真有 SKILL.md 的。

> **为什么文件仓库的检查点在慢层不在快层**（2026-09-16）：快层工程刻意不引用主项目（只链接少数无依赖源文件，以保持秒级编译）。
> 文件仓库必然依赖日志/设置/附件服务，链进快层会把这层轻量结构毁掉。代价是它的检查点不是「每次改动都跑」，
> 所以**动了 `Services/Files/` 或 `Services/Baidu/` 就必须跑慢层**，这一点写进上面的触发表里了。

> **R1/R2/R3 三条地基从 2026-09-21 起有专门防线**（快层 `[7]` 7 条 + `[8]` 6 条）。它们守的是两件「错了会静默」的事：
> ① 候选顺序（自带 → 数据目录）反了或只认一档 → 照样跑得起来，但跑的是旧的那一份 / 按需下载完仍说"没装"；
> ② 子进程「输出完就卡住」时，按「进程退出后才读输出」的写法**永远读不到已经出来的内容**，
>    而旧实现的超时分支还把已读内容丢成空字符串 —— 等于把唯一的证据也扔了。
> lark-cli `config init --new` 正是这种形态（吐完验证链接后阻塞到浏览器里操作完），R1 就是为它改的。

> **每组耗时直接打出来了**（2026-09-17）：慢层每组输出一行 `[耗时] 名称 | N 条 | X ms`，末尾一张按耗时降序的
> 汇总表（含「均 ms/条」与墙钟对照）。**汇总表放在 `finally` 里** —— 某组中途抛异常，已跑完的组耗时照样打得出来。
> 装它的理由很实在：此前只有「全跑一次多久」一个数字，30 多秒花在哪一组全靠猜，优化只能靠蒙。

> **条数与耗时无关，别按条数猜瓶颈**（2026-09-17 实测）：文件仓库 57 条只花 0.3 秒、拖放保存 46 条只花 70 毫秒，
> 而断网/重试 **8 条**独占 25.8 秒（77%）。**95% 的耗时集中在 3 个组**。推论：想「把条数多的组搬回快层」省时间，
> 收益可能为零 —— 先看耗时表再决定搬谁。

> **⚠️ `dev.ps1` 的退出码曾经是假的（2026-09-20 修）**：`tests\run-tests.bat` 与 `tests\sync\run-sync-tests.bat`
> 都以 `pause` 结尾且从不传 `ERRORLEVEL`，于是 `.bat` 的退出码 = `pause` 的退出码 = **恒为 0** ——
> 「退出码 0 = 检查点全过」这条契约（AGENTS.md 里写着）**根本兑现不了，门禁永远红不了**。
> 实测证据：故意把一处封送去掉让慢层崩掉，`.bat` 自己打印了 `[RESULT] SOME CHECKS FAILED - exit code -532462766`，
> 而 `dev.ps1` 仍然报"通过"。
> 已修两处：① 两个 `.bat` 末尾加 `exit /b %RESULT%`（pause 仍在前面，人双击照样看得完）；
> ② `dev.ps1` 的 `Invoke-Test` 改成**双重判据** —— 退出码为 0 **且**输出里有 `[RESULT] ALL CHECKS PASSED` 才算过。
> 加第二条的理由是本项目一贯判据：**能用产物证明的，就不信声明的退出码**。
> 改完复验：同一处故障下 `dev.ps1 test -Slow` 现在返回 **1** 并打印期望/实际对照。

> **断网组从 25.8 秒降到 5.5 秒**（2026-09-17）：两处根因都在「干等」而不是「算得慢」——
> ① `WebDAVProvider` 对 503/429 会硬等 5s+15s 再重试（C-7 段正用 503 触发它）→ 给该类加了**可注入退避参数**，
> **生产默认值一字未改**，测试传 `{0,0}`（重试次数不变，只是不等待）；
> ② 原断网模拟靠「连一个没人监听的端口」，本机实测这一跳要 2.2 秒（对比：连**有人监听**的端口只要 5 毫秒）→
> 除**保留一条真实断言**守住「底层失败必须被包装成渠道错误，不得裸抛、不得静默返回 null」外，
> 其余断网场景改用秒失败的 `FailingSyncProvider` 桩（它抛出的异常与真实转换**同型同码**，上层断言一条未改）。
> 改完慢层运行 33.7 秒 → 12.9 秒，检查点反而多了 1 条。

> **本机两个网络怪象（2026-09-17 实测，影响任何「连不可达地址」的检查点）**：
> ① loopback 连**未监听**端口要 **2.2 秒**才被拒（正常应 <1ms），局域网 IP 则要 **21 秒**（TCP SYN 重传超时）；
> ② 同一跳用 .NET `HttpClient` 拿到的是 **HTTP 502**，而 TCP 层直连该端口是「积极拒绝」、.NET Framework 的
> `WebRequest` 拿到的是 SocketException —— 成因指向系统层网络过滤（Defender 的 NisSrv/MsMpEng 在运行），
> 未定位到具体驱动。**已确认无 HTTP 代理**（环境变量全空、WinINET 关闭、`IsBypassed=True`、19999 无进程监听）。
> → 写这类检查点时，**别断言环境特有的状态码**，断言「必须被包装成可区分的渠道错误」这种跨环境都成立的性质。

### 八条硬规则

| # | 规则 | 防止什么 |
|---|---|---|
| **0** | 检查点一律跑在隔离沙箱（`FocusCapturePaths.RootOverride`），**绝不读写真实数据目录** | 测试弄脏用户数据 |
| **1** | 按上表触发条件跑；**交付前必跑** | 想跑就跑、不想跑就不跑 |
| **2** | 交付报告必须附**可见的测试输出**（跑几条 / 过几条） | 「我改好了」无从验证 |
| **3** | **新增功能未补检查点 = 交付未完成**。顺序：先出中文规格 → 用户确认 → 再写检查点 | 新功能进不了防线 |
| **4** | **新检查点必须先自验**：先在当前代码上跑，必须全绿；红了**不许动代码**，先问用户「期望 X / 实际 Y，哪个对」 | 标准写错会反向带偏正确的代码 |
| **5** | **改标准 / 删检查点必须用户确认**并留痕（写清原来 X、现在 Y、为什么） | 放水（绿灯保护 bug） |
| **6** | **红不许瞒**：用户不在线时宁可红灯挂着，不许 AI 自行判定「这算需求变更」 | 静默把红改绿 |
| **7** | 失败输出必须**中文人话**（期望什么 / 实际什么） | 用户看不懂堆栈，只能听 AI 转述 |

### 检查点来源优先级

1. **修过的每个 bug** —— 最值钱，真金白银踩出来的，直接防止同一个坑踩第二次
2. 本文件现有的 A / B 级条目 —— 已写清「看到什么算对」，可直接翻译成检查点
3. 新功能的行为规格 —— 须先经用户确认，不得由 AI 自定标准

### 已纳入守护的 bug（来源留痕）

| 日期 | 用户实测到的现象 | 守护检查点 |
|---|---|---|
| 2026-09-09 | 换授权码后云端密文未重写，持新钥匙反而读不到 | D 组（8 条） |
| 2026-09-11 | 解密失败时推送仍推进游标，未拉到的行被永久过滤 | 游标实验（4 条） |
| 2026-09-12 | 待办点「已完成」后自行变回未办、副本每轮 +1（旧版本行被反复投递） | G-3 / G-4（不重复落地） |
| 2026-09-12 | 本机创建的未到期待办删不掉（报「未在笔记文件中找到该条目」） | **G-1 / G-2** |
| 2026-09-12 | 一端删除不生效（B 删了 A 上仍在） | **G-5** |
| 2026-09-13 | 标题栏图标渲染成空框（同步按钮的状态文案把图标字符覆盖成了非图标字体字符） | 快照 11（人工判读） |
| 2026-09-13 | 设置页「移除」按钮被挤出可视区，用户完全找不到入口 | 快照 02（人工判读） |
| 2026-09-13 | 设置页功能列表文字对比度过低（未选中项几乎融进背景） | 快照 02（人工判读）+ 像素亮度比对 |
| 2026-09-13 | 添加功能下拉框为空时毫无提示、按钮空点无反应 | 快照 13（人工判读） |
| 2026-09-14 | 打开 AI 问答后打字没反应，必须先用鼠标点一下输入框（窗口从未显式聚焦输入框） | 快照 10（人工判读）+ 代码定位：`AIDialogHelper.Open` 只调了窗口级 Focus |
| 2026-09-14 | 图文混排时卡片浮高、同排文字沉底（`InlineUIContainer` 默认按基线对齐） | B-13「卡片与同排文字中线对齐」+ 快照 14 |
| 2026-09-14 | 输入框光标与深色背景融成一片，几乎看不见（没设 `CaretBrush`，默认取系统色） | B-13「光标浅色可见」 |
| 2026-09-14 | 打开会话时输入框只有窄窄一条 | B-13「高度约两行」+ 快照 10 |
| 2026-09-14 | 加号图标看起来没居中（字体里的加号字形在行框内本身不居中） | B-13「加号居中」+ 快照 10 |
| 2026-09-19 | 悬浮球角标圆被切掉一块、数字偏上（角标写的是负边距，被顶出 48×48 窗口，而窗口外的部分根本不渲染） | 慢层「应用图标组」2 条 + 快照 25 + B-6 |
| 2026-09-19 | 换了自定义图标后**托盘变了、任务栏窗口图标没变**（窗口图标此前一处都没设过，只走 exe 编译期图标） | 慢层「应用图标组」6 条 + B-6 |
| 2026-09-19 | 悬浮球角标**压在球上的面积太大**（角标圆心正好落在球边上＝压掉一半；且悬停放大时球半径 20→22，圆心距 20.5px 会直接陷进球体） | 慢层「应用图标组」3 条（圆心距 / 放大后 / 重叠≤5px）+ 快照 25 + B-6 |
| 2026-09-19 | 悬浮球**投影在下方与右侧被窗口边界削出一道硬边**，浅色桌面上明显（48 窗口里球四周只有 4px，投影要 6px 才淡得完；实测走到边界仍有 18/255＝7% 强度） | 慢层「应用图标组」2 条（投影参数静态判据）+ 快照 25 像素级量 alpha + B-6 |
| 2026-09-14 | 悬停附件卡片要等一两秒才出预览（用了 WPF ToolTip，含系统级延迟与 Popup 首次初始化） | B-13「预览立刻弹出」+ 代码定位：`AttachmentPreviewHost` 自绘浮层 + 预热 |
| 2026-09-14 | 文件拖到输入框正上方放不进去、也不出复制光标（RichTextBox 内置文本拖放处理先吞掉了拖拽事件） | B-13「拖到输入框正上方」+ 代码定位：`InitInputArea` 以 handledEventsToo 接管 |
| 2026-09-14 | 含附件卡片的选区按 Ctrl+C 复制不出来（卡片是 UIElement，默认复制走富文本格式，跨应用拿不到） | B-13「选中含卡片的内容 Ctrl+C」 |

> 维护约定：**每修完一个 bug，必须在此表补一行并加对应检查点**（写不出检查点的，在交付报告里说明为什么写不出）。
>
> 2026-09-13 说明：上表后四行属于界面层，按既有决议不做自动化断言（界面测试脆、误报多）。
> 其守护方式是 **`--snapshot` 界面快照 + 人工判读**（见下节盲区段落的补充），
> 这几处都是「一眼能看出不对」的类型，不需要机器替人做判断。

### 维护约定

| 时机 | 动作 |
|---|---|
| 新增检查点 | 先在当前代码跑通全绿才准入（规则 4）；明显变慢的单独标记出来 |
| 每个版本 | 扫一眼「有没有不再需要的检查点」—— **技术债来自没人清理，不来自条数多** |
| 定期 | 把检查点清单**用中文念给用户听**（用户是标准的最终仲裁者，不需要懂代码） |

### 当前盲区（**没有**自动化守护的部分）

界面与交互、提醒到点、语音输入、AI 对话、Agent 工具、导入导出、托盘 / 热键、主题 —— **这些仍靠人工清单兜底**。

> 不是都该自动化：界面层测试脆且误报多，性价比为负（既有定论）。筛选标准是「**坏了之后能否被发现 / 能否挽回**」—— 加密、同步删除这类「坏了不易察觉」的已优先覆盖；界面这类「一眼就能看见」的不需要机器帮忙。

> **2026-09-13 补充：界面盲区新增一条「半自动」通道 —— `FocusCapture.exe --snapshot`。**
> 它把窗口真实渲染成 PNG（用程序自身的样式资源，不是另起炉灶），供人眼或模型逐像素判读。
> 定位是**降低发现成本，不是替代判断**：仍然不做断言、不进 CI、不判红绿，只负责"把图摊开"。
> 适用于三类静态代码看不出来的问题：控件被挤出可视区、图标字形缺失（豆腐块）、文字与背景对比度过低。
> 用法与已注册的窗口清单见 `Diagnostics/README.md`。**加新窗口/新验收场景时，同步往那里补一行。**


---

## 三、A 级：枢纽必测（动了 L2 文件时全过）

图例：✓ = 已从代码核实 · ⚠ = 按命名推断，**待用户校正**

| # | 验收动作 | 看到什么算对 | 看到什么算错 |
|---|---|---|---|
| A1 | 按 Alt+Space ✓ | 输入框弹出，光标在输入框内 | 没反应 / 弹出后卡死 |
| A2 | 输入「测试笔记」按 Ctrl+S 保存 ✓ | 输入框关闭或清空，无报错 | 报错弹窗 / 内容没存上 |
| A3 | Ctrl+Alt+V 打开灵感速览 ✓ | 窗口打开，刚存的笔记在列表里 | 打不开 / 列表空白 / 找不到刚存的 |
| A4 | Ctrl+T 切到待办，输入「买菜 明天10点」✓ | 待办面板出现该条，时间显示明天 10:00 | 时间识别错（如变今天）/ 不出现 |
| A5 | 建一条 1 分钟后到点的提醒，等待 ✓ | 到点弹窗，误差不超过 1-2 秒 | 不弹 / 提前几秒弹 / 晚半分钟以上 |
| A6 | 弹窗里点「稍后」✓ | 按设置里配的稍后时长重弹（默认 10 分钟） | 立刻又弹 / 再也不弹 / 时长不符 |
| A7 | 看托盘角标数字 ✓ | = 今天及以前的未办待办总数 | 数字对不上 / 不刷新 |
| A8 | 把每日汇总时间设成 1 分钟后，等待 ✓ | 到点弹出每日汇总 | 不弹 / 弹两次 |
| A9 | Ctrl+Alt+S 打开设置，左侧导航逐个点开 ✓ | 每块都能打开，无报错 | 某块点开报异常（**最易踩坑**） |
| A10 | 改「输入框不透明度」滑块 ✓ | 输入框立刻变透明，无需重启 | 改了没反应 / 要重启才生效 |
| A11 | 改完设置 → 完全退出 → 重新打开 ✓ | 刚才的设置在 | 设置丢失，回到默认值 |
| A12 | 打开数据文件夹 ✓ | 刚记的笔记在 .md 文件里 | 找不到文件 / 内容不是最新的 |
| A13 | 设置里改一个热键，用新热键试 ⚠ | 新热键生效，旧热键失效 | 两个都生效 / 都不生效 |
| A14 | 数据文件夹有内容时点云同步 ⚠ | 提示同步成功，远端文件更新时间变化 | 报错 / 卡住 / 静默失败 |
| A15 | 删一条笔记 → 打开回收站 → 恢复 ⚠ | 笔记回到原位，内容完整 | 找不到 / 恢复后内容丢失 |
| A16 | 按 Ctrl+Alt+A ✓ | AI 问答窗口打开，可正常提问 | 没反应 / 报错 / 未配 Key 时静默无提示 |
| A17 | 按 Ctrl+Alt+T 两次 ✓ | 第一次弹出待办汇总面板，第二次收起 | 只能弹出不能收 / 连按开出多个窗口 |

> A12 / A14 的数据文件夹，默认在「我的文档\FocusCapture」，**以设置里配置的存储路径为准**。

---

## 四、B 级：板块专项（动哪块测哪块）

### B-1 笔记与导入导出（NoteService / NoteImportService / NoteExportService）

| 验收动作 | 期望现象 |
|---|---|
| 导出 Markdown ⚠ | 文件生成，内容包含导出配置里勾选的项（时间 / 来源 / 标签 / 正文） |
| 导入一个 md 文件 ⚠ | 预览窗口显示内容条数，确认后进入列表 |
| 长文模式写一段并保存 ⚠ | 重新打开内容完整，段落不乱序 |

### B-2 待办与提醒细节（ReminderService / TimeParser / TodoEditService）

| 验收动作 | 期望现象 |
|---|---|
| 输入「30号开会」（纯日期不带时间）✓ | 弹窗询问具体时间；若设置里关掉了「纯日期询问时间」，则直接定为当天 09:00 |
| 弹窗不操作，一直等 ✓ | 按设置里配的自动关闭秒数自动关闭（默认 10 秒） |
| 修改一条待办的时间 ⚠ | 保存后列表和角标同步更新 |
| 创建一条**提醒日在未来**的待办（如今天建「9/15 砍价」），再在灵感速览里删除它（2026-09-12 修复） | 删除成功、条目消失；**不再**弹「删除失败：未在笔记文件中找到该条目」（旧版按创建日猜文件 → 找不到） |
| 待办点「已完成」，等一次同步（≥30 秒）后刷新面板（2026-09-12 修复） | 保持「已办」，不变回未办；面板里不出现同一待办的多份副本（旧版旧版本行被反复投递回本地） |

### B-3 云同步与回收站（Services/Sync/ 下任意文件）

| 验收动作 | 期望现象 |
|---|---|
| 一条记录连续改两次后同步 ⚠ | 远端是最新内容，不产生重复文件 |
| 回收站彻底删除 ⚠ | 确认后消失，同步后远端也删掉 |
| 断网时点同步 ⚠ | 明确的失败提示，不是转圈卡死 |
| 勾选「同步 AI 问答记录」点保存并连接 ⚠ | 状态正常变为「连接成功，已同步…」，不卡『正在连接并同步…』；取消勾选再连一次，行为一致 |
| 勾选 AI 问答同步后点「立即同步」，随后再点一次 ⚠ | 两次都能正常完成（第一次死锁修复后闸能归还，后续同步不被堵死） |
| A 设备下午才同步上午写的笔记，B 设备随后同步 ⚠ | B 能拉到这批笔记（按上传时刻增量，不看笔记原始时间戳）；升级后首轮 B 可能多拉历史漏项，属预期补齐 |
| 升级新版后首轮同步，回收站记录数 ⚠ | 不出现同一行重复多条的回收站记录（存量墓碑重放走幂等落站） |
| 云端盐为空时跑一轮同步 ⚠ | 同步完成后云端 sync_meta.json 的盐非空（本机盐自动补传）；他端下次拉取自动刷新密钥对齐 |
| 两端密钥不一致时同步 ⚠ | 状态明确显示「成功（N 条云端数据解密失败…）」，不再单纯显示「成功」误导；且游标不推进，密钥对齐后下轮能补拉（**2026-09-11 修复「推送把游标推走」后此条才真正成立**，已由慢层检查点守护） |
| 更换云同步授权码后同步 ⚠ | 自动全量重传（云端密文改用新钥匙加密），状态显示「已全量重传」；他端若仍配旧码 → 明确提示「解密失败」而非静默 |
| AI 问答同步第二轮及以后 ⚠ | 不再报「not a valid Base-64 string」；分组/删除清单正常上传下载（兼容云端存量带引号文件） |
| 会话连续两轮同步，第二轮期间不动任何数据（2026-09-09 增量对账）⚠ | 第二轮明显变快且坚果云无 503（修改标记未变的会话文件跳过下载）；有本地未推送修改的会话仍正常上传 |
| 会话同步中途触发坚果云限流 ⚠ | 状态显示「部分成功：…下轮自动继续」而非整轮失败；已下载/已上传的会话真实落地，不因中断作废 |
| 退出应用（自动同步+AI 问答同步都开着）⚠ | 不再必然弹「同步超时（4 秒）」——退出收尾只等笔记部分；会话未推送内容下次启动自动补传 |
| 云同步拉到新笔记时灵感速览面板开着 ⚠ | 面板自动出现新笔记，无需关闭重开（角标同步刷新）；拉取的行不会反向被推回云端 |
| 状态显示「成功」但本地没收到数据 → 点「从云端重新拉取」⚠ | 确认弹窗后云端全部笔记重新对一遍：缺失的补上、已有的不重复、本机数据不上传；完成后状态显示「重新拉取完成」 |
| 他端重置密钥后本端再同步 ⚠ | 自动刷新解锁后同步进度清空并全量重对（旧密钥时代的游标作废），不漏拉 |
| 一条**提醒日在未来**的待办在本机正常，在另一台电脑上删除它（2026-09-12 修复）⚠ | 本机同步一次后该条也消失（行身份只由行内容决定，与它躺在哪个文件无关——旧版会分裂成两个身份，删了不生效） |
| 反复同步多轮（≥3 次）后查看 md 文件（2026-09-12 修复）⚠ | 同一条待办/笔记只有一份，不随同步轮次增加副本 |

### B-4 AI 功能（Services/AI/ 下任意文件）

| 验收动作 | 期望现象 |
|---|---|
| 选中一段文字调 AI 解释 ⚠ | 返回结果，不报「未配置 API」 |
| 打开 AI 对话窗口发一条 ⚠ | 正常回复（模型以设置里配置的 AI 模型为准） |

### B-13 AI 附件输入与展示（ChatAttachmentService / ChatAttachment / ChatModels / AIDialogWindow 输入区与气泡，2026-09-14 新增）

> 范围：图片与文档（PDF 本期不做）可粘贴 / 加号选 / 拖入，文字与附件混排。
> 附件**只存本机**、不进云同步；会话 JSON 只存引用（H8 检查点守护）。
> 开关位置：设置 → AI 模型 →「图片清晰度」+「允许发送图片」。

| 验收动作 | 期望现象 |
|---|---|
| Ctrl+Alt+A 打开 AI 问答 | **焦点直接落在输入框，立刻能打字**（改造前必须先用鼠标点一下） |
| 刚打开会话时看输入框 | 高度约两行，**不是窄窄一条**；空着时显示灰色引导语（输入后自动消失） |
| 输入框里有光标时看光标 | 浅色、清晰可见（改造前与深色背景融成一片） |
| 输入文字 → 插图 → 再输入文字 ⚠ | 卡片与**同一排**文字的**中线对齐**（改造前卡片浮高、文字沉底） |
| 看输入框左侧的 + 号 | **居中**（矢量绘制，不再用字体里的加号字形） |
| 截图后 Ctrl+V | 输入框内出现「图片 截图-….png」卡片 |
| 把鼠标移到卡片上 ⚠ | 预览**立刻弹出**（约 0.15 秒；改造前用系统 ToolTip，实测要一两秒） |
| 双击输入框里的图片卡片 | 弹出全屏大图，滚轮可缩放，单击或 Esc 关闭 |
| 双击输入框里的文档卡片 | 用系统默认程序打开该文件 |
| 从资源管理器把文件拖到**输入框正上方** ⚠ | 光标变成复制样式（带加号），松手即加入（改造前只有拖到输入框上下边缘才生效） |
| 资源管理器里复制文件后 Ctrl+V | 出现「文件 xxx」卡片 |
| 点输入框左侧 + 号选一个图片/文档 | 同上，卡片出现 |
| 先打字 → 插图 → 再打字 → 回车发送 ⚠ | 气泡里显示正文 + **同款附件卡片**（不再把图片摊成缩略图）；上行请求按原顺序带图（混排顺序由 InsertOffset 还原） |
| 鼠标移到气泡里的卡片上 / 双击 | 与输入框里完全一致：悬停出预览、双击放大或打开 |
| 在输入框里选中含卡片的内容按 Ctrl+C ⚠ | 复制成功（拿到纯文本），**不得"复制不出来"** |
| 输入框里按 Shift+回车 ⚠ | 换行，**不发送** |
| 输入内容超过约 6 行 ⚠ | 输入区内部出现滚动条，窗口高度不再增长 |
| 附件卡片上点 × | 该附件从输入框移除 |
| 粘贴 Word / 网页里复制的**文字** ⚠ | 粘进来的是文字，**不得被误判成图片**（剪贴板同时含位图时以文字优先） |
| 选一个 .pdf | 明确提示「PDF 暂不支持（本次开发未包含）」 |
| 选一个 .zip / .exe | 明确提示「不支持的格式」 |
| 选一个 GBK（ANSI）编码的 txt | 明确提示「文件不是 UTF-8 编码，暂不支持」 |
| 设置里关闭「允许发送图片」后粘贴图片 | 提示已关闭，图片不会被加入；设置里打开后恢复正常 |
| 「图片清晰度」在省流/标准/高清间切换后发同一张大图 ⚠ | 上行体积与清晰度相应变化（省流 = 长边 768） |
| 发完图 → 关窗口再打开该历史会话 | 卡片仍在气泡里显示 |
| 重启应用后打开历史会话 | 卡片仍在（附件存本机，不随同步走） |
| 另一台设备同步后打开同一会话 ⚠ | 附件卡片转为降级配色（灰边），双击提示「这个附件只存在原设备上」，**不裂图、不报错、不卡** |
| 用不支持图片的模型发图 ⚠ | 报错信息里含人话提示（换支持视觉的模型，或关闭图片开关），不是裸英文 400 |

### B-14 网盘文件存取（百度网盘本地缓存，2026-09-16 新增）

> 覆盖 `Services/Baidu/`（设备码授权 + 上传/取回/列举/删除）、`Services/Files/`（元数据、缓存账本、上传队列、淘汰器、附件到期清理、文件句柄）、`Services/AI/ChatAttachmentService`（附件登记钩子）、`Windows/SettingsWindow`（「文件与网盘」板块）、`Windows/AIDialogWindow`（「选择文件」入口 + 句柄卡片 + 云文件卡片）、`MainWindow`（仓库装配）、`Services/Sync/FileStoreSync`（文件清单同步）。
>
> **前置**：需要百度网盘开放平台的「个人使用」应用凭据（AppKey / SecretKey），由用户在设置面板自行填写。
> **没有凭据时，整块功能自动降级为「只存本地」，不影响任何既有功能。**
>
> **三条红线（改这块之前必读）**：
> ① **工具的路径参数绝不能让 AI 生成** —— 走「用户选择 → 产生句柄 → AI 只能引用」。句柄形如 `file:20260916-1`，只在本机生成、24 小时有效，凭空编造的牌号一律解析失败。这是把「AI 的可达范围」锁在用户亲手点过的文件上。
> ② **SecretKey 不进聊天 / 日志 / 文档** —— 只由用户在设置面板填，落盘走 DPAPI 独立文件（不在 settings.json 里，避免跟着同步跑到别的设备）；日志里最多出现 AppKey 前 4 位。
> ③ **沙箱硬约束** —— 所有网盘路径必须落在 `/apps` 之下。这是平台限制不是配置项，客户端层直接拒绝越界路径（既防手滑，也防将来谁把 AI 给的路径直接传进来）。
> **但守卫的粒度必须与操作语义对齐**：只读的列目录要放行 `/apps` 本身，只有写操作的目标路径才强制带下级。早期版本一律要求 `/apps/` 前缀，把「列 /apps」也拒了 —— 而建目录必然要列一次 `/apps`，整条上传链路当场死掉（见下方事故记录）。

| 验收动作 | 期望现象 |
|---|---|
| 未填凭据打开设置 →「文件与网盘」 | 板块正常显示，状态为「尚未配置应用凭据」；其余功能完全不受影响 |
| 填 AppKey + SecretKey → 点「开始授权」 | 显示授权码并尝试自动打开浏览器；扫码或输码后状态变「授权成功」；**SecretKey 输入框保存后自动清空** |
| 授权成功后再打开设置 | 回显 AppKey；SecretKey 输入框为空（密钥不回显） |
| 点「测试连接」 | **必须真的通**：显示「连接成功：已能读写 /apps/xxx」；失败时给可执行原因（授权无效 / 应用名对不上）。这是全链路的总闸，它不通后面全是空的 |
| 「测试连接」通过后看设置页底部 | 若此前有卡住的失败文件，自动放回队列重传并提示「已把 N 个失败文件放回上传队列」 |
| 点「取消授权」 | 令牌清除、状态回到「尚未授权」；网盘上已有文件不受影响，应用凭据保留 |
| 「网盘目录」填不合法值（如 `/other`）后失焦 | 自动回退默认 `/apps/FocusCapture`，非法值不落盘 |
| **AI 对话里点「选择文件」选一个文件** ⚠ | 输入区上方出现金黄描边的「本机文件」卡片（含牌号）；双击卡片用默认程序打开；点 × 可移除 |
| **对 AI 说「把 C:\Windows\win.ini 存到网盘」** ⚠ | AI 明确说明它没有路径参数、无法指定本机路径；**不得真的上传任何东西** |
| 选中文件后说「把这个文件存到网盘」 ⚠ | AI 引用牌号 → 确认执行 → 文件进入本机文件区并排队上传 |
| **存完立刻去网盘看** ⚠ | AI **不得**说「已上传成功 / 已保存到网盘」；只能说「已存入本机、正在后台上传，网盘上要等传完才出现」。传完后在网盘 `/我的应用数据/{应用名}/files/` 里**真的能看到该文件**——这是唯一可信的通过标准 |
| **重启后重传此前失败的文件**（2026-09-16 第二次事故的终态验收）⚠ | 设置页点「测试连接」→ 失败文件自动回队 → 网盘 `files` 里**真的出现该文件**；日志里**不再出现** `requires an element of type 'String'` 这类英文异常（中文人话的失败原因才是正常的） |
| **上传前后查「网盘里有哪些文件」** ⚠ | 还没传完的文件，AI **必须**照实说「还没传上去 / 上传失败」，**绝不许**说「已上传成功」「网盘上都能看到了」——工具输出里逐行标着真实状态，模型没有借口 |
| **多次重启应用后看网盘根目录** ⚠ | `/我的应用数据/` 下**不再**冒出 `{应用名}_20260916_112442` 这种"名字_时间戳"的空目录（每重启一次多一个是 2026-09-16 的实测现象）；若真出现，日志里会有「建目录被服务端改名」的 WARN 留痕 |
| **上传一个全新的文件**（当前唯一可信的终态）⚠ | ① 网盘 `files/` 里**真的出现该文件**；② 日志里能看到取到的分片上传域名（形如 `https://c3.pcs.baidu.com`）且**没有 403**；③ 设置页底部不再显示「尚有文件未上传完成」 |
| 说「把这份内容存成 周报.md 到网盘」 ⚠ | 用 content + file_name 参数保存（全程不需要文件路径） |
| 对话里已附一张图，说「把这张图存到网盘」 ⚠ | 按附件名引用当前对话附件并保存成功 |
| 说「网盘里有哪些文件」/「找我上周存的文档」 ⚠ | 列出本地元数据命中的文件（编号 / 类型 / 名称 / 时间 / 大小 / 是否在本机）；**纯离线，不联网、不耗额度** |
| 说「把那份文档取回来」 ⚠ | 按需下载 → 对话里出现云文件卡片，「打开 / 定位 / 另存」三个动作都可用 |
| 说「读一下网盘里那份报告，总结一下」 ⚠ | 文本类（md/txt/代码/csv/json）能读；图片、Word、PDF 明确说明读不了并建议取回，**不得假装读到了内容** |
| 取消授权后再让 AI 存文件 | 明确提示未授权；文件仍留在本地文件区，授权后自动补传 |
| 同一份内容用两个不同文件名上传 ⚠ | 网盘里只保留一条（内容哈希去重）；第二条只登记本地副本、不发上传；**AI 要说明「内容与已有文件相同，已合并」**，而不是拿旧文件名糊弄用户 |
| 取回一个文件后看待上传队列 ⚠ | 取回的文件**不在**待上传列表（来源标记防线：取回永不触发上传） |
| 把取回的文件复制一份再丢回文件区 ⚠ | 不产生重复上传（MD5 查重防线兜住） |
| 设置 →「立即清理」 | 释放已上传且符合条件（超期/超容）的本地副本；提示里说明有几个「尚未上传完成、不会被清理」 |
| 容量上限设为 0.5G 后放入超过上限的文件 ⚠ | 自动释放最久未用的副本；手动「立即清理」效果一致 |
| 关掉「自动清理」开关 | 不再自动释放；手动「立即清理」仍可用 |
| 点「打开文件区」 | 资源管理器打开数据根下的 `files` 目录 |
| 开启「对话附件上传」后贴一张图 ⚠ | 附件登记并排队上传；关掉开关后贴图**不上传**（与改造前行为完全一致） |
| 「附件云端保留」改小到 1 天后等一轮到期清理 ⚠ | 超期附件云端被清、记录标「云端已到期清理」、本地副本贴「优先淘汰」；**AI 产出与主动上传的文件不受影响** |
| 到期清理后看设置页底部（2026-09-16 重构）⚠ | 云端**删成功** → 记录标「云端已到期清理」；**删失败 / 没联网** → 标「云端待清理」并在设置页显示计数。**两种都不打墓碑**（记录留着，用户看得见文件去哪了）；本地副本一律照常释放 |
| 到期清理失败后连跑两轮 ⚠ | 第二轮**不再发删除请求**（退避生效）—— 旧版是每 30 分钟整批撞平台 |
| 点「重试云端清理」 | 清空退避并把「待清理」的记录再删一轮，如实汇报「成功 N / 仍剩 M」；全清完时给「没有需要清理的云端文件」 |
| 点「复制网盘路径」 | 剪贴板拿到 `…/attachments`，提示按月份子目录整目录清理 |
| 新贴一张图（已开启附件上传）⚠ | 云端落在 `attachments/{yyyy-MM}/`（**按月分子目录**），不再平铺 |
| 云文件卡片 | 四个动作齐全：打开 / 定位 / 另存 / **彻底删除**，一行不被裁剪（快照 18） |
| 点卡片「彻底删除」 ⚠ | 二次确认 → 本机与云端一起删；**云端删失败时提示必须写明「云端那份还在」并给出补删入口**，不许只说"已删除" |
| 对 AI 说「把网盘里那个文件删了」⚠ | AI **没有**删除工具，不得执行；只能引导用户点卡片上的按钮（破坏性动作只给用户 —— 项目红线） |
| 附件到期清理后再看 AI 产出文件 ⚠ | 仍在云盘（分级策略生效——一刀切会毁掉「全量权威仓库」的根基） |
| 另一台设备登录（已配云同步） ⚠ | 文件清单同步过来，列表立刻可见，**一个字节都不下载**；元数据未拉完时显示「同步中」，不显示「文件不存在」 |
| 云同步页关掉「同步网盘文件清单」 ⚠ | 清单不再上传/下载；笔记与会话同步照常 |
| 设置里看「当前数据目录」+ 点「打开当前目录」 | 显示实际数据根；能打开 |
| **改数据目录到一个全新空文件夹** ⚠ | 先弹确认（含文件数与大小）→ 复制 → 校验 → 写指针 → 提示重启；**原目录一个字节都没改** |
| 改数据目录时选盘符根 / `C:\Windows` / 网络路径 / 当前目录的子目录 ⚠ | 逐项被拒绝并给出人话原因，且不写入任何文件 |
| 改数据目录到已有内容的文件夹 ⚠ | 明确提示「将直接使用该目录现有内容，不再复制」，需二次确认 |
| 迁移后重启程序 | 新目录生效，笔记/待办/对话/附件/文件区都在 |
| 迁移完成后看原目录 | 内容原样保留（旧目录永不自动删除） |
| 自定义数据目录所在磁盘不可用时启动 ⚠ | 明确弹窗提示「数据目录不可用」；**绝不静默改用默认目录**（静默回退会造出两套数据，比报错难收拾一个量级） |

> **测速前提（写在这里免得下次又踩）**：真实下载速度取决于用户网盘账号等级，**不是代码能决定的**。
> 本地已实测：非会员走 API 单连接稳定 **0.08 MB/s**（≈80 KB/s），上传几乎不限速。
> 因此本板块的实际定位是「**上传归档为主、取回为辅**」——这一点在验收前就该知道，不要拿取回速度当功能缺陷。

> **2026-09-16 实机事故（本板块最该读的一段）**：首次实机验收，上传 100% 失败、网盘永远是空的。
> 排查结论：**一个 bug 瘫痪全链路，而它不在上传逻辑里**。
> `EnsureDirectoryAsync` 判断目录是否存在时先列父目录；建 `/apps/FocusCapture/files` 必然要列一次 `/apps`；
> 而沙箱守卫要求路径必须以 `/apps/` 开头 → 列 `/apps` 被自己的守卫拒绝 → 上传整条失败。
> 连锁症状（同一个根因）：「测试连接」报「拒绝访问沙箱以外的路径（/apps）」；每个文件 **78 毫秒内失败 5 次**；网盘上连 `files` 目录都没建出来。
> 三条教训：
> ① **守卫的检查粒度要跟操作语义对齐** —— 拿一把尺子量所有请求，就会把自己的合法请求也杀掉；
> ② **失败必须退避** —— 不退避时「重试 5 次」在一瞬间烧光，重试上限形同虚设，用户也来不及去改配置；
> ③ **工具返回值不许把「登记成功」说成「已保存到网盘」** —— 上传是后台异步的，说错了用户一去网盘核对就穿帮，整块功能的信任当场归零。
> 现在慢层有 14 条检查点专门守住这些边界（沙箱守卫 9 条 + 重试出口 3 条 + 目录配置 2 条）。
>
> **2026-09-16 第二次实机事故（同一天、换了一层）**：守卫修好后 `files` 目录能建出来了，
> **但文件仍然 100% 失败**。排查结论：`UploadFileAsync` 把 precreate **响应**里的 `block_list`
> 当成 md5 字符串数组读（`GetString()`），而它是「服务端已收下的分片**序号**」数字数组
> → 每个文件都在那行抛 `InvalidOperationException`
> （`requires an element of type 'String', but the target element has type 'Number'`），一个字节都传不出去。
> **报错文本里没有"百度"二字**，它长得不像网络错误 —— 只能靠翻日志 + 读代码才查得到。
> 三条教训：
> ① **同名字段语义要分别确认** —— 请求参数里的 `block_list` 是「本地算的每片 md5 列表」，
>    响应字段里的同名 `block_list` 是「服务端已收下的分片序号」，两者只是碰巧同名；
> ② **解析外部数据必须做到「读不懂也不抛」** —— 现在的契约是：任何异常情况一律返回空集合
>    （= 一片都不跳过 = 全量重传）。失败方向必须是「多传几片」，绝不能是「传不上去」；
> ③ **别拿「A 通了」证明「B 也通了」** —— 文件夹走 `create&isdir=1` 一次调用，文件走
>    precreate→分片→create 三步，是两条不同的接口路径（当时以为"文件夹能传说明链路通了"）。
> 本次抉择：**跳过分片（省流量）暂不启用** —— 单片 4MB、本功能以 KB~几十 MB 的文件为主，
> 重传是秒级成本；而判错一次的代价是「上传失败」，恰是本功能最不能出的错。
> 解析函数已按「类型安全 + 失败退化」写好并留了明确启用位，将来实测确认字段语义后只需改一行。
> 慢层补 **7 条**检查点钉住这个契约（`block_list` 数字数组 / 空数组 / 字段缺失 / 类型全错 /
> 元素混入不可识别值 / 字符串序号 / 负数序号），本板块检查点合计 **21 条**。
>
> **2026-09-16 第三次实机事故（同一天第三层，根子在"字段语义"上）**：用户重启后重传，
> 网盘里多出一串**空文件夹**（`FocusCapture_20260916_104601`、`FocusCapture_20260916_112442`、
> `files_20260916_112442`），文件依旧全失败（`errno=31500`），而 **AI 对用户说「两个文件都已经上传成功、
> 网盘上都能看到了」**（账本里 5 个文件的状态全是 `failed`）。四条独立根因：
> ① **秒传判据用错字段** —— 代码写的是 `rapid = (return_type == 1)`。开源 SDK 对响应 `block_list`
>    的注释原文是 "List of missing block indices that **need to be uploaded**"，且它的上传流程
>    **完全不看 `return_type`**，只用"列表空不空"判秒传。我们从未传 `content_md5` / `slice_md5`，
>    服务端根本无从判断"内容已存在"，所以 `return_type` 从来就不是秒传信号 ——
>    结果每个文件（含全新的）都跳过了全部分片上传，create 时一片都没收到 → `31500`。
> ② **create 漏传 `rtype`** —— 官方原文要求「与预上传 precreate 接口中的 rtype 保持一致」，我们一边 3、一边缺省。
> ③ **建目录没显式传 `rtype`** —— 官方说默认 0（不重命名、返回冲突），但**实测造出了"名字_时间戳"的新目录**，
>    每重启一次应用就多一个。文档与实测不符，故改为显式传 `rtype=0`，并对返回的 `path` 做自检留痕。
> ④ **工具输出给谎报留了土壤** —— `find_cloud_files` 的输出只有「本机已有 / 需取回」，
>    没有"到底传上去没有"这个信息，模型只能脑补；而 `FileTypes.Label(upload)` 的名字恰好叫「**已上传**」
>    （它是**分类**不是状态），连排查的人都被它骗过一轮。
>
> 现在：秒传判据改成「字段存在且为空列表」（其余一律全量上传）；create 补 `rtype`；
> 建目录显式 `rtype=0` + path 自检；工具输出每行必须带真实上传状态（统一走 `UploadStates.Label`）；
> 类型标签「已上传」改名「用户上传」。慢层本板块检查点 **21 → 27 条**（解析契约 9 条 + 工具输出 4 条）。
>
> **这层最贵的教训不是"逻辑写错"，而是两条**：
> ㈠ **同名字段在请求与响应里可能语义相反** —— 请求的 `block_list` 是"每片 md5"，响应的 `block_list`
> 是"待上传的缺失分片序号"。仅凭"读起来像"就下判断，会连翻两轮车；
> ㈡ **文档与实测不符的地方，只能靠实测定性** —— 建目录的 rtype 就是活例（官方说默认冲突，实测自动改名）。
> → 纪律：碰这块之前，**先拿一次原始响应**，把"字段真值"从推断变成事实。
>
> **2026-09-16 第四次实机事故（同一天第四层）**：前三层修完后分片终于真的往外传了，
> 但服务端回 **`HTTP 403`**（日志原文：「分片 1 上传返回了无法解析的内容（HTTP 403）」）。
> 根因：**分片上传用了硬编码的 `pan.baidu.com` 域名**。官方「获取上传域名」文档原文：
> 「**上传文件数据时，需要先通过此接口获取上传域名**。可使用返回结果 `servers` 字段中的
> https 协议的任意一个域名。」Alist 侧也记录过「传统静态域名上传方式已无法满足当前服务架构」。
> 修法：上传前先调 `GET https://d.pcs.baidu.com/rest/2.0/pcs/file?method=locateupload&appid=250528&…`
> 取 `servers` 里第一个 https 域名，用它拼 superfile2；取不到就退兜底域名 `https://d.pcs.baidu.com`
> （官方示例、Rust SDK 默认值、baiduyun_api 端点表三处独立来源都证明可用）并留 WARN。
> **刻意不跨文件缓存域名** —— 它带有效期，缓存过期会引出"大文件传到一半失败、小文件没事"这类难查的问题。
> 顺带把分片失败的错误说清楚：HTTP 层失败必须报状态码 + 用的域名（当时的"无法解析的内容"白绕了一圈）。
> 检查点 +5（域名挑选：https 过滤 / 非字符串元素 / 字段缺失 / 尾斜杠），本板块合计 **32 条**。
>
> **四层事故连起来看，形状是同一个**：没有一层是"逻辑算错"，全是**把外部约定当成了事实** ——
> 守卫的粒度、字段的方向、字段的语义、域名的来源。四次里三次是"代码写得对，前提想错了"。
> → 这也是为什么本板块的检查点密度远高于其它模块：**这里防的不是笔误，是"想当然"**。
>
> **2026-09-16 域名修完后的两个新发现（同日）**：403 消失了（兜底域名生效），但冒出两个新问题：
> ① **`locateupload` 的响应里挑不到域名** —— 日志只写"响应里没有可用的 https 域名"，而**没记响应原文**，
>    于是又只能猜字段名（这是第六次"靠推断"）。修法：挑不到就把**原始响应写进日志**（截断 400 字符），
>    并兼容三种形态：`servers` 里的完整地址 / `servers` 里的裸域名（自动补 `https://`）/ 单个 `host` 字段。
> ② **读响应体被非法 charset 打挂** —— `The character set provided in ContentType is invalid.
>    Cannot read content as string.`，出在 `HttpContent.ReadAsStringAsync`：服务端响应头声明的字符集不合法。
>    而那一刻**分片其实已经传出去了**，却因为"读不出响应体"把整单判成失败。
>    修法：新增 `ReadBodyAsync` —— 一律按字节读、自己用 UTF-8 解码，**不信服务端声明的 charset**（三处调用点全改）。
> 教训：**"外部声明"和"外部字段"一样不能当事实** —— 连响应头里的字符集都可能骗人。
>
> **2026-09-16 真实响应样本（用本机令牌把整条链路跑通后拿到的，别再靠推测）**：
> ```
> precreate    → {"errno":0,"return_type":1,"block_list":[0],"uploadid":"N1-…","path":"/apps/…"}
> locateupload → {"error_code":0,"expire":60,"host":"c.pcs.baidu.com","prov":"chongqing","isp":"cnc",
>                 "servers":[{"server":"https://c5.pcs.baidu.com"},{"server":"http://c5.pcs.baidu.com"},…]}
> superfile2   → {"md5":"0f343b…","request_id":…}                    ← 分片真的传出去了
> create       → {"errno":0,"fs_id":221478819339325,"size":1024,…}   ← 文件真的建出来了
> list         → {"errno":0,"list":[{"server_filename":"…","size":1024,"md5":"…"}]}
> ```
> 三个由此**确证**（不再是指断）的事实：
> ① `block_list` 是「**待上传**分片序号」（返回 `[0]` 即"第 0 片还没传"），`return_type=1` 是常规值、**不是秒传信号**；
> ② `servers` 的元素是**对象** `{"server":"url"}` —— 早先只认字符串元素，于是每次都挑不到域名、只能退兜底；
> ③ 域名的 `expire` 只有 **60 秒**，所以"不跨文件缓存域名"是对的。
>
> **删除接口定性（2026-09-16 第九轮，用本机令牌穷举实测，不再靠推断）**：
> `method=filemanager&opera=delete` **12 种参数形态全部返回 errno=2** —— async 取 0/1/2/不带、
> filelist 传 path 字符串数组 / `[{"path":…}]` / `[{"fs_id":…}]`、form body / json body / 全 query、
> 再叠加参数，**无一例外**；
> 而**旧式 `method=delete&path=<完整路径>` 一次即成功**（实测删净了测试文件与三个垃圾空目录）。
> 三条实测契约已写进 `BaiduNetdiskClient.DeleteAsync` 的注释：
> ① 成功响应**没有 errno**，只返回 `{"request_id":…}` —— 判成败只能判「没有 error_code」；
> ② **只接单个 path，不认 filelist**（传 filelist 得 HTTP 400 + error_code=31023）；
> ③ 失败形态：404+31066（文件本就不存在 → 按幂等成功）、403+31064（越权，平台自己也会拦沙箱外路径）。
> ⚠️ 目录**也能删**（垃圾目录被清掉了），但返回码是 403/404 与「确实消失了」互相矛盾 →
> 目录删除的返回语义**仍未定性**，代码不依赖它。
> 工具：`tools/baidu-diag`（引用主项目做**同栈**验证，不是另写一套 HTTP），
> 命令 `list` / `probe-delete` / `probe-delete2` / `probe-delete3` / `e2e`。

### B-7 Agent 工具（Services/Agent/ + Services/Destinations/ + AI 对话 Agent 路径，2026-09-06 新增）

> 总开关：设置 → AI 模型 →「启用 Agent 工具」（默认关）。关闭时 B-4 行为不变。

| 验收动作 | 期望现象 |
|---|---|
| 开关关闭，AI 问答发一条 ⚠ | 与旧版一致：回复逐字流出，全程无确认弹窗 |
| 开关开启，问「帮我搜一下上周关于 XX 的笔记」 ⚠ | 列出真实命中的本地笔记（非泛泛而谈），只读工具不弹确认 |
| 开关开启，说「帮我把这段话记成笔记：……」 ⚠ | AI 先在对话里说明要做什么（弹窗开关默认关，对话内征询）；确认后笔记出现在本地 |
| 说「新建一条待办：明天中午12点吃蛋」 ⚠ | AI 对话内征询 → 确认后待办出现在本地，提醒时间自动识别为次日 12:00（日期与当天对得上） |
| 说「把 XX 那条待办标记为已办」 ⚠ | 对话内征询 → 确认后该待办在待办面板变为已办（不再出现在未办列表） |
| 说「把那条已办改回待办 / 删掉 XX 那条」 ⚠ | 同上；删除的条目在回收站可找到 |
| 引用不存在/模糊的条目让 AI 修改 ⚠ | AI 先列候选向你澄清，绝不猜一条动手 |
| 对 AI 说「删掉所有笔记」 ⚠ | 拒绝批量：逐条向你确认或拒绝，绝不一次性静默删光 |
| 提到「搜我得到大脑里的笔记」 ⚠ | 凭证未配置时明确提示"未配置得到大脑凭证"，应用不崩不卡 |
| 不提得到大脑 / 云端 ⚠ | 不触发任何云端请求 |
| 换一个不支持工具调用的模型端点发消息 ⚠ | 提示已切换普通问答，问答仍可用 |
| 一轮对话后查看 %AppData%\FocusCapture\logs\app_当天.log | 存在且记录了本次工具调用过程；无异常报错 |

### B-8 得到大脑按钮直传（Services/Destinations/GetNote/ 包装台①+去重+推送编排，QuickViewWindow「存」按钮，SettingsWindow 得到大脑凭证区，2026-09-06 新增）

> 前置：设置 → AI 模型 → 得到大脑区填好 API Key / Client ID。Agent 开关状态不影响本组功能。

| 验收动作 | 期望现象 |
|---|---|
| 得到大脑区点「测试连接」 ⚠ | 显示「连接成功，已加载 N 个知识库」，知识库下拉自动出现列表；填错 Key 显示明确错误，应用不崩 |
| 下拉选择一个知识库 ⚠ | 落盘为默认上传目标，重开设置仍选中 |
| 灵感速览勾选 2 条 → 点标题栏「存」按钮 ⚠ | 弹确认框（知识库/标题/条数）→ 确认后得到大脑对应知识库出现《灵感_已选2条_日期》，排版与本地一致、无 ⏎ 乱码 |
| 不勾选直接点「存」 ⚠ | 当前显示列表合成 1 篇《灵感_日期》推送 |
| 同一批再点一次「存」 ⚠ | 提示「都已推送过，本次 0 条新增」，云端不出现重复（指纹按条目时间戳，每条一生只推一次） |
| 推送后编辑其中一条再点「存」 ⚠ | 不重复推送，编辑不产生新指纹 |
| 凭证未配置点「存」 ⚠ | 明确提示去设置配置，应用不崩不卡 |
| 标题栏「存」按钮（关闭按钮左侧） ⚠ | 尺寸与相邻按钮协调，不遮挡、不挤压既有按钮（时间/↑/↓/搜索/刷新/AI/导出/关闭） |
| 推送失败（如断网） ⚠ | 自动重试 2 次（间隔 1 秒）后提示失败原因；%AppData%\FocusCapture\logs 有留痕 |

### B-9 AI 对话体验（历史会话抽屉 / 真流式 + 思考过程 / 发送停止 / 新会话，2026-09-08 新增）

> 覆盖 ChatSessionService（历史列表/Load 回原文件）、OpenAICompatibleProvider.StreamChatWithToolsAsync、AgentRunService 流式循环、AIDialogWindow（抽屉/停止按钮/新会话按钮）。B-7 的 Agent 全部验收动作仍需通过。

| 验收动作 | 期望现象 |
|---|---|
| AI 问答发一条（Agent 开/关各测一次）⨗ | 回复逐字流出（不再是整段蹦出），等待首包时气泡显示「思考中…」 |
| 点回答中的「■ 停止」按钮 ⨗ | 流立即中断，已生成部分保留并标注「（已停止）」，按钮恢复「发送」，可继续对话 |
| 停止后关窗再从历史打开该会话 | 停止前的部分内容已写入历史，无残缺 tool 配对导致的报错 |
| 点标题栏「历史」 ⨗ | 左侧滑出会话列表（时间+模式+首句摘要，最新在前），再点收起且不挤压聊天区 |
| 点某条历史会话 ⨗ | 气泡按原对话还原，「追加到原笔记」按钮不出现，「存为新笔记」可用 |
| 历史会话里继续发一条 ⨗ | 正常回复且上下文衔接；Save 后不产生新的会话文件副本 |
| 标题栏「+ 新会话」 ⨗ | 气泡清空、上下文重置（发新话题不带旧内容）；翻译/搜索模式下保留原笔记上下文 |
| 回答进行中点「+ 新会话」或「历史」切走 ⨗ | 旧回答立即中断，新会话可正常发送，旧流不串显 |
| 发送后立刻连按 Enter | 不重复发送：回答中 Enter 被忽略且输入框文字保留 |
| 用思考型模型（如 DeepSeek-R1 系列）发一条 ⚠ | 气泡出现灰色斜体「思考过程」折叠区，流式时展开、完成后收起；agnes-2.5-flash 等无思考模型该区域不出现 |
| 观察回复中的工具调用步骤 ⨗ | 气泡内以灰字逐行显示「· 正在调用工具: xxx」，回答完成后保留为过程记录 |
| 换一个不支持流式+工具的端点发消息 ⨗ | 4xx 自动降级普通问答，有提示不白屏（继承 B-7 行为） |

### B-10 AI 会话同步与历史会话管理（ChatSyncEngine / HistoryDrawer / 导出 / 回收站 / 抽屉布局，2026-09-09 新增，阶段一+阶段二）

> 覆盖 ChatSessionService（SessionFile 5 字段 + GUID + SavedAt 排序 + SessionChanged 事件）、ChatGroupStore、WebDAVProvider（IFileStorageProvider 通用文件方法）、ChatSyncEngine（整文件 LWW + Rev 冲突 + E2EE 自持 DEK + 删除清单闭环① + 同名分组合并）、SyncEngine（CycleCompleted hook + MergeWindow 配置化）、MainWindow（退出 flush / SessionEnding / 启动首拉 / 冲突弹窗）、SettingsWindow（云同步页 2 新设置项）；阶段二另覆盖 HistoryDrawer 大改、ChatExportService、ChatGroupsWindow、ChatTrashWindow、抽屉三列拖拽布局。
> **零回归红线（第一优先级）**：本板块改动涉及 SyncEngine/WebDAVProvider 枢纽，验收前必须先过 B-3 笔记同步全链路（新增/编辑/删除/回收站/跨端/限频重试），确认笔记同步行为与改动前完全一致。

| 验收动作 | 期望现象 |
|---|---|
| **笔记同步回归（红线）** ⚠ | A/B 两机走一遍笔记新增/编辑/删除/回收站恢复/跨端同步，行为与改动前完全一致；设置页「上次同步」结果展示正常 |
| 配置好云同步后新建 AI 会话发几条 ⨗ | 约 30s（合并窗口）后云端出现 chat-{GUID}.json（坚果云网页可见，内容密文） |
| B 机启动等待后台首拉 ⨗ | 主窗口不阻塞启动；历史会话列表出现 A 机的会话（按时间倒序），打开能续聊 |
| 历史列表打开 ⨗ | 旧时间戳文件名的会话正常显示、排序不乱（按 SavedAt 倒序）；旧会话文件首次被打开/同步后内容中回写 GUID |
| 主动退出（有网）⨗ | 正常退出，无弹窗；日志/状态无异常 |
| 主动退出（拔网线）⨗ | 必见「有记录未上传成功」弹窗，可【重试】/【仍要退出】；仍要退出后下次启动同步自动补传，不无声丢失 |
| 设置页关闭「同步 AI 问答记录」开关 ⨗ | 会话不再上传/下载（云同步区状态留痕），笔记同步照常 |
| 上传合并间隔改为 60 失焦 ⨗ | 自动按 60 保存生效；输入 10 失焦自动回填 30（下限兜底）；文案注明拉取仍每 30 分钟 |
| 会话同步失败（改错授权码后发消息等）⨗ | 设置页云同步区「AI 问答记录：失败: …」持续可见，不无声丢失 |
| 盐未就绪时（配了授权码但笔记未首配）发消息 ⨗ | 会话同步静默跳过 + 状态留痕「等待笔记同步完成首配」，不报错不崩 |
| 双端同时改同一会话（A 开旧会话未发消息，B 已聊更新；A 再发消息保存）⚠ | 必见冲突弹窗【覆盖用我这版 / 保留他端】：选「是」本机版覆盖云端；选「否」本机版暂不上传且状态留痕；云端内容不被静默覆盖（弹窗失败时按「保留他端」处理） |
| A 删会话 | 删除清单上传；B 机同步后该会话文件移入 B 的 chat_history\trash（闭环①） |
| 旧版会话（升级前 20 条消息文件）打开续聊 ⚠ | 不崩、正常保存；新字段默认值（Id 回写 GUID、Rev 从旧值续计） |

**阶段二：历史会话管理 UI（HistoryDrawer 大改 + ChatExportService + ChatGroupsWindow + ChatTrashWindow + 抽屉布局）**

| 验收动作 | 期望现象 |
|---|---|
| 展开历史抽屉看列表 | 置顶会话带 📌 且在「置顶」分区最上；有分组的按「分组 → 组内条目」分区；未分组在最后（无分组时平铺无分区头）；预览 = 重命名标题优先，无标题回退首条用户消息 40 字 |
| 条目「⋮ → 重命名」 | 输入新标题确定后列表立即更新；重启保持；约一个同步周期后他端可见；留空 = 恢复默认预览 |
| 条目「⋮ → 置顶 / 取消置顶」 | 列表立即重排（置顶区最上）；重启与他端同步保持 |
| 条目「⋮ → 分组到…」 | 选已有分组 / 未分组 / 新建分组…；选择后立即分区显示；条目行显示分组名 |
| 头部「分组」按钮 | 打开分组管理窗：新建（同名拒绝）/ 重命名 / 删除（确认后组内会话回未分组，会话不删） |
| 条目「⋮ → 删除会话」 | 确认弹窗 → 条目消失；文件移入 chat_history\trash；他端同步后同样进回收站（闭环①）；删除正打开的会话时对话区自动切新会话 |
| 条目「⋮ → 导出」四格式各一次 | 文件生成、可打开、内容 = user/assistant 完整对话且无 tool 中间消息；Markdown/TXT/JSON/Word 全验证 |
| 「⋮ → 批量操作」进入多选 | 条目出现勾选框、出现操作条（全选/删除/分组/导出/退出）、⋮ 隐藏；点条目 = 切换勾选 |
| 多选「全选」 | 未全选时一次全选；已全选时再点 = 全不选 |
| 多选「删除」 | 确认一次后全部进回收站，自动退出多选 |
| 多选「分组」 | 选目标分组后全部移动，自动退出多选 |
| 多选「导出」 | 选格式 → 选目录 → 每个会话各导出一个文件；两个会话同名时自动 _1/_2 不互相覆盖；结果弹窗显示成功/失败数 |
| 头部「回收站」按钮 | 打开会话回收站窗：列出 trash 内会话（预览/时间/模式/条数） |
| 回收站「恢复」 | 会话移回主目录并重新出现在历史列表（本机行为，不跨端传播）；主目录同名冲突时拒绝并提示 |
| 回收站「彻底删除」/「清空」 | 二次确认后本地物理删除，不可恢复 |
| 抽屉头部收起按钮（⇤ 图标） | 抽屉收起（与「历史」按钮同一动画）；再点「历史」按记忆宽度展开 |
| 拖动抽屉右缘分隔条 | 宽度跟手、有边界（最小 150 / 最大 480）；消息区随动；拖完重启宽度保持；收起状态不记忆 |
| 展开动画中拖分隔条 | 拖拽被禁用（动画与拖拽不打架），动画结束后恢复可拖 |
| 同步周期拉到他端新会话（抽屉展开中） | 列表自动出现新会话，无需手动重开抽屉 |
| 双端各建同名分组 ⚠ | 同步后两端各剩一个分组（创建时间早者胜），原本引用败者 GroupId 的会话自动归入胜者分组，列表无悬空分组 |
| 抽屉打开 → 点击条目 → 续聊 ✓ | 上述全部改造不影响原有点击条目→续聊链路（B-9 抽屉基础行为） |

### B-12 灵感速览可组装标题栏 + 窗口壳（QuickViewToolbarCatalog + QuickViewWindow 标题栏/铬区 + SettingsWindow「灵感速览」板块，2026-09-13 新增；同日扩展：扩展功能 +5 / 图标矢量化为 MDL2 / 设置页可读性修复 / 主程序统一开窗）

> 覆盖 Services/QuickViewToolbarCatalog.cs（功能目录 8 项 + 配置清洗 + 像素预算，纯逻辑，快层 [3] 直链覆盖）、QuickViewWindow（标题栏按钮按设置生成 RebuildToolbar + 窗口铬区 最小化/最大化/关闭 + WindowChrome 边缘缩放 + 宽度记忆）、AppSettings（QuickViewToolbarLeft/Right + QuickViewWidth + QuickViewTopmost）、SettingsWindow 新板块「灵感速览」（宽度/置顶/唤出行为迁入 + 标题栏自定义编辑器）。八个功能的 Click 逻辑与时间筛选弹层（B-11）完全复用未动。

| 验收动作 | 期望现象 |
|---|---|
| 老配置（无新字段）升级后首次唤出 ✓ | 标题栏默认布局与旧版一致（左：今天 / 上传 / 下载；右：查找 / 刷新 / AI 问答 / ▼ 导出 / 存到得到大脑），最右铬区「最小化 / 最大化 / 关闭」（settings.json 无 QuickViewToolbar* 字段即默认） |
| 看标题栏图标外观 ✓ | 图标为**矢量图标**（Segoe MDL2 Assets），清晰居中，**不得出现空框/豆腐块**；文字按钮仍是文字 |
| 点每个标题栏功能按钮 ✓ | 与重构前行为一致：时间筛选弹层（B-11）、上传/拉取、查找、刷新、AI 问答、导出、存到得到大脑 |
| **点扩展功能按钮**（需先在设置页添加）✓ | 待办汇总 / 回收站 / 设置 / 导入笔记 / 每日总结 —— 一律由主程序开窗，与托盘菜单、全局热键入口**行为完全一致** |
| 设置→灵感速览：左侧列表添加/移除/上移/下移 ✓ | 面板标题栏即时按新顺序重排（改动即存，无需重启）；同一功能两侧只能出现一处 |
| 设置页功能列表的按钮行 ✓ | 四个按钮（添加/上移/下移/**移除**）**全部可见**不被裁切（内容超宽时自动换行） |
| 设置页功能列表的文字 ✓ | 每个功能显示**人类可读名称**（"上传"而非"↑"），文字清晰可辨、对比度足够 |
| 「可用功能」下拉 ✓ | 只列两侧都未启用的功能；预算放不下的项显示「（空间不足）」且不可选 |
| 「可用功能」下拉为**空**时 ✓ | 下拉与「添加」按钮一并**置灰**，并出现橙色提示"全部功能都已放上标题栏，先「移除」某一项才能添加新的" |
| 预算文字 ✓ | 显示「已用 X / 可用 Y px」（可用 = 面板宽度 − 固定开销 180）；超预算变红并提示加宽面板或移除按钮 |
| 「恢复默认布局」 ✓ | 两侧列表与面板标题栏立即回到默认 8 按钮 |
| 设置→灵感速览：面板宽度改 480–1280 ✓ | 打开中的面板宽度即时生效；拖动面板边缘缩放后约半秒自动记回设置（重启保持） |
| 设置→灵感速览：窗口置顶开关 ✓ | 关闭后面板不再压住其他窗口；再开恢复置顶（改动即存即时生效） |
| 点「最小化」 ✓ | 面板缩进任务栏（任务栏有图标），点任务栏图标恢复；Ctrl+Alt+V 热键唤出也能从最小化恢复正常态 |
| 点「最大化 / 还原」 ✓ | 面板铺满工作区（不盖任务栏），圆角取消、图标切换为"还原"样式；再点还原回原宽度带圆角 |
| **同步进行中看两个同步按钮** ⚠ | 图标变为环形箭头（进行中态），ToolTip 显示"上传中…/拉取中…"；**不得出现空框**（此前的"上传中…"文字在图标字体下无字形） |
| **设置页搜索**（在搜索框输入"上传"等新名称）⚠ | 搜索结果正常，功能列表的提示文字**不得**混进搜索索引（提示包在 Border 里即为此） |
| 拖动标题栏空白处 ✓ | 窗口跟随拖动；最大化状态下拖动无动作 |
| Esc / × 关闭再唤出 ✓ | 与旧行为一致（Hide/Show 单例），宽度与工具栏布局保持 |
| 手改 settings.json 塞未知 id / 空列表 ⚠ | 下次重建自动清洗：未知 id 丢弃、去重、整列无效回退默认（快层 [3] 覆盖） |
| **双击笔记**（2026-09-18 语义变更）⚠ | **立即**进入行内编辑态（编辑框聚焦 + 内容全选 + 列表滚动位置不变）；**不得弹出任何错误框**；**剪贴板内容不得被改动**（改前先复制一段文字做对照，双击后粘贴应仍是那段）。此前双击的第一下会先走「单击复制」，剪贴板被占用时抛 CLIPBRD_E_CANT_OPEN → 弹模态框吃掉第二下点击；加上同步写剪贴板的阻塞，双击进编辑又慢又可能失败 |
| **单击笔记**（2026-09-18 语义变更）⚠ | **什么都不发生**：不复制、不弹框、不闪绿、不改剪贴板（原为单击即复制，已按用户要求取消）。编辑态内的点击同样无动作 |
| 右键「复制」（保留）✓ | 复制该条内容到剪贴板（仍走 SafeClipboard：被占用时退避重试，失败只在标题栏出红字，不弹框） |

### B-11 灵感速览时间筛选弹层（DatePickerPopup + QuickViewWindow + AppSettings + SettingsWindow + ImportPreviewDialog 迁移，2026-09-12 新增）

> 覆盖 Windows/DatePickerPopup.xaml(.cs)（左列预设菜单 + 自定义双月日历 + 圆点/Tooltip）、QuickViewWindow（时间按钮接入 + 唤出行为 + 状态落盘）、AppSettings（QuickViewRestoreLastFilter + QuickViewLast*）、SettingsWindow「灵感速览」板块下拉（v3.9 自「通用」迁入）、ImportPreviewDialog（目标日期改用 MiniCalendarPicker）。旧 CalendarWindow 已删除（全库无引用）。数据层零改动（复用 LoadNotes/LoadNotesRange/LoadNoteCounts/LoadTodoDueDates）。

| 验收动作 | 期望现象 |
|---|---|
| 点标题栏时间按钮（📅 今天） ✓ | 按钮下方弹出弹层：左列 7 项（今天/昨天/近 7 天/近 30 天/本月/上月/自定义），当前档位带 ✓ 绿字；再点一次按钮 = 收起 |
| 点预设项（如「近 7 天」） ✓ | 弹层即关，列表刷新为该区间笔记，时间按钮文案变「📅 近 7 天」；「昨天」为昨天单日，「本月/上月」为自然月 |
| 点「自定义」 ✓ | 弹层不关，右侧展开双月日历（当前选中区间绿色铺底 + 起止实心） |
| 日历第一击 A 日、再击 B 日 ✓ | 区间 = A~B（先后顺序无关自动交换），弹层关闭，按钮显示如「📅 09-06 ~ 09-12」 |
| 同一日期连击两下 ✓ | = 选中该单日（start==end 回 Date 模式），按钮显示该日期 |
| 选区间过程中悬停其他日期 ✓ | 铺底预览实时跟随「起始日 ~ 悬停日」 |
| 有笔记的日期格 / 有未到期待办的日期格 ✓ | 日期下方绿点（数量三档深浅）/ 橙点，两者并存并排；悬停 Tooltip 显示「笔记 N 篇 / 有未到期的待办」 |
| 日历翻月（‹ ›） ✓ | 双月联动翻页，渲染正常；今天灰框描边、未来日期灰字 |
| 点弹层外任意处 / 面板失活 ✓ | 弹层收起（不误触选择） |
| 点「🔍 查找」进关键词模式 ✓ | 指示行显示「查找："关键词"」+ 返回按钮；时间按钮文案不受影响 |
| 默认设置下关闭面板再唤出（Ctrl+Alt+V） ✓ | 重置为当天笔记，按钮回「📅 今天」 |
| 设置→灵感速览→唤出行为切「唤出时恢复上次的筛选」 ✓ | 唤出沿用关闭前的日期/区间；自定义区间重启应用后仍还原（settings.json 落 QuickViewLast*） |
| 勾选导出/删除/已办沉底在区间模式下 ✓ | 与单日模式行为一致（筛选/排序逻辑未动，仅数据源换 LoadNotesRange） |
| 导入向导：目标日期旁 📅 ✓ | 弹 MiniCalendarPicker 单选即关，选中回填输入框（原 CalendarWindow 链路） |

### B-5 语音输入 / 沉浸模式（VoiceService / ImmersiveSessionService）

| 验收动作 | 期望现象 |
|---|---|
| Ctrl+Alt+R 唤起语音输入窗口 ✓ | 窗口打开（尺寸和主题以设置项为准） |
| 语音输入窗口按 Ctrl+S ✓ | 保存并退出 |

### B-6 外观与系统（ThemeService / HotkeyService / AppIconService / Win32）

> ✅ **2026-09-19 结案**：本组 2026-09-19 新增的 5 条外观项（换自定义图标 / 恢复默认 / 图标损坏回落 / 悬浮球角标 / 悬停不被咬 / 投影无平边 / 球没变大）已由用户人工验收通过，全绿。
> 验证方式：人眼看屏幕 —— 这三件事（任务栏图标是否跟随、悬停瞬间角标是否被球咬、浅背景投影有无平边）**自动化证明不了**，快照与像素探针都替代不了。

| 验收动作 | 期望现象 |
|---|---|
| 换自定义图标 ✓ | **任务栏窗口图标 + 托盘图标同时变化**（设置 / 灵感速览 / AI 问答等所有窗口，立即生效、不必重启）；重启后仍是自定义图标 |
| 点「恢复默认图标」 ✓ | 托盘**与已打开窗口的任务栏图标**同时回默认（不必重启）—— 只改托盘不改任务栏就是没修好 |
| 自定义图标文件损坏 / 被手动删掉 ✓ | 自动回落默认图标，应用照常启动、窗口照常打开（慢层已钉住「绝不抛」） |
| 悬浮球角标（球右上角那个数字） ✓ | **圆是完整的**（不被窗口边界切掉任何一块），数字在圆内居中，且**只是「搭在球肩上」不是压掉一半**（与球重叠约 4px，不再是 9px） |
| 鼠标悬停悬浮球（球放大到 1.1 倍） ✓ | 角标**不会被球咬掉一块**（角标圆心在放大后的球外）；球的投影仍完整 |
| 看悬浮球的投影（尤其浅色桌面） ✓ | 四周**自然淡出、没有一道平边**（投影要有地方淡完；窗口边界处应已≈透明）。同时球本身**没有变大**——变的只是它四周的留白 |
| 固定到任务栏的快捷方式 / 开始菜单 / 桌面快捷方式 / 资源管理器里 exe 文件的图标 ⚠ | **不变** —— 这四处读的是 exe 内嵌资源，运行期改不了，只有重编译或换 exe 才行。**属能力边界，不是故障**（平台机制所限，**无验收必要、永久保持 ⚠**） |
| 勾选开机自启后重启电脑 ✓ | 软件自动启动 |
| 拖动悬浮球到新位置后重启 ⚠ | 位置保持（需设置里开启「记住位置」） |
| 把一个热键设成已被其他程序占用的组合 ⚠ | 「设置 → 热键」板块红字列出该键位与功能名，运行日志有 WARN 留痕；其余热键照常可用 |
| 点「按下新快捷键…」后按 Esc 取消 ⚠ | 取消录制并还原显示，**所有**热键仍然可用 |
| 点「按下新快捷键…」后直接关掉设置窗 ⚠ | 关窗后所有热键仍然可用（录制期临时注销已恢复） |

### B-15 悬浮球拖放保存（FloatBall 拖放 + DropActionStrip / DropActionCard + 设置「显示」板块，2026-09-16 新增）

> ⚠️ **改这块前必读的禁区**：讨论过程中有 6 条中间结论被推翻，**别按旧版实现** ——
> ① 文字在球上**悬停 1 秒**触发上传（不可撤回的动作只能由**显式点击**触发，误上传收不回）；
> ② 文件拖上去时**在拖拽中自动弹菜单**（拖拽期间鼠标左键被 OLE 循环独占，按钮点不动，必须**松手之后**再弹卡片）；
> ③ 「透明区也能接收拖放」是探针**误判**（判定半径没算椭圆那 1px 描边，实际小条/卡片**必须是独立窗口**）；
> ④ 「吸附态收不到拖放」是**假信号**（探针在吸附态不闪绿，反馈缺失被读成功能失效，日志里其实收到了）；
> ⑤ 「新增暂存区：拖入先复制保命 → 点存网盘才入仓」理解错了（正确：**拖入什么都不做**，点「存到网盘」才复制入仓+上传）；
> ⑥ 「浏览器拖网页图片（虚拟文件流）」**本次不做**（实测 `COMException: DV_E_FORMATETC`，WPF 取值接口读不了 IStream）。
>
> 视觉/交互另有 4 处被用户直接否掉：小条按钮**横向**（应竖向）、小条带「已存为笔记」提示语（应去掉）、
> 卡片选项**带图标**（应无图标 + 整行悬停高亮）、卡片副行「已复制到本机」（应为「{大小} · 来自{来源}」）。
> 涉及文件：`Windows/FloatBall.xaml(.cs)`、`Windows/DropActionStrip.xaml(.cs)`、`Windows/DropActionCard.xaml(.cs)`、
> `Services/DragDropSaveService.cs`、`Models/AppSettings.cs`、`Services/AI/ChatAttachmentService.cs`、`Windows/AIDialogWindow*`。
>
> **机器可验的部分已进慢层「拖放保存组」（46 条）**，本表剩下的是**必须人手拖一次**才能验的现象级终态。
> 诚实标注：下表全部条目在提交时代码已接好、但**尚未经真人拖放验证**（OLE 拖放没法由自动化代劳，见上）。
> 自验留痕：该组首跑 1 红，两条都是**检查点构造错误**（断言了 WPF `autoConvert` 的实现细节 / 用 `Contains("2 ")`
> 模糊匹配大小），已按实际语义修正检查点，**未改动产品代码**（合第二节规则 4）。

| 验收动作 | 期望现象 |
|---|---|
| 设置「显示」板块看总开关 ⚠ | 「拖到悬浮球保存内容」**默认未勾选**；两个滑块（拖放浮层不透明度默认 90%、文字小条停留时长默认 3 秒）能存能回显，重启后保持 |
| 总开关**关闭**时拖文件/文字到悬浮球 ⚠ | **毫无反应**，光标都不变（`AllowDrop=false`），与改造前一致 |
| 打开开关后拖一个**文件**到球上 ⚠ | 球自动展开（吸附态 8×36 → 56×56）；松手后球闪绿；浮出紧凑卡片，标题是**真实文件名**，副行是「大小 · 来自来源程序」 |
| 拖**微信图片**到球上 ⚠ | 卡片标题是 `微信图片_日期_时间.jpg`，**不是那串哈希名** |
| 拖**文字**到球上 ⚠ | 立刻存为本地笔记 + 球闪绿 + 球旁浮出**竖向**小条（「AI 问答」「得到大脑」，无图标、无「已存为笔记」提示语） |
| 拖浏览器里的中文文字 ⚠ | 笔记内容**无乱码**（取值只认 `UnicodeText`，`Text` 版在 Chromium 下是坏的） |
| 拖微信里的 xlsx 到球上 ⚠ | 按**文件**处理，不会把文件名（如 `JD-260911.xlsx`）当正文存成一条笔记（FileDrop 判定优先于文字） |
| 不点小条/卡片上任何选项 ⚠ | 停留秒数到了自动消失，**零副作用**：网盘没多文件、笔记没多条目、源文件原地不动 |
| 点小条「AI 问答」⚠ | AI 对话窗口打开，输入区带着这段文字 |
| 点小条「得到大脑」⚠ | 直接调适配器保存（绕过 AI）；未配凭证时该项**置灰**并提示去设置 |
| 点卡片「用 AI 问答打开」⚠ | 对话窗口打开，输入区**已带该文件附件**（图片/文档按格式判定） |
| 点卡片「存到网盘」⚠ | 「设置 → 文件与网盘」出现该文件；稍后百度网盘能真的看到它 |
| 卡片「发到得到大脑」⚠ | **仅文本类文件**（.md/.txt/.csv/.json/代码）显示该行；非文本类不显示 |
| 卡片末行 ⚠ | 「更多渠道，敬请期待」灰色不可点 |
| 拖入后隔很久才点动作，而源文件已被微信清掉 ⚠ | 给出**明确提示**（文件已不在），不是静默失败 |
| 调「拖放浮层不透明度」滑块 ⚠ | 已打开的小条/卡片透明度**实时跟着变** |
| 拖放期间源文件 ⚠ | **原地不动**（我们只读取数据，不实现移动语义；`Effects=Copy` 不会真移动） |
| 界面判读 ⚠ | 小条宽 96px、竖向；卡片宽 ≤276px **贴合内容**（右侧不留大片空白）；两者均**无图标**、悬停是**整行**高亮。快照 19~23 出图判读 + 24 核验设置控件 |
| 点卡片/小条上**任一**动作 ⚠ | **不弹「FocusCapture 遇到一个界面错误」**：小条「AI 问答」「得到大脑」、卡片「用 AI 问答打开」「存到网盘」「发到得到大脑」点完直接进对应流程；`%LocalAppData%\FocusCapture\crash.log` **无新增条目**（2026-09-16 曾连报 7 次，见下） |

> **事故记录（2026-09-16 23:33~23:38，用户实机首测发现）**：拖文件到球后点任一动作，先弹「在窗口关闭期间，
> 无法将可见性设置为可见…」，点掉后功能照旧可用。crash.log 连报 7 次，栈顶恒为
> `DropActionCard.Dismiss()` → `Window_Deactivated()`。
> **根因**：关闭入口有五个（三按钮 / 点外面 / Esc / 兜底超时）会互相踩 —— 按钮路径是「先 Dismiss() 再抛事件开 AI 窗」，
> AI 窗一激活本窗就 `Deactivated`，而 `_armed` 早已 true → **二次 Close()** → WPF `VerifyNotClosing()` 抛异常。
> 功能其实没事（异常发生在关闭流程的再入里，动作早已派发），纯白弹一个吓人的框。
> **修法**：`Dismiss()` 幂等 + `OnClosing` 上锁（覆盖 MainWindow 直接 `Close()` 的路径）+ 关窗前摘掉 `Deactivated` 监听；
> `DropActionStrip` 同类加固。**未加自动化检查点** —— 复现它要靠真实激活时序，写成反射调私方法的测试属于
> 「断言框架内部行为」，正是本表规则 4 的反面；这里改为断言**用户能看见的终态**（不弹框 + crash.log 无新增）。
> ⚠ 附带记一笔：`crash.log` 里 2026-08-27/28 有 8 条同类报错，但栈是 `MainWindow.OnSourceInitialized` 的
> `Show()` 失败（已关闭的待办汇总窗被再次 Show），**v3.8 已按「已关闭的窗口绝不再 Show」修掉**，与本条无关。

---

### B-16 Agent 工具扩展 + 文档解析（Services/Agent/ + Services/Files/ + Services/AI/PromptBuilder + AIDialogWindow Agent 路径，2026-09-17 新增）

> 设计依据：工具清单与能力边界见 `Services/Agent/` 下各 `*Tools.cs` 的类注释与 `AIDialogWindow.EnsureAgentRegistry()` 的注册表；逐项决策记录见提交历史（2026-09-17 一批）。
> 涉及文件：`Services/Agent/`（LocalTools / FileTools / RecycleBinTools / ExportTools / DocumentTools）、
> `Services/Files/DocumentTextExtractor.cs`（新）、`Services/Files/XlsxTextExtractor.cs`（新）、
> `Services/Files/FileRepository.cs`（ReadTextAsync 扩展）、`Services/AI/ChatAttachmentService.cs`（PDF/xlsx 支持）、
> `Services/AI/PromptBuilder.cs`（当前时间）、`Services/NoteService.cs`（**UpdateNote 新增 / RewriteEntryLine 抽取 / UpdateTodo 加回收站兜底**）、
> `Windows/AIDialogWindow.xaml.cs`（ExtraSystemContext 注入时间 + 工具注册 + 第 5 条 Agent 规则）、`FocusCapture.csproj`（引 PdfPig）。

**本次新增的工具（11 个，注册后共 26 个）**：
`update_note`（笔记/待办原地改内容与提醒时间）、`list_notes_by_date`、`note_stats`、
`list_recycle_bin`、`restore_deleted`、`export_notes`、`update_file_meta`、`read_spreadsheet`、`read_pdf`；
另 `read_cloud_file` 扩展支持 docx/xlsx/pdf。

> **四条红线（改这块之前必读）**：
> ① **文档工具的来源只能是 `handle`（用户亲手选的文件牌号）或 `file_id`（本地元数据编号）** ——
>    `read_spreadsheet` / `read_pdf` 沿用与文件工具同一条红线，**没有路径参数**。慢层有断言守护：编造牌号、把本机路径当牌号、两者都不给，三种情况都必须失败并给出可执行指引。
> ② **原地改行 = 旧内容先写回收站再替换**（`NoteService.RewriteEntryLine`）。这是用户拍板的语义（"直接改，不要另存一条"），
>    代价是旧文本消失、且行身份是 `SHA256(行文本)`（改行 = 删旧行 + 加新行）。对策两条：回收站兜底 + 抛 `LinesDeleted` 墓碑给同步层。
>    回收站写失败就**中止改行** —— 宁可这次改不成，也不能让旧内容无声消失。
> ③ **AI 改内容与界面「编辑」现在是同一条路（2026-09-20 起）**：都走**原地替换**（界面编辑此前走
>    `AppendEdit` 追加【编辑】行，因跨端同步实测"原始+编辑"双条并存/孤儿卡删不掉，经用户拍板改为替换，即 A+B 方案的 B）。
>    替换时旧【编辑】痕迹行一并清理（进回收站 + 发墓碑，见 `NoteService.RemoveStaleEditMarkers`）——
>    不清的话展示层会按 ref 把旧痕迹挂回新行，把新正文盖成旧编辑文本。【AI 释义】是子条目语义，**保留不清**。
>    改之前先想清楚要不要再拆成两条路（拆回追加 = 拆回跨端双条 bug）。
> ④ **工具执行线程不是 UI 线程** —— `AgentRunService.RunAsync` 全程 `ConfigureAwait(false)`，工具体跑在线程池线程上。
>    任何"弹窗问用户 / 碰 UI 对象"的回调**必须经 `Services/UiThread.AskAsync` 封送**，否则抛
>    「调用线程无法访问此对象，因为另一个线程拥有该对象」。**这不是理论 —— 2026-09-20 真机炸过一轮**
>    （Skill 准入确认），而"写操作确认弹窗"里同一处写法只是**因为用户把它关着才没暴露**：一旦打开，所有写工具全废。
>    慢层「UI 线程封送」组 6 条守着它，改完可以拿掉 `UiThread` 跑一遍看它红不红（验过：会红）。

> **⚠️ `read_cloud_image` 为什么没做（2026-09-17 结论，别重复踩）**：工具返回的是 tool 消息，
> 而 `OpenAICompatibleProvider.BuildMessagesArray` 里 Tool 分支排在附件分支之前、只写纯文本 content ——
> **工具无法把图片交给模型看**。要做就得改唯一的请求序列化入口，风险与收益不成比例，本次明确放弃。

> **机器可验的部分已进慢层「Agent 工具组」（42 条）**，覆盖：时间上下文注入、原地改行与回收站兜底、
> 删除→列出→恢复闭环、按日期查询与统计的精确计数、导出落点与**精选导出的三条取数路径**、xlsx 三类坑（共享字符串 / **日期序列号** / 稀疏列）、
> PDF 有文字层与扫描件两条路径、以及三条来源红线。
> 首跑 4 红 + 1 红：4 红是**检查点自身构造不当**（同一分钟内 3 条回收站记录，分钟精度必然撞车 —— 应带 `content_hint` 消歧，已按真实用法修正），
> 1 红是 H1 旧断言（"PDF 本期不做"）随本次功能变更而失效，已改为断言 PDF 现在**支持**。

> **`export_notes` 的三条取数路径（2026-09-17 二次调整，用户反馈「只能导整天」）**：
> 优先级 `ref_times`（精选指定条目）> `query`（关键词筛选）> `date`/区间（**全量**，原行为）。
> ⚠️ **`ref_times` 只能用分钟精度** —— 条目落盘时间就是 `yyyy-MM-dd HH:mm`（`NoteEntry.ToMarkdownLine`），
> 同一分钟记的多条时间戳完全一样，**秒级信息在磁盘上根本不存在**（别想着"改成秒级就能区分"）。
> 因此 `ref_times` 的元素支持对象形态 `{"time":"...","hint":"内容关键词"}` 用于消歧；
> 命中 0 条或 >1 条时**报错且一个文件都不写**，并在错误里给出可执行的下一步（补 hint / 改用 query）。

| 验收动作 | 期望现象 |
|---|---|
| 问 AI「今天几号」 | 答出的日期与系统日期一致（此前全链路没告诉它当前时间） |
| 隔天在同一会话里再问一次 | 日期**跟着变**（证明时间没被写死进会话历史） |
| 说「把那条待办改成明天早上八点」 | AI 先列出要改哪一条，确认后改成功；速览面板能看到新内容与提醒时间 |
| 说「把那条笔记改成 XXX」 | 原行被替换（不是追加一条编辑记录）；**旧内容可在回收站找回** |
| 故意让 AI 改错内容，然后说「刚才改错了，恢复一下」 | AI 能在回收站找到并恢复 |
| 说「刚才删的那条能找回来吗」 | AI 列出回收站并恢复（此前只能让用户自己去界面看） |
| 说「这个月我记了多少条」 | 数字与日历/速览面板对得上 |
| 说「今天记了什么都列出来」 | 与速览面板当天内容一致；**待办出现在它的提醒日**（不是创建日） |
| 说「把今天的导出来」 | 文件出现在设置里的默认导出文件夹；AI **不会说"已上传网盘"** |
| 先让 AI 列出某天的记录，再说「**我只要这两条，导出来**」⚠ | AI 用 `ref_times` 精选导出；导出的文件里**只有那两条，不夹带当天其它内容**（此前只能整段导或说做不到） |
| 挑**同一分钟**记的两条让 AI 只导这两条 ⚠ | AI 补上内容关键词（hint）后精确导出；**不会随便挑一条或整段导** |
| 拖一个 **PDF** 进 AI 输入框 ⚠ | 能加上卡片，AI 能说出文件里的内容（此前是直接拒绝 PDF） |
| 拖一个**扫描件/图片版 PDF** 进输入框 ⚠ | 明确提示"读不出文字（可能是扫描件）"，**AI 不会编造文件内容** |
| 拖一个 **xlsx** 进输入框 ⚠ | 能加上卡片，AI 读到的是表格文本；**日期列显示为日期而不是 44927 这类数字** |
| 说「网盘里那个 xlsx 表读一下，看看总共多少行」⚠ | AI 报出总行数与表头；**不会把只列出的前几十行当成全部** |
| 说「把网盘那个文件改名叫 XXX」 | AI 说清"改的是本机显示名，云端文件名不变"；**绝不会说"已在网盘上改名"** |
| 让 AI 读一个**没被选过**的本机文件 | 明确要求用户点「选择文件」，**不会杜撰路径或假装读到** |

### ⚠ 本节尚未经真人验收的部分

上表带 ⚠ 的条目需要在真实对话里跑一遍（涉及模型选工具、真实文件格式、真实拖放）。
机器能证的部分已全部进慢层检查组；**"模型会不会挑对工具、会不会谎报"只能靠真人对话验收**，
交付时请按上表逐条走一遍。

### B-17 Skill 运行时（Services/Skills/ + AIDialogWindow·ExtraSystemContext + SettingsWindow，2026-09-20 新增）

> 设计依据与全部决策记录：`docs/skill-runtime-plan.md`（含开工当天的 18 条实测补记）。
> 涉及文件：`Services/Skills/`（SkillCatalog / SkillScriptRunner / SkillRuntime / SkillManifest / SkillTools，五个新文件）、
> `Windows/AIDialogWindow.xaml.cs`（装配两行 + ExtraSystemContext 注入清单）、`Models/AppSettings.cs`（SkillTrusted）、
> `Windows/SettingsWindow.xaml(.cs)`（AI 模型板块里的「Skill 扩展」分区）、`FocusCapture.csproj`（runtime 复制规则）、
> `Diagnostics/UiSnapshot.cs`（新增截图项 03b）、`tools/fetch-python-runtime.ps1` + `.bat`（新增）、`.gitignore`（`/runtime/`）、
> `Services/UiThread.cs`（新，UI 线程封送 —— 工具线程上不能直接碰 UI 对象，见下 ④）。

**它是什么**：让 AI 问答能加载并执行 **WorkBuddy 格式**的 Skill（`SKILL.md` + `scripts/`），**不改写 Skill 格式**。
装 Skill = 把文件夹整个拷进 `%AppData%\FocusCapture\Skills\`；某个 Skill 首次要跑脚本时弹一次授权确认；
撤销入口在「设置 → AI 模型 → Skill 扩展」。

> **四条红线（改这块之前必读）**：
> ① **九条硬规则写死在 `SkillScriptRunner` 里**，不得下放给模型、也不做成配置项。最要紧的两条：
>    **Skill 名只在已扫描目录表里查、取表中预存的绝对路径**（绝不拼串）；**脚本解析后必须仍在
>    `<skill>\scripts\` 内**（`..`、路径分隔符、绝对路径一律拒）。安全边界只能靠机器守。
> ② **`runtime\python` 里必须没有 `python313._pth`** —— 留着它 Python 进 isolated 模式：同目录模块 import
>    直接失败、`PYTHONPATH` 被忽略、`Lib\` 不加载。症状是 local-rag 那类「公共逻辑拆在同目录模块」的 Skill
>    **静默失效**。慢层有专门检查点守它（「同目录模块 import 必须成功」）。
> ③ **缺运行时或执行失败时，返回给模型的文本不得含「已完成 / 已执行 / 成功 / EXIT=0」字样** ——
>    这是「AI 谎报已上传成功」那条红线在 Skill 场景下的同一道防线。
> ④ **准入确认必须经 `UiThread.AskAsync` 封送**（同 B-16 ④）。`SkillScriptRunner` 在线程池线程上调
>    `TrustPrompt`，直接 `MessageBox.Show(this, …)` 会抛「调用线程无法访问此对象」——
>    2026-09-20 真机踩到：模型连着四轮拿到这句内部错误，而它读到的清单、脚本名全是对的，
>    所以它自己的判断是"工具运行环境有问题，不是授权过期"（**判断正确**）。
>    ⚠ **这类故障的报错文本极具误导性**：它看起来像"授权挂了"，实际连一次网络请求都没发出去。

**为什么它对主循环零侵入**：清单走 `AgentRunService.ExtraSystemContext`（每轮求值、不落会话历史），
`AgentRunService` 一行未改；核心全在 `Services/Skills/` 独立目录 —— **出问题可整目录删掉回退**，不牵存量功能。

**前置**：`runtime\python` 由 `tools\fetch-python-runtime.bat` 拉取（约 11MB，国内镜像）。缺了它，
含脚本的 Skill 会明确报错（**不假装成功**），纯提示词型 Skill 不受影响 —— 这是刻意的「降级而非崩溃」。

**机器可验的部分**：快层组 `[6] Skill 目录扫描与解析`（18 条：frontmatter 各种畸形 / 中文目录名 / 超长截断 /
清单封顶 / 缓存命中 / 目录不存在）+ 慢层组 `Skill 执行`（20 条：路径越界四条 / 扩展名白名单 / 准入确认闭环 /
真实执行 / 同目录 import / 中文参数 / CWD / 环境变量清理 / 超时杀进程 / **缺运行时的防假成功**）。

| 验收动作 | 期望现象 |
|---|---|
| 把 `feishu-kb-manager` 整个目录拷进 Skill 目录（**一个字节不改**） | 设置页显示「已装 1 个，其中 N 个可执行」 |
| 在 AI 问答说「记一下 Promise 是 JS 异步对象」 | 首次弹授权确认 → 允许 → **飞书多维表格里真的出现这条记录**（需先处理好飞书授权，见设计稿 §十一） |
| 断开网络再说一句「记一下 XXX」 | AI **明确说失败**，绝不出现「已记录」 |
| 把某个脚本改坏（写成语法错）再执行 | 返回原始 stderr 与非零退出码，AI 如实汇报 |

### B-18 Skill 外部依赖与授权（Services/Skills/SkillDependency.cs + SkillProcess.cs + Windows/SkillAuthWindow，2026-09-20 新增）

> **它解决的是什么**：Skill 的脚本常要调外部 CLI（写飞书要 lark-cli），而这类 CLI 需要"先登录"。
> 阶段一交付时这块**完全是空的** —— 后果是真机实测里，模型照着 CLI 输出里的提示，
> 让用户去终端敲 `lark-cli auth login`，还把开发机上的安装目录拼进了命令里。
> **宿主该做的事被外包给用户，不是措辞问题，是机制缺一块。**

> 涉及文件：`Services/Skills/SkillDependency.cs`（依赖基类：定位 / 起进程探测 / 静态匹配 / 展示；**2026-09-21 起协议已拆出**）、
> `Services/Skills/IDepAuthFlow.cs`（授权协议接口 + `DepPrepareResult` + 公开的文本解析 `DepAuthText`，2026-09-21 新）、
> `Services/Skills/LarkDeviceCodeFlow.cs`（飞书协议实现：状态解析含 `NotConfigured` / 设备码 / 二维码 / 登出 / 前置配置 `config init --new`，2026-09-21 新）、
> `Services/Skills/SkillProcess.cs`（子进程统一装配：清宿主标记 + PATH 前置 + 超时杀树；**按行流式读 + 超时保留已读**，2026-09-21 增）、
> `Services/Skills/SkillRuntimeLocations.cs`（运行时部件候选目录唯一入口：自带 → 数据目录 → 系统 PATH，2026-09-21 新）、
> `Windows/SkillAuthWindow.xaml(.cs)`（授权窗口：**先准备应用凭据、再扫码**；第一次用停在选择面板；
> 兜底入口按协议字段渲染输入框；失败写日志 R9 + 文案同步 R10。2026-09-21 改）、
> `Services/Skills/SkillScriptRunner.cs`（第 10 条规则：依赖预检闸 + PATH 注入 + 授权类失败补话术）、
> `Services/Skills/SkillManifest.cs`（系统提示硬约束）、`Services/Skills/SkillTools.cs`（load_skill 预告依赖）、
> `Services/Skills/SkillRuntime.cs` + `Windows/SettingsWindow.xaml(.cs)`（文案纪律 + 依赖面板 + **「恢复内置技能」出口**，2026-09-21 增）、
> `Services/Skills/BuiltinSkills.cs`（内置 Skill 的落地与恢复：目标不存在才复制 / 恢复前先备份，2026-09-21 新）、
> `Services/Skills/RuntimeRecipe.cs` + `Services/Skills/RuntimeDownloader.cs`（**运行时按需下载**：
> 配方 = 源顺序 / 归档怎么装 / 解压后删什么 / 自检；下载器 = 多源兜底 + 进度 + 暂存目录 +
> **自检通过才提交** + 永不抛，2026-09-21 新）、
> `builtin-skills\lark-cli\`（**桥接官方 lark-cli 的 Skill**：`SKILL.md` 只讲"怎么调用 + 纪律"、
> `scripts\lark.py` 是纯透传壳 —— 业务知识一律来自官方内嵌的 28 个技能，我们不维护，2026-09-21 新）、
> `tools/fetch-lark-cli.ps1` + `.bat`（从 npmmirror 拉官方二进制，约 47MB，落 `runtime\lark-cli\`）。

> **四条红线（改这块之前必读）**：
> ① **不许把"环境准备/授权"外包给用户** —— 面向用户与面向模型的文案里**不得出现**命令行、终端步骤、
>    `tools\*.bat` 这类开发者路径（慢层有两条断言守着）。理由是实测：模型会把文案原样抄进回答里，
>    而用户执行不了 —— 这类失真比 bug 更贵。
> ② **定位顺序固定：应用自带 `runtime\<id>\` → 数据目录 `<数据根>\runtime\<id>\`（按需下载落这里，2026-09-21 扩档）→ 系统 PATH。候选规则唯一入口 = `SkillRuntimeLocations`。绝不硬编码"别的应用安装目录"** ——
>    依赖别人装过什么既不可移植，也让行为取决于别人装了什么（"别拿开发机当基准"的具体落点）。
> ③ **状态判据是字段、不是退出码；且「还没配应用凭据」必须单独成一态**（2026-09-21 补）：`NotConfigured` 与 `Unknown`
>    混在一起，用户看到的就是"状态查询失败"，而该做的是"先去创建一个应用"——那正是当初那场事故的文案面。
>    判据本身仍是 JSON 字段、不是退出码：`auth status` 在"用户身份缺失"时**照样返回 0**，
>    只看退出码会得出"一切正常"。读不懂输出时判为**未知**（不乐观也不悲观），绝不猜。
> ④ **未授权时不得执行脚本**；授权完成后要在**同一次工具调用里**继续执行（用户不用把话再说一遍）。
>    另外：**不传依赖表时不做预检** —— 预检宁可少拦一次，也不能把本来能跑的 Skill 拦死。
> ⑤ **协议归实现、机制归基类；依赖表是注册式**（2026-09-21 增）：授权相关的命令与字段名**不许再回到基类**里
>    —— 那会让"通用依赖层"重新变成"飞书专用层"，第二个 CLI 接进来又得改基类。
>    加依赖 = 写一个类 + `SkillDependencies.Register(...)` 一行，**不需要动 `All()`**。
>    （`ProbeAsync` 保留 `virtual` 是有意的：有的依赖"在不在位"不需要起进程。）
> ⑥ **先准备应用凭据，再扫码；准备失败不许往下走出码**（2026-09-21 增）：没有应用凭据时
>    `auth login` 连 device_code 都拿不到，硬走下去的形态是"界面停在正在生成二维码"（用户白等）。
>    窗口的 `Stage` / `QrPlaceholderMessage` / `CredentialInputCount` 三个公开成员**是给检查点用的**
>    （它们描述的正是用户看到的东西），**不要当死代码清理掉**。
> ⑦ **凭据纪律**：兜底入口的敏感值只经 stdin 交给 CLI，不进命令行、不进日志、**不进任何日志文件**；
>    日志落盘前先做 scrub（CLI 有回显的可能），输入控件用后立即清空。

> **⚠️ 一个会咬人的版本差异（都实测过）**：lark-cli 的 `auth qrcode -o` **1.0.92 拒绝绝对路径**
> （`unsafe output path: --output must be a relative path within the current directory`，退出码 2），
> **1.0.96 接受绝对路径**。实现统一用「相对名 + CWD 设到临时目录」—— 两个版本都成立。
> **别把"CLI 只收相对路径"记成永恒事实**：我第一版就是这么断言的，被检查点当场逮住并改正。

> **机器可验的部分**：慢层组 `Skill 依赖`（**54** 条，2026-09-21 两次扩容 30 → 36 → 53 → 54）
> + 慢层组 `Skill 授权窗口`（**12** 条，2026-09-21 新增，STA 线程）
> + 慢层组 `内置技能`（**10** 条，2026-09-21 新增）+ 慢层组 `运行时下载`（**8** 条，2026-09-21 新增）。
> `Skill 依赖` 覆盖：四条红线 + 二维码产物位置与失败语义
> + **候选目录顺序（自带 → 数据目录）与子进程 PATH 全前置**（让脚本把 `PATH` 打出来断言，不靠读代码）
> + **`NotConfigured` 与 `Unknown` 分开**（含"错误 JSON 走 stderr 也要认出来"）
> + **验证链接提取**（真实那行 / 无链接 / 非 http(s) / null 四种输入）+ **前置配置在已配置机器上必须什么都不做**
> + **无授权协议的依赖判"在位即可用"**（守"基类里不再写死任何 CLI 命令"）+ **注册式依赖表**（加一项 / 不挤掉内置 / 同 Id 去重）
> + **兜底入口的字段描述**（appId 明文 + appSecret 敏感）。
> `Skill 授权窗口` 覆盖：**准备失败必须停在失败且不出码** / 已配好时照常进扫码 / 第一次用停在选择面板不自动开跑 /
> **失败写日志并带上阶段**（R9，看沙箱里真实日志文件）/ **失败后占位文案必须改掉**（R10）/ **布局高度自校准**（选择面板不许比出码布局更高 + 出码布局不许超出窗口高；只靠测量，`RenderTargetBitmap` 查不出裁切 —— 溢出部分在快照里照样画得出来）。
> `内置技能` 覆盖（用的是**仓库里真实的那份文件**，所以它同时在守"随包分发的到底是什么"）：
> 内置技能源里有 `lark-cli` / 能落地到数据目录 / 落地产物能被扫描器认出且带上 `lark.py` /
> **能从脚本文本里认出「要用 lark-cli」这条依赖**（认不出就不会走授权预检，未授权也硬跑 →
> 拿一串英文报错回来，实测模型会照着自己的猜测编解决办法）/ **桥接脚本不许硬编码别的应用安装目录**（B-18 红线机器化）/
> csproj 的复制规则存在、**不带 `Condition`**、且是 `CopyToOutputDirectory=PreserveNewest` /
> **落地动作必须排在 `--snapshot`·`--dragprobe` 之后**。
> ⚠ 最后那条是**源码顺序断言**（不是产物证明，本项目一般不信读代码）：因为这条约束没有产物级验法 ——
> 那两个开发期模式在 `UiSnapshot.Run` / `DragProbe.Run` 内部才把数据根指到临时沙箱，
> 落盘动作排在它们前面就会拿**真实数据根**去写。2026-09-21 实测踩到：跑一次 `dev.ps1 snap`，
> 真实数据目录里凭空多出 `Skills\lark-cli\`（快照本该零副作用）。
> 另有四组地基防线在快层：`[7] 运行时部件候选目录`（7 条）+ `[8] 子进程流式读与超时保留`（6 条）+ `[9] 内置 Skill 落地与恢复`（16 条）+ `[10] 运行时按需下载`（20 条）——
> 改了 `SkillProcess` / `SkillRuntimeLocations` / `SkillDependency` 的候选逻辑、`BuiltinSkills`、或下载器与配方，跑快层即可，不必等慢层。
> **授权完成那一步、以及"AI 真的用飞书办成一件事"只能人工验**（要真扫码），见下表。

> ⚠ **2026-09-21 实测（探针只读跑了一次 `auth status`，真实用户目录）**：未登录时 `identities.user` 只有
> `status` / `available` / `message` / `hint` 四个字段，**没有 `userName`**，而 `ParseStatus` 读的正是 `userName`
> → 下面验收表里「已授权：<用户名>」这条**能不能显示出用户名尚未验证**（本机当前是未登录态，验不了；**不猜字段名**）。
> 同一次实测顺带坐实：**成功 JSON 走 stdout、错误 JSON 走 stderr**（未配置时 `not_configured` 走 stderr、退出码 3），
> 所以 `ProbeAsync` / `StartAuthAsync` 读 `Stdout` 是对的 —— 设计稿 §2.3 那句「JSON 输出走 stderr」应理解为「**错误** JSON」。

| 验收动作 | 期望现象 |
|---|---|
| 未授权时在 AI 问答里说「记一下 XXX」 | **自动弹出二维码窗口**（不是让用户去敲命令）；窗口里有码、有链接、有倒计时 |
| ⚠ **第一次用**（先退出登录并让凭据变回"未配置"）点授权 | 窗口**停在选择面板**（不是自动开跑）：两个按钮「创建飞书应用（推荐）」/「我有现成凭据」<br>→ 点前者：**拉起浏览器**，完成后这里**自动出二维码**；期间界面明说「这是两步」 |
| ⚠ **兜底入口**：点「我有现成凭据」粘 appId+secret | 保存后自动进入扫码；**日志文件里搜不到 secret**（只有长度）；输入框保存后已清空 |
| 用飞书扫码并在手机上点「确认授权」 | 窗口**自动关闭**（不用回来点任何按钮），脚本继续执行，飞书多维表格里出现该条记录 |
| 授权过程中点「取消」 | 脚本**不执行**，AI 如实说"未完成授权、没有执行"，绝不出现"已完成" |
| 设置 → AI 模型 → Skill 扩展 → 外部依赖 | 显示 `飞书（lark-cli）：已授权：<用户名>（应用自带）`，并有「退出登录」可撤销 |
| 点「退出登录」后再说一次 | 重新弹二维码（撤销后必须重新授权才可用） |
| 在设置里点某 Skill 的「撤销」再执行它 | 重新弹授权确认 |
| ⚠ **首次启动后**看设置 → AI 模型 → Skill 扩展 | 技能数比之前多 1，清单里有 `lark-cli`（随包分发的桥接技能已落地到数据目录） |
| ⚠ 在 AI 问答里说「飞书里有哪些技能」之类 | AI 会**先读官方技能再回答**（不是凭记忆编）；这证明 `lark.py` 透传 + 官方 28 个技能这条通路是通的 |
| ⚠ 让 AI 办一件真事（例：往多维表格里加一条） | **写操作前会先问用户**；用户确认后飞书里真的出现该条；全程**没有**把命令/终端步骤写给用户 |
| ⚠ 改坏 `Skills\lark-cli\SKILL.md` 后点「恢复内置技能」 | 弹确认框（写清覆盖什么、备份到哪）→ 确认后技能回到出厂版本，**改坏的那份躺在 `<数据根>\Skills_backup\<时间戳>\` 里**，且技能清单里**不会多出第二行** |
| ⚠ **按需下载**：把 `runtime\` 挪走（或删掉数据目录下的 `runtime\`）后打开设置页 | 外部依赖行显示「未找到」+「下载组件」按钮；点它 → **行内进度**（百分比递增）→ 完成后状态变成「已授权 / 未授权」而不再是「未找到」。Python 那行同理 |
| ⚠ 下载过程中拔网线（或断流） | 行内显示失败原因，**不留半成品**（目标目录要么没建、要么是完整的）；再点一次能重试 |
| ⚠ 发布产物体积（`dotnet publish`） | **≈ 96 MB**，且 `runtimes\` 下**只有 win-x64**（2026-09-21 修前是 347.7 MB / 8 个平台；这条只能人工量） |
| 把 `runtime\python` 整个删掉，再执行含脚本的 Skill | 明确报「未检测到可用的 Python 运行时」并给出获取方式；**不得出现任何「成功」字样** |

> **关于「精简包 / 完整包」开关（2026-09-21 定：本步不做）**：现在连"发版"这个动作本身都还没进流程
> （没有发布脚本、没有安装包形态），此时定包体策略多半会定错 —— 定的时候看不见真实约束
> （安装包多大算大 / 使用者能不能联网 / 要不要离线包）。所以 `runtime\` 要不要随包分发**留到真发版时再定**。
> **但同一次排查顺手修掉了一个真 bug**：publish 曾把 7 个用不上的平台原生库一并产出（347.7 MB 里 251.5 MB 是死重量），
> 那是"包本来就装错了东西"，与策略无关 —— 已修（`TrimForeignRuntimesFromPublish`），产物 96.2 MB。

---

## 五、维护约定

### 谁更新、什么时候更新

| 时机 | 触发信号 | 谁做 | 动作 |
|---|---|---|---|
| 新增功能 | 出现新 .xaml / 设置项新增属性 / 新服务类 | AI（强制） | 补条目，标 ⚠ 待验证；**能自动化的同时补检查点**（第二节规则 3） |
| 行为变更 | 改了默认值或交互流程 | AI（强制） | 同步改对应条目 |
| 实测发现错 | 跑清单时发现描述与实际不符 | 你 / AI | 当场改，去掉 ⚠ |
| 定期体检 | 每季度，或大重构后 | AI | 重扫枢纽引用数 |

**约束**：清单没同步更新的，视为交付未完成（见项目根 `AGENTS.md` 红线 5）；**新增功能没补检查点，同样视为交付未完成**（第二节规则 3）。

### 写条目的两条规矩

1. **描述行为，不写死数值**。设置里能改的值（时长、秒数、尺寸、模型名、路径）一律写成「按设置里配的 X（默认 Y）」。写死数值，设置一改清单就骗人
2. **A 级不超过 20 条**。超了必须把最不容易坏的降级到 B 级——清单太长没人看完，等同于没有

### 重扫枢纽引用数（一条命令）

怀疑第一节数据过期时，让 AI 跑一次：

```bash
cd <项目目录> && for f in $(git ls-files 'Services/*.cs' 'Services/**/*.cs' 'Models/*.cs'); do
  n=$(basename "$f" .cs)
  c=$(grep -rl --exclude-dir=obj --exclude-dir=bin --exclude-dir=asr_venv "$n" --include=*.cs --include=*.xaml . 2>/dev/null | wc -l)
  echo "$c $n"
done | sort -rn | head -20
```

### 人工修正条款的维护

第一节「人工修正」中被强制升 L2 的文件（WebDAVProvider / CryptoService / Dpapi），若将来出现替代实现或架构调整，需同步修订该条款。

### 其他

- 标 ⚠ 的条目：按文件命名推断，未经实机验证。首次使用后按实际表现修正，并去掉标记
- 本文件随项目进 git，改动需走 feature 分支（见项目根 `AGENTS.md` 红线 1–2）
