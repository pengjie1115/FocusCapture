# 升级与迁移指引 —— 行身份改造（2026-09-12）

> 适用版本：`feature/sync-line-identity`（含 `SyncNote.ComputeId` 去掉相对路径、未到期待办定位修复、
> 原地改行补墓碑、空上传时刻存量补齐）。老版本升级到本版本时按本文做一次迁移，之后无需再操作。

---

## 一、这次修了什么

| 修复项 | 现象（用户实测） |
|---|---|
| 行身份不再绑定文件路径 | 待办点「已完成」过一会又变回未办、每轮多留一份已办副本 |
| 删除/编辑定位改全库扫行 | 本机创建的未到期待办删不掉（弹「未在笔记文件中找到该条目」） |
| 原地改行补删除墓碑 | 跨端删除不生效（B 端删了 A 端仍在） |
| 空「上传时刻」存量补齐 | 老云端记录每次同步都被无条件投递（永不衰减） |

## 二、为什么要按顺序做

旧版本写进云端的记录，**ID 里含文件路径**（如 `灵感_2026-09-15.md`）；新版本不再含路径。
两种口径的记录会并存：旧记录虽然不会再造成"身份分裂"，但**仍可能把老版本的行投递回本地**。
所以升级后需要做一次**清理 + 重置同步**，把云端重建成新口径。顺序不能颠倒：

- 先清理、后重置 → 云端按"清理干净的本机状态"重建（正确）
- 先重置、后清理 → 云端会留下已删行的记录，他端又落回来（错误）

## 三、准备：给另一台电脑装新版

在开发机（本机）执行：

```
dotnet publish FocusCapture.csproj -c Release -r win-x64 --self-contained false -o "D:\桌面文件存放位置260518\FocusCapture-修复版"
```

把 `FocusCapture-修复版` 整个文件夹拷到另一台电脑，替换原程序文件夹（数据不在这里，不受影响）。
另一台电脑需要已装 .NET 8 桌面运行时（它本来就在跑这个应用，通常已有）。

## 四、迁移步骤

> 全程只需 10 分钟左右。每一步都写了"为什么"，出问题时才好判断该不该继续。

| # | 做什么 | 为什么 |
|---|---|---|
| 1 | 本机装新版并启动一次，确认能正常打开、笔记都在 | 先确认程序本身没问题，再动数据 |
| 2 | 等一次同步跑完（约 1 分钟）。若那几条老待办又变回"未办"，**属预期**，先不管 | 旧云端记录的最后一次投递，后面两步会清掉 |
| 3 | **关闭应用**，双击 `tools\run-todo-cleanup.bat` 看预览报告 | 预览只读、不改任何文件；先确认要删的就是那些重复行 |
| 4 | 确认无误后执行清理：`powershell -NoProfile -ExecutionPolicy Bypass -File tools\todo-cleanup.ps1 -Apply` | 同一条待办只保留一份（优先保留「已办」）；执行前自动整份备份到 `.cleanup-backup-<时间戳>\` |
| 5 | 启动应用 → 设置 → 云同步 → 点「**重置同步状态**」（会二次确认） | 清空云端旧格式记录，并用本机清理后的状态全量重传 —— 这一步做完云端才彻底干净 |
| 6 | 打开另一台电脑的应用 → 同步一次；如那台也有重复/复活的待办，在那台上同样跑一次第 3–4 步 | 两台都干净后，云端与两端才会完全一致 |

## 五、验收自查（现象级，看到才算过）

| 检查 | 期望现象 |
|---|---|
| 那几条老待办在待办汇总里点「已完成」 | 过 30 秒刷新仍是「已办」，不再变回未办 |
| 重复副本 | 灵感速览面板里同一条待办只剩一行，不再有多份已办 |
| 今天新建一条提醒日在下周的待办，然后右键删除 | 删除成功，不再弹「未在笔记文件中找到该条目」 |
| 在另一台电脑删掉本机建的未到期待办 | 本机同步一次后该条也没了 |
| 云端状态 | 云同步页显示「成功」，无「N 条云端数据解密失败」提示 |

## 六、出问题怎么办

| 情况 | 处置 |
|---|---|
| 清理工具删错了 | `.cleanup-backup-<时间戳>\` 里有受影响 md 文件的完整备份，整份拷回即可 |
| 应用起不来 / 行为异常 | 把程序文件夹换回旧版本（数据文件格式未变，可直接回退） |
| 云端数据想回到升级前 | 旧记录在重置同步后已从云端删除，无法找回；但**本机 md 是事实源**，本地数据始终完整，必要时用重置同步把本机状态重新推上云 |

## 七、决策记录：为什么不做"以云端为准的整文件覆盖"

曾评估用"最简单"的整文件覆盖（云端为权威）替代行级合并，**结论是不采用**：

1. 它把"最后写入者赢"换成"云端赢"，**丢数据场景反而增加**：断网期间在本机写的内容，下次启动被云端覆盖（回收站里都没有）；新设备或配置丢失时云端为空 = 清空本机；两台都离线写过 → 输家的内容永久消失。定时上传/退出上传/启动下拉都是事后补偿，"本机写了但还没传上去"这个窗口永远存在。
2. 它解决不了本次的两个 bug：根因是"同一行在两台设备落进不同文件 → 身份分裂"，与合并策略无关；整文件覆盖反而会把重复副本"正当地"同步到所有端。
3. 与产品定位冲突：本地优先 / 离线可用 / 云端只是镜像；且授权码即钥匙，云端不具权威性。

该简化的是**行的身份**（已改为只由行文本决定），不是合并策略。
